# A 股口径统一：撤销 14:30 禁新仓 + 仓位改风险预算法

日期：2026-09-16　触发：老罗「A 股也一样，和美股没有区别，有这种 setup 就应该
准备入场，14:30 是不合理规则禁止新仓，否则到了点位没法买入」

---

## 一、撤销「14:30 后不许新开仓」

### 为什么它错

这条线把**时间**当成了否决理由，但挂单式的通道（突破单、回踩确认）**只看价不看钟**：
盘前把条件单挂好，价格自己来找。若 14:35 才成交，那说明点位到得晚 —— 因为
「14:30 已过」而拒单，等于**白等一整天**。

它的合理内核（尾盘成交 = 隔夜 T+1 敞口更大）**保留为提示，不保留为否决**。

### 代码改动

| 位置 | 旧 | 新 |
|---|---|---|
| 通道 C（A 股） | `elif late: → ❌ 已过 14:30，不开新仓` | 分支删除；放行后在成功行下加提示 |
| 突破预案单 | `时段: 14:30 后不再新开仓` | `时段: 全天有效（含尾盘）—— 挂单只看价不看钟` |

新提示文案：

```
⏰ 触发于 14:35（14:30 后）—— **仍可买**（挂单/回踩不看钟）；
   但离收盘不足 30 分钟，隔夜 T+1 敞口更大，可自行减半仓
```

### 顺带发现：文档写了代码没实现的规则

`SKILL.md` 原写 A 股量能突变「时间窗 ≥15 分钟，**且 14:30 前**」，但代码里
A 股通道 A 只有下界 `10:00`，**根本没有 14:30 上界**。文档与代码不一致 —— 已修文档。

---

## 二、仓位口径统一（风险预算法）

### 旧口径的问题

「先手仓 = 计划总仓的 1/3」—— 一个与风险无关的比例；且不传 `--qty` 就算不出股数。

### 新口径（与美股 `us_lots` 同构，5 处调用点全部替换）

```
股数 = min( 账户 × 1.5% / (入场 − 硬止损) ,  账户 × 上限 / 入场 )
       ↓ 再向下取整到最小申报单位
```

| | A 股 | 美股 |
|---|---|---|
| 风险预算 | 1.5% | 1.5% |
| 仓位上限 | **30%**（T+1 隔夜跌停 −10% 兜底） | 50% |
| 申报单位 | 科创板 688/689 = **200 股**，主板/创业板 = **100 股** | 1 股 |

**算不出 1 手**（1 手即超上限）→ 明确输出「不做」，并说明是**钱不够**这个硬约束。

### 顺带修掉一个真 bug：科创板 200 股从未生效

`lots_for` 里 `FIRST_LOT = 100` 是硬编码，**所有 A 股都按 100 股取整**。
原代码在通道 C 处有一行 hack：

```python
n = min(n, FIRST_LOT * (2 if code.startswith(("300", "301", "688", "689")) else 1))
```

这行**把创业板 300/301 也当成 200 股（错，创业板是 100 股）**，而且只作用于通道 C，
其余 4 处完全没有约束。

新增 `first_lot_of(code)`：688/689 → 200，其余 → 100。5 处全部走 `ash_lots`。

---

## 三、意外发现：涨停锚用错了价（影响最大）

### 现象

002961（瑞达期货）9/16 涨停收 20.97，收盘后跑次日预案：

```
旧：⚠ 触发价 21.55 距涨停 20.97 只剩 -2.77%（<1.5%）—— 贴板买不到，本通道放弃
     ↑ 负数百分比，且把「今天已封住的价」当成了「明天的涨停价」
```

### 根因

```python
lim = ash_limit_price(code, snap.get("prev") or basis[-1]["c"])
```

`snap` 来自新浪实时接口。收盘后 `snap = {prev: 19.06, spot: 20.97,
date: '2026-09-16', time: '15:36'}` —— `prev` 是 **9/15** 的收盘。

于是 `lim = 19.06 × 1.1 = 20.97` = **今天已经封住的那个价**。
次日（9/17）真实涨停价应是 `20.97 × 1.1 = 23.07`。

**后果**：次日若开在 21.50（+2.5%），代码判「开盘 ≥ 20.97（涨停）→ 撤单」，
而 21.50 距真实涨停 23.07 还有 7.3% —— **误撤单，正是「点位到了却买不到」**。

### 修法：统一取 `basis[-1]["c"]`，三种时态全对

`basis` 已是：`daily[:-1] if live else daily`

| 时态 | basis | basis[-1] 收盘 | 含义 |
|---|---|---|---|
| 实时盘中 | `daily[:-1]` | 昨日 | = 今日涨停基准 ✓ |
| 收盘后 | `daily` | 当日 | = **次日**涨停基准 ✓ |
| 历史回放 | 截至 asof | 对应日 | ✓ |

且 `snap["prev"]` 在**历史回放**时完全错 —— 它返回的是「此刻」的快照，与回放日期无关。
三处调用点（突破预案单 / 量能突变 / 关键位突破）已全部改为 `basis[-1]["c"]`。

### 修复效果（002961，同一份数据）

| | 修复前 | 修复后 |
|---|---|---|
| 涨停价 | 20.97 ❌ | **23.07** ✓ |
| 通道结论 | 贴板放弃 | **★ 强 setup** |
| 触发 / 止损 | — | 21.55 / 21.26 |
| 股数 | — | 600 股 = 12,930 元（25.9%） |
| 最大亏损 | — | 174 元（0.35%） |
| 目标 / 盈亏比 | — | 22.97 / **4.90:1** |

---

## 四、验证

| 项 | 结果 |
|---|---|
| 回归测试 | **168 断言全绿**（144 → 168，新增 4 个用例） |
| 002961 | 700 股（预案单）/ 600 股（突破单） |
| 300723（创业板） | 400 股 = 13,612 元（27.2%），取整到 100 ✓ |
| 688002（科创板） | **不做 —— 最小 1 手 200 股 = 36,638 元 = 账户 73%** ✓ 物理约束正确拦截 |
| 603228 / 601233 / 002415 | 买区已作废，无预案单（正常） |
| 美股 ILMN | 7 股 = $1,518（30.4%），不受影响 ✓ |
| A 股非盘中路径 | 不崩 ✓ |

---

## 五、待老罗定的事（我没有擅自决定）

### A 股 5 万账户下，「风险预算法」对窄止损的单子会退化成「固定 30%」

| 标的 | 入场 | 止损 | 每股风险 | 风险预算法 | 仓位闸 30% | 取整后 | **实际风险占账户** |
|---|---|---|---|---|---|---|---|
| 002961 | 19.84 | 19.02 | 0.82 | 914 股 | 756 | **700 股** | 1.15% |
| 300723 | 34.03 | 33.44 | 0.59 | 1,271 股 | 440 | **400 股** | **0.47%** ← 被闸门夹住 |

止损越窄（<2%），风险预算法算出的股数越大，几乎必然顶到 30% 闸门 ——
此时**闸门才是实际约束，1.5% 风险预算形同虚设**，先手仓一次就占 27%+。

这未必是坏事（集中度被 30% 守住），但它意味着「先手」不再是小仓试探。
若想保持小仓试探，需要加一条更小的上限（如账户 10%）或把风险预算减半。
**这一段等老罗定，代码暂按上表默认执行。**

## 六、诚实交代

1. `ASH_RISK_PCT = 1.5%` / `ASH_MAX_POS = 30%` 是**沿用原有依据**（1.5% 抄美股、
   30% 由 T+1 跌停 −3% 倒推），不是回测出来的。
2. A 股通道 C 的「ok」分支至今**没有真实数据样本**（池里的票分别落到
   pending / failed / 未突破），只被合成用例覆盖。14:30 撤销因此在真实数据上
   未能实测 —— 防回退靠源码断言（`test_ash_late_is_not_a_ban`）。
3. 涨停锚修复只验证了 002961 一个案例。
