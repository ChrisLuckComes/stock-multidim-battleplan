# AGENTS.md · stock-multidim-battleplan

本仓库独立操作约定。**不要**依赖或回写上级目录 `D:\code\AGENTS.md` 里的股票条款；股票相关规则只维护本文件。

---

## 0. Non-negotiables

1. **No flattery, no filler.** 直接答或直接做。
2. **Disagree when you disagree.** 用户前提错了先说清楚。
3. **Never fabricate.** 路径、哈希、测试结果、API 名不得编造。
4. **Stop when confused.** 两种合理解释且影响产出时先问。
5. **Touch only what you must.** 每行改动必须能追溯到用户请求。
6. **No meaningless changes.** 能跑的原代码，非需求不重构、不「顺手优化」。

---

## 1. Stack / Commands

- Language: Python 3.8+
- 无外部付费数据源；A 股东方财富，美股 Yahoo
- 测试：`python -m pytest test_*.py -q` 或单文件 `python test_review_fixes.py`
- 探测：`python probe_intraday.py <code>`；账户额度见 `account_config.py` / `.env.example`

---

## 2. 账户配置边界

- **人与钱**（账户、主力/备用、单笔硬顶、风险预算%）→ `env` / `.env`（`account_config.py`）。
- **策略常数**（ATR 倍数、通道阈值、`ASH_RESERVE_TIER`、均线闸门等）→ 代码，不进 `.env`。
- 美股无额外单笔比例上限：仓位只受 `US_ACCOUNT` 全额约束（默认 $5000）；旧 50% 闸门已移除。

---

## 3. Project Learnings（本仓库）

审查/修复类任务做完且测试通过后直接 `commit` + `push`，不要再问「要不要提交/推送」。临时产物（`out_*.json`、cache、data 缓存）不入库。

- 硬止损必须有 gap：锚三选一（**MA5** / **阳线下沿** / **大阳中点=(高+低)/2**），再下 0.10×ATR；沿线回踩默认 MA5；很长突破大阳用中点（含平台突破单）。盘中破且 3 分钟不收回才走；破了禁止换锚改宽。
- 个股作战计划强制扫雷：大额解禁、减持、立案调查及其他重大利空；有雷且窗口临近则降仓或 wait，不得只凭技术面给买入。
- 强制双轨打分：投资价值与短线博弈各 0–10 分并写理由；禁止用涨停次数抬高投资价值分，禁止只打一个总分。
- 点名个股时必须找同板块/概念龙头（1–3 只）做近期强度与趋势轻量对照；不得默认运行完整单票流程，只有用户显式要求同行完整分析时才逐票跑 `rule123`、扫雷、双轨打分和计划卡。
- 日线快照落盘后，probe 必须 `--data` 复用，禁止再调 `bars_from_us` 取日线；主标的研究检索与分钟 probe 并行启动，默认不搜同行财报。
- 沿线回踩若锚为 MA5/EMA10/SMA20：须 `regime=continuation` 或收盘>SMA20，否则 `recommend=False`（防弱票贴短均线假 T1；反例科华数据 002335）。
- `recommend=False` 时不得打印可执行下单指令（挂单价/股数当成交单）。
- 低开 ≠ 低吸：低开本身不是买入理由，须落入买区且模式成立。
- 美股 `session=Open` 时必须把**今日未收盘 bar 合成并入结构**（`merge_intraday_bar`，5 分钟线聚合）再判模式/买区：日线源盘中不返回当日半根，只按昨收算会让突破当天的买区滞后一整天，输出「永远等回踩」（INTC 2026-09-17）。盘前/盘后与 `Closed` **不合成**（稀疏成交不能当「收盘站上」）；合成 bar 的 `v` 按已走时段折算成量比口径，`v_raw` 才是累计量。
- 赔率优先于形态信仰：突破后高波动导致止损被迫放宽（≥8% 才抗洗）时，优先前移买点，仍映射六种 mode；**前移主渠道 = `pre_breakout` 预案单（与 mode 并存、买在突破位不追高）与 `line_pullback`**；盘中通道 A/B/C 单笔 R 更小但回测净账户为负，只可小仓试错；禁止用事后突破倒推「昨天就该预判买」。
- 提交前检查 diff：业务代码不得含临时 mock / 调试残留；测试里的正式 fixture 可保留。
- 报告类字段必须与引擎同步：`scan_all/scanner.py` 负责把新字段（`pre_breakout` / `struct_exec` / `hard_exec` / `hard_dist_atr` / `hard_noise`）导出到 `results.jsonl`，`scan_all/report.py` 负责渲染；**只改引擎不改这两处 = 全市场扫描用不到该功能**（CLI/probe 能打印不等于报表能看）。
- 美股必须有 HTML 报告通道（2026-09-22 补）：`battle_analyze.py` 原来只收 6 位 A 股代码，美股 ticker 直接 SystemExit ⇒ 美股永远只能拿 `rule123.py --out` 的 JSON 手搓，出不了报告。现由 `is_us_code()` 分流，美股走 `bars_source.us_quote` / `probe_intraday.probe_us` / `probe_intraday.us_lots`（1 股起、无整手、`$`、无 T+1）。`report_render.py` 按 `meta.market` 切换币种、交易制度文案与「超 40% 仓位习惯线」提示（该提示是 A 股 30%×跌停 10% 倒推的，美股两条前提都不成立）。**给美股出报告一律走两步流程，禁止手写 HTML。**
- 「硬止损贴噪声带」（距买区下沿 <0.25×ATR）**只对突破类成立**（`BREAKOUT_MODES`）：突破类买区下沿＝突破位＝真实成交价；回踩类买在线上、主风控是收盘破线的结构止损，硬止损本就是 0.10×ATR 毛刺滤网，套突破类阈值会 1/3 误报（2026-09-18 样本 44 候选误报 15）。
- **禁止把引擎 anchors 里的「基准日实体中点」当买入档**：`battle_analyze.py:219` 把这个价标注为 **断头铡刀位**（跌破即否定这根大阳，是出场参考，不是支撑位），而技能里合法的「大阳中点」是 `(高+低)/2`（`rule123.py:1944`）—— 两者不是一个价。反例 603067 振华股份 2026-09-23（涨停收 39.86，(开+收)/2 = 38.08 被当低吸档写进报告，当日撤回）。
- **涨停/大阳次日的「回踩低吸档」在写进计划前必须先做「触发概率 + 逆选择」双检验**：在同批样本上算 P(盘中触及买入价) 与 P(盘中触及上方突破位)，再看「曾触及买入价」子样本的收盘中位/收红率。实证（603067 自身 24 个涨停样本 / `data/` 池 38 个）：触及 −4.47% 的低吸档只有 **37.5% / 13.2%** 概率给到，且该子样本收盘中位 **−2.51% / −5.49%**、收红率从无条件 58% / 61% 掉到 **22% / 20%**（日内还不修复）；同期「冲上 +2.89%」的突破档却有 **66.7% / 71.1%** 概率触发、收盘中位 **+4.76% / +3.94%**。⇒ **低价挂单成交的那一支，往往就是「这波失败」的那一支；不要用「低吸成交概率高」自欺。**
- **涨停次日的买入档必须按「次日开盘跳空」分三路统计，不许只算「盘中触达率」就定档**：路 A＝开盘即 ≥ 触发价（**真实成交价是开盘价，不是触发价**）、路 B＝开盘 < 触发价而盘中上穿（成交价＝触发价）、路 C＝全天未触（不参与）。603067 实证（自身 24 个涨停样本 / `data/` 池 38 个）：A 占 **21% / 21%**、以开盘价买入的胜率 **80% / 75%**；B 占 **46% / 50%**、胜率只有 **55% / 53%**；C 占 **33% / 29%**。**跳空过位那一档胜率反而高一个台阶**（差异在确定性不在幅度 —— 两路收盘中位几乎相同 +0.88% vs +0.98%），但 A 仅 **5/8 例**、且跳空幅度与当日表现几乎无关（r +0.12~+0.16、R² 1~2.5%）⇒ **只能说「场景有效」，不能说「幅度有规律」**。
- **集合竞价段买入的执行约束**：限价买单**只有上限、没有下限** ⇒ **禁止在 09:25 前预挂「触发价上限」单**（开盘低开时也会成交，等于自动买进最差的路 C）；且 09:20–09:25 不可撤单、09:25–09:30 不接受申报 ⇒ 正解是 **09:25 看开盘价 → 09:30 手动打**。买价上沿一律按 **R** 定、不按固定价格定（603067：买 41.01 → R 4.16；41.80 → 2.33；42.29 → 1.59），上沿附近必须「浮盈 ≥2% 即把止损提到成本价」。
- **持仓止损不许按「成本 ± X%」写，必须按锚（MA5 / 阳线下沿 / 大阳中点）− 0.10×ATR 写**：2026-09-23 持仓 4 只里 2 只踩坑 —— 688428 诺诚健华写 **32.00**（低于其硬止损 32.68，方向是「按成本放宽」）、301165 锐捷网络写 **114.23**（仅 0.06×ATR，落在噪声带内，当日收 114.16 即被扫掉）。**「距现价 < 0.25×ATR」的噪声带检查必须在建仓/上线当天做、并按锚重设，不许等它被破了再改宽**（破后禁止换锚改宽 = risk-exit.md 铁律，不许用「当初定得太贴」当改宽理由）。止损合规性只认两个数：**锚位 − 0.10×ATR**，以及**距现价 ≥ 0.25×ATR**。
- **集合竞价段卖出的执行约束（与买入那条对称）**：限价卖单**只有下限、没有上限** —— 挂 113.02 卖出，若开盘 116 也以 **116** 成交 ⇒ ① 若要「无条件出局」，09:15–09:20 挂一个**低于任何可能开盘价**的卖单（最稳＝当日跌停价，301165 即 114.16×0.8 = **91.33**）即可**保证以开盘价成交**、连 09:30 都不用盯；**挂太高会不成交** —— 挂昨收 −5%（108.45）遇开盘 −8% 就废单，反而要 09:30 补一刀 ⇒ 卖单挂价必须 ≤ 可能的最低开盘价，而集合竞价是**单一定价撮合**、成交价恒为开盘价，所以「挂低价」不会卖亏；② **「高开就不卖」不靠「不挂单」实现，靠「09:15–09:20 可撤窗口内先挂后撤」实现** —— 09:15 挂跌停价单「占位」→ 09:19:xx 看虚拟开盘参考价 → 达门槛就**撤单**留着、不达门槛就不撤（09:20 后深市不可撤、09:25 撮合出开盘价）；**09:20 之后才挂 = 锁死**，**09:25–09:30 深交所交易主机不接受任何申报**（深证会〔2016〕138 号）⇒「09:30 再手动卖」拿不到开盘价（那是连续竞价价）。残余风险必须接受：09:20–09:25 虚拟价仍会变（09:19 看着绿、09:24 拉红 ⇒ 卖飞；反向 ⇒ 留亏），故挂单/撤单都别卡在 9:19:59、留 30 秒余量。**「开盘不上攻就走」的「上攻」必须换成一个具体价格门槛**（用结构锚，如 MA5），不许留模糊词 —— 否则等于给已触发的止损开例外（301165 锐捷网络 2026-09-24：门槛定 115.56＝MA5，开盘低于它一律清，站上它才暂留一日且**止损维持 114.23 不许改宽**）。
- **持仓止损已破后，评估「次日不等、开盘即处置」要用「开盘卖 vs 收盘卖」的自有样本差值判断是否值得等**：差值 = 当日 `c/o − 1` 的中位。301165 自身 500 根实证：全样本 +0.29%、weak 态（前收<MA20 且 MA20 下行）跳空 ≤−1% 那档只有 **+0.17%**，而该档**收红率仅 18%、盘中最低中位 −4.49%** ⇒ **开盘卖几乎不付代价，却免掉深水回撤与全天盯盘**；反之「跳空 ≥+1%」的 weak 子样本收红率 **88%**、前收→收中位 **+2.09%**，所以「上攻才留」不是拍脑袋、有统计支撑（但 n 仅 25，且留仓不得放宽止损）。
- **「红开就留、绿开就卖」的门槛必须用统计定，不许用「任意红开」**：301165 自身 500 根、weak 态（前收<MA20 且 MA20 下行，最贴近它 2026-09-23 的状态）实证 —— 跳空 ≤−1% 档 n=28：收盘守住止损仅 **18%**、前收→收 −1.50%、盘中最低中位 −4.49%；−1%~0 档 n=57：守住 **42%**；**0~+1% 微红档 n=45：守住 62%**（区分度弱、近抛硬币，不足以当留仓理由）；≥+1% 档 n=25：守住 **84%**、收红 88%；**≥+1.23%（=MA5 115.56）档 n=20：守住 90%**。⇒ 门槛定在**结构锚（MA5）**，不是 0%；「微红开」与「绿开」在留仓胜率上没有本质差别。
- 「开盘动作」的执行流程统一成一条：**09:15 挂占位单 → 09:19:xx 按门槛决定撤/不撤 → 09:25 出开盘价**。买入侧同理（但买单是「只有上限」：09:20 前挂触发价单会被低开直接吃成路 C，所以买入**只在下方买点**用竞价挂单，上方突破买必须 09:30 后手动打）。
- **回踩类（`line_pullback`）的「买区下沿」可能低于它自己的结构止损 ⇒ 该档是「开仓即止损」，必须单独检查并否掉**（2026-09-23 跑 300661 圣邦股份暴露）：引擎给 买区下沿 **121.37**，而 `stop_plan.struct` = MA5 **121.67**（收盘破）—— **止损比买价还高**；配套硬止损 121.08 距买价只有 0.29 = **0.05×ATR**。而赔率表那一行用的止损是「触发日前一根K低点 119.0」（另一套口径），所以**赔率表看着能过（R→t1 1.03）、`stop_plan` 却不可执行**。⇒ 判据：`hard_dist_atr < 0.10` 就是这个病（与突破类的 `hard_noise` 阈值 0.25 是两个概念 —— 回踩类硬止损本就是 0.10×ATR 的毛刺滤网，**小于它就说明锚贴在买价上、买价被上抬到了锚下方**）；凡是 `odds` 的 `warn` 里出现「买价≤结构止损位（开仓即止损口径）」，该档一律不作主推，改走 T0 / `pre_breakout`。
- **引擎 `targets.t1` 可能低于 T0 的触发价 ⇒ `R→t1` 算出负数或 <1 是口径产物，不是「赔率差」，不许据此否定计划、更不许照抄成目标位**：300661 圣邦股份 2026-09-23，t1 = **123.8**（08-14 枢轴），而 T0「过昨高」的触发价 = **124.29** —— 目标比触发还低，赔率表里突破档的 `r1` 直接是 **−0.63**。⇒ 遇到 `t1 ≤ 触发价`，必须改用 `struct.levels` 之上的**真实阻力阶梯**重算 R（该票为 127.47 → 127.77 → 136.3 → 143.0 → 151.9），并在报告口径里写明「引擎 t1 已失效」。同理，`buy_zone.primary_lo < struct` 与 `t1 < 入场价` 这两类自相矛盾，都要在出报告前各查一次。
- **「低吸能不能做」不看风格偏好、看两条硬指标（均线阶梯宽度 + 触发概率），且必须与突破档实算 R 比大小**：① **阶梯宽度 = (MA5 − EMA10) ÷ ATR ≥ 0.25×ATR**，低吸档才有地方放止损；< 0.25×ATR 时要么「无处放止损」（603067 振华 0.01×ATR：MA5 36.70 ≈ EMA10 36.67），要么只能把止损退到更远的大阳下沿（36.70→35.65，需回落 −7.93% / 触发概率 8.3% = 等不到）。② **触发概率 ≥ 40%**，否则「等不到的价 = 0」，不得当主路径。③ **不许默认「买得低 = 赔率好」**：300661 圣邦阶梯宽 **0.79×ATR** 最足，但止损必须退到 EMA10（风险 4.30）⇒ 低吸 R **3.40** 反被突破档（风险 3.21）的 **3.74** 盖过；而 301607 富特（0.33×ATR）低吸 R **2.73** 完胜其突破档 **0.25**、300378 鼎捷（0.17×ATR，止损用 08-31 大阳中点 37.25 而非 EMA10）低吸 R **5.27~6.82** 完胜突破档 **1.76**。⇒ **同一只票低吸/突破都要算，取 R 高的那条，别按喜好选。**
- **「挂均线价等回踩」这个动作本身零超额、且带逆选择，必须配「收回确认」才有正期望**（2026-09-23 实证：`data/` 池 45 只 + 4 只点名票，400 根日线）：全池 n=6306（上升趋势，收>MA20）—— 直接买中位 **+0.14%** / 均 **+1.36%**，挂 MA5 等回踩中位 **0.00%**（成交率仅 **55.9%**，未成交计 0）/ 均 **+0.71%**；且**没等到的那批更强**（若直接买中位 **+0.26%** vs 成交批 **+0.15%**）⇒ 逆选择在「是否给到成交」这一层就存在。但**按成交折价分档，收益单调递增**（折价 = 买入日收盘 ÷ 实际成交价 − 1）：折价 <0（成交价比当日收盘还贵 = 弱势票）中位 **−1.83%** / 胜率 41%；0~0.5% **+0.92%** / 55%；1~2% **+1.21%** / 57%；2~5% **+3.42%** / 64%；>5% **+8.41%** / 78%。⇒ **低吸的价值不在「价低」、在「回踩后能被收回」**，正确形态是「价格进入买区 **且当日收盘收回买区上沿**」的确认型，不是「价格到了就买」。**回测限价单成交价必须按 `min(开盘价, 挂单价)` 建模**（跳空低开时成交价更优），否则会高估低吸成本、「等回踩」显得毫无价值。
用户纠正后：把新规则写成**一行具体指令**追加到本节；已有条目则收紧，不堆抽象空话。
- **长期均线若落在「最近一根大阳的最低点」（证伪线）下方，它就不构成回踩买区——不是「更深的机会」而是「结构已经死了的价」**（2026-09-24 跑 300661 圣邦股份暴露）：09-18 大阳 开 118.20 / 高 127.47 / **低 116.50** / 收 125.13（+7.44%），**证伪线 = 116.50**；而 MA10 **115.12** / MA20 **113.10** / MA50 **109.79** 三个 **全在证伪线下方**（MA20 低 **2.92%** / 3.40 元）。相对现价 122.45 的完整降序是：MA5 121.67（−0.64%，−0.13×ATR）→ EMA10 117.37（−4.15%，−0.86×ATR）→ **116.50 证伪** → MA10 115.12（−5.99%，−1.25×ATR）→ MA20 113.10（−7.64%，−1.59×ATR）→ MA50 109.79（−2.15×ATR）。⇒ **要摸到 MA20，必须先把证伪线 116.50 跌破**；那一刻「突破→回踩均线」的计划已作废，正确动作是**出池**，不是把买点/止损往下挪一格（挪 = 换锚改宽，铁律禁止）。配套三条实证：① 触及概率（自有 400 根，从「收>MA5 且近20日内有≥5%大阳」出发、均线滚动）——前瞻 5 日摸到 MA20 只有 **36%**、10 日 **47%**、20 日 **63%**；② 在 MA20 买到底值不值（收盘首破 MA20，n=**14**）——后 5 日中位 **−1.31%**、上涨仅 **43%**（买早了先亏），后 10/20 日中位 +7.97% / +11.31%、71% 上涨，**但这段样本恰好全在个股上升趋势里，反弹是趋势自带的、不是 MA20 这个位置的功劳，n=14 又太小 ⇒ 严禁把它当作「MA20 低吸有效」的证据**，且尾部 −23.4% / −24.5%；③ 退一步看证伪线**上方**的唯一合法回踩位 EMA10 117.37，距 116.50 只有 0.87 元 = **0.15×ATR < 0.25×ATR** ⇒ 按上一条阶梯宽度判据，**低吸档根本放不下止损**。⇒ 综合结论：圣邦只剩两个分支 —— **守住 116.50**（结构活，但低吸档无处放止损，只剩 T0 过昨高 124.29，且需美股转好）或 **跌破 116.50**（证伪出池）；**MA20 113.10 不属于任何一个分支**。将来若在 P0/P1 低点 105.67 / 104.22 一带重新筑底并重新站上均线，那是**新标的、新计划**，必须重跑引擎，不许旧计划改锚续用。
