# AGENTS.md · stock-multidim-battleplan

本仓库独立操作约定。**不要**依赖或回写上级目录 `D:\code\AGENTS.md` 里的股票条款；股票相关规则只维护本文件。

---

## 0. Non-negotiables

1. **No flattery, no filler.** 直接答或直接做。
2. **Disagree when you disagree.** 用户前提错了先说清楚。
3. **Never fabricate.** 路径、哈希、测试结果、API 名不得编造。
4. **Stop when confused.** 两种合理解释且影响产出时先问。
5. **Touch only what you must.** 每行改动必须能追溯到用户请求。
6. **No meaningless changes.** 能跑的原代码，非需求不重构、不「顺手优化」。

---

## 1. Stack / Commands

- Language: Python 3.8+
- 无外部付费数据源；A 股东方财富，美股 Yahoo
- 测试：`python -m pytest test_*.py -q` 或单文件 `python test_review_fixes.py`
- 探测：`python probe_intraday.py <code>`；账户额度见 `account_config.py` / `.env.example`

---

## 2. 账户配置边界

- **人与钱**（账户、主力/备用、单笔硬顶、风险预算%）→ `env` / `.env`（`account_config.py`）。
- **策略常数**（ATR 倍数、通道阈值、`ASH_RESERVE_TIER`、均线闸门等）→ 代码，不进 `.env`。
- 美股无额外单笔比例上限：仓位只受 `US_ACCOUNT` 全额约束（默认 $5000）；旧 50% 闸门已移除。

---

## 3. Project Learnings（本仓库）

审查/修复类任务做完且测试通过后直接 `commit` + `push`，不要再问「要不要提交/推送」。临时产物（`out_*.json`、cache、data 缓存）不入库。

- 硬止损必须有 gap：锚三选一（**MA5** / **阳线下沿** / **大阳中点=(高+低)/2**），再下 0.10×ATR；沿线回踩默认 MA5；很长突破大阳用中点（含平台突破单）。盘中破且 3 分钟不收回才走；破了禁止换锚改宽。
- 个股作战计划强制扫雷：大额解禁、减持、立案调查、**标的自身的「利好公告反应史」**（上次同类公告当日是否高开低走/放量收阴，有前科则公告当日禁止追入）及其他重大利空；有雷且窗口临近则降仓或 wait，不得只凭技术面给买入。午间消息出来后、下单前必须先扫黑历史和利好出尽嫌疑（公告前已带量下跌、午后买盘是兑现窗口）；任一命中当日不做，包括 13:00（诺诚 688428，2026-09-24）。
- 消息驱动型标的（BD 授权 / 对外合作 / 大额订单 / 业绩预告 / 政策批文）下单前必跑 `python pre_runup.py <code> --date <公告日> [--bench <板块指数>]`：**公告前 5 日 ≥10% 或 10 日 ≥15% ⇒ 强抢跑，公告日禁止追入**（消息前已有涨幅 = 内幕盘已建仓，公告日 = 兑现窗口；科创板创新药 / Biotech 的 BD 类最典型）；5 日 ≥6% 或 10 日 ≥10% ⇒ 只做公告后 ≥5 分钟第二结构且仓位减半。判据是**否决理由，不是买入理由**。
- 买点选择前必跑 `python stock_character.py <code>`（股性体检，两个维度）：**① 突破后行为** —— 取历史单日涨幅 ≥5% 的大阳，以「大阳当日收盘」为买点算**赔率 = 后5日最高价均值 ÷ |后5日最低价均值|**。**赔率 ≥1.5 且延续率 ≥50% ⇒ 突破加速型**（可用突破买）；**赔率 <1.0 或延续率 <35% ⇒ 拉高消化型**（突破当日禁止追入，买点只放回踩 MA10/MA20）；其余为混合型（优先回踩）。睿创微纳 688002 赔率 **0.87**（<1.0 单独命中）属消化型（2026-09 最大亏损源 −1,528.71）。**② 利好兑现习惯** —— 自动抓公告筛利好事件（授权许可/合作/中标/业绩预增/获批/回购增持），定位**真实反应日**（公告日异动算当日，否则算次日），算**「反应日收盘买入」后 5 日期望**：**期望 <0 且后 5 日为正 <50% ⇒ 出消息即顶型，公告日禁止追入**；期望 ≥0 且后 5 日为正 ≥55% ⇒ 利好有效型；其余为混合型。诺诚健华 688428 8 次利好事件 **7 次后 5 日为负**（期望 −3.79%）属出消息即顶型。**样本 <3 个标注「仅供参考」，不得作为否决理由**。两个维度互相独立（博济/东材/有研硅同为加速型，利好习惯却是有效/即顶/有效）。**判据只决定用哪种买法、能不能在公告日动手，不决定买不买**。公告抓取**刻意不做早停**（2026-09-24 实测：命中 3 个即停会把天元宠物 301335 从「出消息即顶型」翻成「混合型」，错向是鼓励交易；「近期无利好即跳过」会漏掉振华 603067 第 3~4 页的 2 个事件）—— 省时用 `--no-ann` 或 24h 缓存，不加隐式早停。回归 `python test_stock_character.py`。**输出顶部先给【结论】两句话（突破买能不能做 / 出消息能不能追），冲击率已从人读输出撤掉**（既非判据也不改类；老罗 2026-09-24：「我看不懂，打出来没意义，我要的是结论」）。
- 选股参考清单三因子（细则 `references/stock-selection.md`）—— **以趋势为准，三条均为参考指标，非门禁**：① **市值**粗筛 `总市值 ≤ 1000 亿`（老罗口径「大票」= 超 1000 亿，超了只是不优先，非禁买）；`FreeLtgb × 现价` 自由流通市值仅作弹性细看的补充（锐捷 301165 总市值 1,249 亿属大票但浮筹仅 149.9 亿 = 12.0%）。② **股东户数**（`tdx_security_deep_info`）**必须叠趋势读，单看增减无意义** —— 上升趋势中户数增长是常态（锐捷 2025 主升段股价 +54% 而户数 +83%，若当门禁会误杀），仅「横盘/下跌 + 户数增」偏派发且只作警惕项。③ **距 52 周高点 ≤15%** ⇒ 上方套牢盘偏薄（**同样仅参考**：套牢盘是**动态量**，会被下跌换手消解，静态看会让「下跌过一段的票永远涨不起来」；放量站上关键位即为吃掉套牢区，属买点）。**三条均为参考，任一不通过都不构成否决**，能否决交易的只有硬约束（钱不够 / 涨停买不到 / 结构已坏）。
- **顶部标志 K 线（墓碑线 / 上吊线 / 射击之星 + 变种）与「次日确认制」**（`top_signals.py`，2026-09-25 新增，细则 `references/top-signals.md`）：**第一批三根**是「冲高被卖回」的同族表达，差别只在实体厚薄与影线方向 —— **墓碑线**（几乎无实体 + 长上影 + 收在低位 `pos≤0.25`）、**射击之星**（实体小 0.10~0.35 根幅 + 长上影 + 低位 `pos≤0.35`）、**上吊线**（长下影 ≥0.5 根幅 + 小实体居区间上端 `pos≥0.55`、几乎无上影）；**第二批两根**（2026-09-25 追加）—— **顶部十字星** `doji`（实体 ≤0.05 根幅，老罗定「次级高危」）、**大阴线** `big_yin`（阴线 + 实体 ≥0.55 根幅 + 收位 ≤0.30，**上影不设门槛** —— 原 `≤0.20` 已于 2026-09-25 取消，老罗定「顶级高危」）。形态优先级**固定不可换**：墓碑→射击之星→上吊线→大阴线→**十字星（最后）**—— 否则会把墓碑线的 `doji` 变种、上吊线的 `dragonfly` 变种全吞掉；十字星也**没有**「均衡」变种（`ur+lr+body_r≡1`，不可达）。四道闸门：① 形态比例（`classify_shape`）② 位置＝**是这一波最高点**（`is_wave_high`，左侧 60 根内无更高高点，老罗追加）③ 量能档（巨量 ≥2.0×20日均量 / 放量 ≥1.5 / 平量 / 缩量）④ **次日确认**（`confirm_state`：其后收盘 < 信号收盘 = 走弱 ⇒ `confirmed`；其后收盘收复信号最高价 = 反包 ⇒ `invalidated`）。**实证推翻了四处直觉**（`python research_top_candles.py --universe sample` 可复现，393 只随机全A / 169,247 根日线 / 后 5 日，各组一律用**同位置对照组**）：① **形态当日没有预测力**（相对「是高点但无形态」的对照超额 **+0.68%、t=1.59 不显著**，中位 −0.98%、−5% 概率 28.5% 比对照差但右尾同样更厚 ⇒ 双向放大，不是方向信号）；② **「必须是这一波最高点」这道位置闸门不成立**（闸门内 +0.68 vs 闸门外 **+0.48、t=3.39** —— 非高点组反而更稳）—— 但它仍是老罗要的定义，**保留为标注 `is_top`，不作否决依据**；③ **「次日走弱」才是那个信号、且与形态无关**（有形态 −6.64% vs 无形态 −6.95%，**t=0.43** ⇒ 三根形态在「走弱」之上一点信息都没多加；反包组 +1.37% ⇒ 推翻成立）；④ **第二批两形态没有改变上面任何一条**（当日同样无预测力：大阴线 +0.06/t=0.11、十字星 −0.25/t=−0.40；**次日走弱确认后** −5.91% / −8.66%，与对照 −6.95% 同量级）⇒ **五根一视同仁、都走同一条次日确认制**。⚠️ 一度实现过「大阴线跌幅当日已实现 ⇒ 当日自确认、不等次日」，**实证推翻、已删**：当日 −0.28% 无信息，而 **80.9% 的大阴线其后会被反包（+1.75%）**，跳过确认 = 误杀 4/5。⇒ **veto 语义默认 `confirmed`**（只在「波峰 + 次日走弱」否决 recommend；`veto="signal"`＝信号当日即否决，回测不支持、默认关）。接入方式＝**出口包装**：`plan_entry` 改名 `_plan_entry_core`，新 `plan_entry = apply_top_signal_gate(_plan_entry_core(...))`，闸门落在 T0 接管**之后**（避免在 25 个 return 处逐一打补丁）；命中时 `recommend=False`、`top_signal_veto=True`、`pre_breakout["suppressed_by"]="top_signal"`，并挂 `result["top_signal"]`，`evaluate` 透传 `top_signal`/`top_signal_veto`。**`BLOCK_TIERS=("极高","高")`＝已确认档**才够格否决（**五形态同样适用**；`pattern_grade` 的「顶级/标准/次级」只是**显示标签**，回测**不支持**它排序预测力）；`_size_factor` 已确认→0 / 巨量未确认→0.5 / 放量→0.75（advisory，不自动生效）；**未确认档只降档预警、不改 recommend**（同项目既有口径：波动系数 ≠ 准入闸门）。`live_last=True` 时确认扫描只看 `bars[:-1]`（盘中半日 bar 不判「次日走弱」，否则一探底就误报「顶部确认」）。**筛选层统一入口 `top_signals.screen_exclude`；顶分型统一入口 `top_signals.top_fractal(s)` —— 本仓库 7 处筛选脚本的重复顶分型实现已全部收敛到它（`screen_mlcc/kc50/semi/storage/device/refine` + `check_tf_601208`，改一处不再漏六处）。** 全市场扫描：`scan_all/scanner.py` 导出 `top_state/top_block/top_veto/top_pattern/top_date/top_vol/top_rvol/top_prob/top_invalidation/top_size_factor/top_summary`，`scan_all/report.py` 渲染第 16 列三态（已确认·否决 / 待确认·预警 / 已推翻）。**单票硬指标**（老罗 2026-09-25：「这个判断顶部的逻辑可以单独提取作为硬指标，我在跑个股的时候，如果出现这种要避雷」）：`top_signals.top_verdict()` 是**单票唯一结论出口**，独立 CLI `python top_signal_check.py <code> [--us] [--strict] [--json] [-v]`（`--data x.json` 可离线跑）—— **退出码 = level（`ok=0` / `alert=1` / `block=2`，多票取最大，取数失败 3），可直接用于流程拦截**；`battle_analyze.summarize()`、`report_render.build_top_signal()`（首屏「最高盈亏比路径」下方独立卡片 + 左边框按档变色 + 首屏徽标 + 一页汇总一行）、`probe_intraday._print_top_signal()`（A 股 `probe` / 美股 `probe_us`，并写进 `out["top_signal"]`）三处**一律读它**，不得各自解释 `plan["top_signal"]`；**已推翻（`invalidated`）回落成 `ok`、不报预警**（报成预警就是把方向说反），`alert` 只预警缩仓、不否决买点。`--strict` ＝ `veto="signal"`（信号当日即 `block`，默认关）；`--holding` / `--flat` 只改 `advice` 文案、**不改 level**。回归 `python test_top_signals.py`（**47 项**）+ `python test_top_signal_gate.py`（9 项）+ `python test_top_signal_check.py`（15 项）+ `python test_sndk_top_sequence.py`（12 项）+ `python test_lj_top_sequence.py`（12 项）+ `python test_casebook_top_sequence.py`（**34 项**）。★★ **2026-09-25 第三批修订（老罗又报 5 个亲身案例：顺丰 002352 / 茅台 600519 / 石头科技 688169 / 中国平安 601318 / 比亚迪 002594）**：① **取消大阴线的上影门槛**（原 `upper_r ≤ 0.20`）—— 数学上多余（`upper_r = 1−body_r−pos`，`body_r ≥0.55`、`pos ≥0` ⇒ `upper_r ≤ 0.45` 恒成立），且恰好排除掉最典型的顶部形态「冲高回落」（**茅台 2021-02-18 历史最高 2627.88 当天：实体 0.718 / 下影 0.037 / 收位 0.037 / 位置=波峰 全部达标，只因上影 0.245 被卡掉 ⇒ 改前引擎全程零反应**；改后 02-18 `alert` → 02-19 `block`）；大样本（波峰大阴线 1,575 例）确认率 18.1%→**22.5%**、确认后 −7.29%→−6.54%（同量级）、样本 **1.56×** ⇒ 不稀释、覆盖更完整；变种三分 `textbook`/`long_body`/`spike`，`BY_UPPER_SPIKE` **只作标签不作门槛**。② **射击之星的「下影≤0.15 / 收位≤0.35」暂不放宽**（比亚迪 2022-06-13 的 358.86 高点因此仍未命中）：放宽后确认率 13.4%→13.8% 不塌、确认后 −6.51%→−7.13% 略更负，但波峰候选 **1,512→3,249（×2.15 ⇒ 全组合 `alert`/缩仓翻倍）**、差异统计上不显著（SE≈1.5~1.7）⇒ **不因单一案例改阈值**，记为**已知未覆盖样本**。③ ★★ **结构性发现：五个「断头铡刀」案例里，唯一四要件齐备的那一根（石头科技 2021-06-23）恰恰不是波峰；而三个真波峰（顺丰 02-18 / 茅台 02-18 / 平安 11-30）四要件一个都不齐** ⇒「铡刀」是**均线关系**定义、「顶部标志 K 线」是**位置+次日行为**定义，**两套定义互不对齐**。④ **平安 2020-11-30 是确认制漏掉的一次历史大顶**（波峰 + 教科书射击之星 + 量比 2.24 巨量；估成交额 ≈140.7 亿，老罗记的 143.16 亿对应均价 93.0、落在当日区间内 ⇒ 对得上），但次日反抽 **+3.72%** ⇒ 全程只 `alert`、**未升级 `block`** ⇒ 如实记录「确认制有必然代价」。⑤ 顺丰 2021-02-18 是四个案例里引擎表现最好的（**02-10 上吊线已 `alert`，早 6 个交易日；02-18 当日 `block`**），且老罗「前两根合成吊颈线 / 三根合成超大阴线」两个描述经合并 K 线核验**完全成立**（实体 0.559 / 0.897）。★ **2026-09-25 追加：跳空 / 断头铡刀两组提名全部被否**（源自老罗给的 **SNDK 2026-06 教科书顶部序列** 逐根核对：06-22 跳空十字星 → 06-23 跳空大阴 → 06-25 吊颈线大涨 → 06-26 跳空大阴 → 06-29 吊颈线 → 07-01/07-02 两根断头铡刀）。引擎该抓 6 根、**漏 4 根核心 K 线**，两个共因：**「跳空不参与形态判定」**（跳空低开后反抽把 `upper_r` 撑到 0.45~0.48 卡死大阴线 `≤0.20`，且「实体」只算当日开→收、**跳空口那段跌幅被排除在实体之外**）+ **「位置闸门只认绝对最高点、认不出右肩」**（06-29 吊颈线、07-02 大阴线形态**都识别出来了**，被 `is_wave_high` 判非波峰而丢弃）。**两点均为刻意保留**：1079 只 / 465,317 根日线实测 —— **跳空低开（−1.48% / −1.28%）与跳空高开（−1.66%）一样差**、只有无跳空明显更好（**+0.30%，t 2.46**）⇒ 跳空是**波动**维度（−5% 概率 16.6% ⇒ **42.9%~47.9%**，与「巨量」同构，避雷⑫），**不接 veto**；**断头铡刀**（两连阴 + 破 MA5/MA10/MA20/EMA10 + 前收在均线上方）**3674 例 +0.33%（t −0.10）**、对照「两连阴（全部）」+0.39%/标尺 +0.34% ⇒ **中性偏正，明确否定**（且**仅波峰位置只剩 4 例**，无统计意义）；位置闸门**不放宽**（闸门内 −7.65% vs 闸外 −5.32%，收紧才拿得到最极端的一批）。⇒ **形态族仍是五根、不加第六个**；「跳空低开大阴」在 A 股 1079 只里**仅 25 例**（跌 ≤−8% 只 15 例），**美股个案不可照搬**。**过程中一度算出 t −4.98 的「跳空低开闸门」，因其对照组含「当日跌幅」= 未来信息、且样本外单独看 t 仅 −1.03 而自我推翻 —— 该序列已固化为回归 fixture（12 项、内置 84 根真实日线），改形态阈值或位置口径会触发它失败。**
- **T0 出单前先判「均线刚收复」还是「已沿线上行」；且沿线只是背景、当日形态是事件**（`rule123.ma_ride_state()`，2026-09-24 三轮定稿）：**判据自归一化 —— 看该票与均线的关系状态，不看「今天距均线几个点」**。MA5/EMA10/MA20 **各算一遍**：`20 根斜率 > 0 且近 20 根 ≥12 根收在线上` 即成线，**多条成立时取收盘下方离现价最近、且距现价 ≤1.5×ATR 的那条**（贴着哪条就锚哪条，**不写死五日线**）⇒ `line_ride`；**有线成线但一条都不在可回踩距离内 ⇒ `new_high`（新高·无回踩锚）**，`prefer=breakout`、**不给回踩买点**（老罗：「没有锚点就代表是新高，不需要找锚」；门槛落点：距线 >1.5×ATR 时 5 日内回踩触及率仅 24%、触及后期望转负 −0.22%，振华 603067 09-24 锚 MA20 36.32、距 1.92×ATR 即此类）；三条都不成线、且 MA5 斜率 ≤ 0 且站上 ≤11 根 ⇒ `fresh_reclaim`，T0 照常接管；其余 `mixed`。
  **优先级：当日形态（事件）> 沿线上行（背景）** —— `line_ride` 只在**当日别无买点**时才把入口改道为 `line_pullback`（回踩被选中的那条线挂限价：买区 线−0.05×ATR ~ 线+1.0×ATR，结构止损=收盘破该线，硬止损=线−0.10×ATR），记 `t0_held_for_ride` **且 `result["ride_redirected"]=True`**；**当日已有 `platform_break`/`w_bottom_break`/`flag_tl_break`/`downtrend_tl_break` 且 recommend=True 时不改道**，突破为主、沿线锚仅作突破失败后的次选，写进 `plan["ride_priority"]`（`winner=breakout`）。依据：同日突破买（1.5×ATR 止损）均R +0.212 / 收益 +1.16% / 胜 44%，而「挂限价等回踩」只有 31% 的日子等得到（每机会 +0.39%）。老罗原话：「否则所有股大部分时间都是沿均线走的。」
  ⚠️ **判「是否真改道」必须看 `ride_redirected`**，不能看「`mode == line_pullback` 且有 `t0_held_for_ride`」—— 东材 601208 09-24 当日**本来就有** `line_pullback` 买区（demand 锚 EMA10 52.23），带 `t0_held_for_ride` 却**未改道**，靠 mode 判会把报告徽标误标成「T0 已改道」。
  **字段口径：`line*` = 被选中的那条线，`ma5*` / `above20` / `bounce20` 恒为 MA5 口径 —— 报告与池表不得混用**（本地 `data/` 53 只 A 股池里 21 只成线 ⇒ 剔 1 只超距后 `line_ride` **20** + `new_high` **1**；`line_ride` 里 **16 只锚非 MA5**（EMA10 8 / MA20 8），取 ma5 会报出完全不同的价位）。判别结果挂在 `plan["ma_ride"]` 上，**与 T0 是否成立解耦**（东材 601208 09-24 收盘创 25 根窗口新高 ⇒ 头上无墙 ⇒ T0 不成立，但它就是 `line_ride`；只在 T0 成立时才输出状态，等于这只票恰恰看不到答案）。**旧口径「D0 收盘距 MA5 ≤4%」已作废** —— 4 只票 × 6 个月拟合，扩到 53 只 × 500 根、按真实触发价 + MA5 硬止损回放后**方向反转**（>4% 均R −0.032 vs ≤4% −0.196）；真正的问题是**止损锚太紧**（MA5 锚仅 2.3% ⇒ 77% 被毛刺扫掉），不是「追高」本身。**改道不是否决**（买区在现价下方 = 可预挂限价单）。回归 `python test_ma_ride.py`（29 项），复现 `python research_ride_priority.py`。
- 强制双轨打分：投资价值与短线博弈各 0–10 分并写理由；禁止用涨停次数抬高投资价值分，禁止只打一个总分。
- 点名个股时必须找同板块/概念龙头（1–3 只）做近期强度与趋势轻量对照；不得默认运行完整单票流程，只有用户显式要求同行完整分析时才逐票跑 `rule123`、扫雷、双轨打分和计划卡。
- 日线快照落盘后，probe 必须 `--data` 复用，禁止再调 `bars_from_us` 取日线；主标的研究检索与分钟 probe 并行启动，默认不搜同行财报。
- **快照必须能被判定「取数时刻」，否则不得直接当收盘日线用**：缺 `as_of` 的 spot 型快照（`fetch_ashare.py` / `fetch_market.py` 产物）按**文件 mtime** 兜底 —— mtime 落在末根同一自然日且早于收盘 ⇒ 判为盘中半日 bar、降级走缓存/实时源（`_intraday_mtime`）。缓存侧判据本来就完整（`_check_rules`：末根日期 + 存盘时刻两条），**快照侧此前只有「末根日期」一条**，因此「今天盘中抓的快照」会被当成完整收盘日线 —— 2026-09-24 东材 601208 踩到：10:04 的快照末根收 55.01（量 31.7万手），真实收盘 56.44（量 93.2万手），振华 603067（11:24）/ 有研硅 688432（10:58）同批中招。
- 沿线回踩若锚为 MA5/EMA10/SMA20：须 `regime=continuation` 或收盘>SMA20，否则 `recommend=False`（防弱票贴短均线假 T1；反例科华数据 002335）。
- `recommend=False` 时不得打印可执行下单指令（挂单价/股数当成交单）。
- 低开 ≠ 低吸：低开本身不是买入理由，须落入买区且模式成立。
- 美股 `session=Open` 时必须把**今日未收盘 bar 合成并入结构**（`merge_intraday_bar`，5 分钟线聚合）再判模式/买区：日线源盘中不返回当日半根，只按昨收算会让突破当天的买区滞后一整天，输出「永远等回踩」（INTC 2026-09-17）。盘前/盘后与 `Closed` **不合成**（稀疏成交不能当「收盘站上」）；合成 bar 的 `v` 按已走时段折算成量比口径，`v_raw` 才是累计量。
- 赔率优先于形态信仰：突破后高波动导致止损被迫放宽（≥8% 才抗洗）时，优先前移买点，仍映射六种 mode；**前移主渠道 = `pre_breakout` 预案单（与 mode 并存、买在突破位不追高）与 `line_pullback`**；盘中通道 A/B/C 单笔 R 更小但回测净账户为负，只可小仓试错；禁止用事后突破倒推「昨天就该预判买」。
- 提交前检查 diff：业务代码不得含临时 mock / 调试残留；测试里的正式 fixture 可保留。
- 报告类字段必须与引擎同步：`scan_all/scanner.py` 负责把新字段（`pre_breakout` / `struct_exec` / `hard_exec` / `hard_dist_atr` / `hard_noise`）导出到 `results.jsonl`，`scan_all/report.py` 负责渲染；**只改引擎不改这两处 = 全市场扫描用不到该功能**（CLI/probe 能打印不等于报表能看）。
- 美股必须有 HTML 报告通道（2026-09-22 补）：`battle_analyze.py` 原来只收 6 位 A 股代码，美股 ticker 直接 SystemExit ⇒ 美股永远只能拿 `rule123.py --out` 的 JSON 手搓，出不了报告。现由 `is_us_code()` 分流，美股走 `bars_source.us_quote` / `probe_intraday.probe_us` / `probe_intraday.us_lots`（1 股起、无整手、`$`、无 T+1）。`report_render.py` 按 `meta.market` 切换币种、交易制度文案与「超 40% 仓位习惯线」提示（该提示是 A 股 30%×跌停 10% 倒推的，美股两条前提都不成立）。**给美股出报告一律走两步流程，禁止手写 HTML。**
- 「硬止损贴噪声带」（距买区下沿 <0.25×ATR）**只对突破类成立**（`BREAKOUT_MODES`）：突破类买区下沿＝突破位＝真实成交价；回踩类买在线上、主风控是收盘破线的结构止损，硬止损本就是 0.10×ATR 毛刺滤网，套突破类阈值会 1/3 误报（2026-09-18 样本 44 候选误报 15）。
- **禁止把引擎 anchors 里的「基准日实体中点」当买入档**：`battle_analyze.py:219` 把这个价标注为 **断头铡刀位**（跌破即否定这根大阳，是出场参考，不是支撑位），而技能里合法的「大阳中点」是 `(高+低)/2`（`rule123.py:1944`）—— 两者不是一个价。反例 603067 振华股份 2026-09-23（涨停收 39.86，(开+收)/2 = 38.08 被当低吸档写进报告，当日撤回）。
- **涨停/大阳次日的「回踩低吸档」在写进计划前必须先做「触发概率 + 逆选择」双检验**：在同批样本上算 P(盘中触及买入价) 与 P(盘中触及上方突破位)，再看「曾触及买入价」子样本的收盘中位/收红率。实证（603067 自身 24 个涨停样本 / `data/` 池 38 个）：触及 −4.47% 的低吸档只有 **37.5% / 13.2%** 概率给到，且该子样本收盘中位 **−2.51% / −5.49%**、收红率从无条件 58% / 61% 掉到 **22% / 20%**（日内还不修复）；同期「冲上 +2.89%」的突破档却有 **66.7% / 71.1%** 概率触发、收盘中位 **+4.76% / +3.94%**。⇒ **低价挂单成交的那一支，往往就是「这波失败」的那一支；不要用「低吸成交概率高」自欺。**
- **涨停次日的买入档必须按「次日开盘跳空」分三路统计，不许只算「盘中触达率」就定档**：路 A＝开盘即 ≥ 触发价（**真实成交价是开盘价，不是触发价**）、路 B＝开盘 < 触发价而盘中上穿（成交价＝触发价）、路 C＝全天未触（不参与）。603067 实证（自身 24 个涨停样本 / `data/` 池 38 个）：A 占 **21% / 21%**、以开盘价买入的胜率 **80% / 75%**；B 占 **46% / 50%**、胜率只有 **55% / 53%**；C 占 **33% / 29%**。**跳空过位那一档胜率反而高一个台阶**（差异在确定性不在幅度 —— 两路收盘中位几乎相同 +0.88% vs +0.98%），但 A 仅 **5/8 例**、且跳空幅度与当日表现几乎无关（r +0.12~+0.16、R² 1~2.5%）⇒ **只能说「场景有效」，不能说「幅度有规律」**。
- **集合竞价段买入的执行约束**：限价买单**只有上限、没有下限** ⇒ **禁止在 09:25 前预挂「触发价上限」单**（开盘低开时也会成交，等于自动买进最差的路 C）；且 09:20–09:25 不可撤单、09:25–09:30 不接受申报 ⇒ 正解是 **09:25 看开盘价 → 09:30 手动打**。买价上沿一律按 **R** 定、不按固定价格定（603067：买 41.01 → R 4.16；41.80 → 2.33；42.29 → 1.59），上沿附近必须「浮盈 ≥2% 即把止损提到成本价」。
- **持仓止损不许按「成本 ± X%」写，必须按锚（MA5 / 阳线下沿 / 大阳中点）− 0.10×ATR 写**：2026-09-23 持仓 4 只里 2 只踩坑 —— 688428 诺诚健华写 **32.00**（低于其硬止损 32.68，方向是「按成本放宽」）、301165 锐捷网络写 **114.23**（仅 0.06×ATR，落在噪声带内，当日收 114.16 即被扫掉）。**「距现价 < 0.25×ATR」的噪声带检查必须在建仓/上线当天做、并按锚重设，不许等它被破了再改宽**（破后禁止换锚改宽 = risk-exit.md 铁律，不许用「当初定得太贴」当改宽理由）。止损合规性只认两个数：**锚位 − 0.10×ATR**，以及**距现价 ≥ 0.25×ATR**。
- **集合竞价段卖出的执行约束（与买入那条对称）**：限价卖单**只有下限、没有上限** —— 挂 113.02 卖出，若开盘 116 也以 **116** 成交 ⇒ ① 若要「无条件出局」，09:15–09:20 挂一个**低于任何可能开盘价**的卖单（最稳＝当日跌停价，301165 即 114.16×0.8 = **91.33**）即可**保证以开盘价成交**、连 09:30 都不用盯；**挂太高会不成交** —— 挂昨收 −5%（108.45）遇开盘 −8% 就废单，反而要 09:30 补一刀 ⇒ 卖单挂价必须 ≤ 可能的最低开盘价，而集合竞价是**单一定价撮合**、成交价恒为开盘价，所以「挂低价」不会卖亏；② **「高开就不卖」不靠「不挂单」实现，靠「09:15–09:20 可撤窗口内先挂后撤」实现** —— 09:15 挂跌停价单「占位」→ 09:19:xx 看虚拟开盘参考价 → 达门槛就**撤单**留着、不达门槛就不撤（09:20 后深市不可撤、09:25 撮合出开盘价）；**09:20 之后才挂 = 锁死**，**09:25–09:30 深交所交易主机不接受任何申报**（深证会〔2016〕138 号）⇒「09:30 再手动卖」拿不到开盘价（那是连续竞价价）。残余风险必须接受：09:20–09:25 虚拟价仍会变（09:19 看着绿、09:24 拉红 ⇒ 卖飞；反向 ⇒ 留亏），故挂单/撤单都别卡在 9:19:59、留 30 秒余量。**「开盘不上攻就走」的「上攻」必须换成一个具体价格门槛**（用结构锚，如 MA5），不许留模糊词 —— 否则等于给已触发的止损开例外（301165 锐捷网络 2026-09-24：门槛定 115.56＝MA5，开盘低于它一律清，站上它才暂留一日且**止损维持 114.23 不许改宽**）。
- **持仓止损已破后，评估「次日不等、开盘即处置」要用「开盘卖 vs 收盘卖」的自有样本差值判断是否值得等**：差值 = 当日 `c/o − 1` 的中位。301165 自身 500 根实证：全样本 +0.29%、weak 态（前收<MA20 且 MA20 下行）跳空 ≤−1% 那档只有 **+0.17%**，而该档**收红率仅 18%、盘中最低中位 −4.49%** ⇒ **开盘卖几乎不付代价，却免掉深水回撤与全天盯盘**；反之「跳空 ≥+1%」的 weak 子样本收红率 **88%**、前收→收中位 **+2.09%**，所以「上攻才留」不是拍脑袋、有统计支撑（但 n 仅 25，且留仓不得放宽止损）。
- **「红开就留、绿开就卖」的门槛必须用统计定，不许用「任意红开」**：301165 自身 500 根、weak 态（前收<MA20 且 MA20 下行，最贴近它 2026-09-23 的状态）实证 —— 跳空 ≤−1% 档 n=28：收盘守住止损仅 **18%**、前收→收 −1.50%、盘中最低中位 −4.49%；−1%~0 档 n=57：守住 **42%**；**0~+1% 微红档 n=45：守住 62%**（区分度弱、近抛硬币，不足以当留仓理由）；≥+1% 档 n=25：守住 **84%**、收红 88%；**≥+1.23%（=MA5 115.56）档 n=20：守住 90%**。⇒ 门槛定在**结构锚（MA5）**，不是 0%；「微红开」与「绿开」在留仓胜率上没有本质差别。
- 「开盘动作」的执行流程统一成一条：**09:15 挂占位单 → 09:19:xx 按门槛决定撤/不撤 → 09:25 出开盘价**。买入侧同理（但买单是「只有上限」：09:20 前挂触发价单会被低开直接吃成路 C，所以买入**只在下方买点**用竞价挂单，上方突破买必须 09:30 后手动打）。
- **回踩类（`line_pullback`）的「买区下沿」可能低于它自己的结构止损 ⇒ 该档是「开仓即止损」，必须单独检查并否掉**（2026-09-23 跑 300661 圣邦股份暴露）：引擎给 买区下沿 **121.37**，而 `stop_plan.struct` = MA5 **121.67**（收盘破）—— **止损比买价还高**；配套硬止损 121.08 距买价只有 0.29 = **0.05×ATR**。而赔率表那一行用的止损是「触发日前一根K低点 119.0」（另一套口径），所以**赔率表看着能过（R→t1 1.03）、`stop_plan` 却不可执行**。⇒ 判据：`hard_dist_atr < 0.10` 就是这个病（与突破类的 `hard_noise` 阈值 0.25 是两个概念 —— 回踩类硬止损本就是 0.10×ATR 的毛刺滤网，**小于它就说明锚贴在买价上、买价被上抬到了锚下方**）；凡是 `odds` 的 `warn` 里出现「买价≤结构止损位（开仓即止损口径）」，该档一律不作主推，改走 T0 / `pre_breakout`。
- **引擎 `targets.t1` 可能低于 T0 的触发价 ⇒ `R→t1` 算出负数或 <1 是口径产物，不是「赔率差」，不许据此否定计划、更不许照抄成目标位**：300661 圣邦股份 2026-09-23，t1 = **123.8**（08-14 枢轴），而 T0「过昨高」的触发价 = **124.29** —— 目标比触发还低，赔率表里突破档的 `r1` 直接是 **−0.63**。⇒ 遇到 `t1 ≤ 触发价`，必须改用 `struct.levels` 之上的**真实阻力阶梯**重算 R（该票为 127.47 → 127.77 → 136.3 → 143.0 → 151.9），并在报告口径里写明「引擎 t1 已失效」。同理，`buy_zone.primary_lo < struct` 与 `t1 < 入场价` 这两类自相矛盾，都要在出报告前各查一次。
- **「低吸能不能做」不看风格偏好、看两条硬指标（均线阶梯宽度 + 触发概率），且必须与突破档实算 R 比大小**：① **阶梯宽度 = (MA5 − EMA10) ÷ ATR ≥ 0.25×ATR**，低吸档才有地方放止损；< 0.25×ATR 时要么「无处放止损」（603067 振华 0.01×ATR：MA5 36.70 ≈ EMA10 36.67），要么只能把止损退到更远的大阳下沿（36.70→35.65，需回落 −7.93% / 触发概率 8.3% = 等不到）。② **触发概率 ≥ 40%**，否则「等不到的价 = 0」，不得当主路径。③ **不许默认「买得低 = 赔率好」**：300661 圣邦阶梯宽 **0.79×ATR** 最足，但止损必须退到 EMA10（风险 4.30）⇒ 低吸 R **3.40** 反被突破档（风险 3.21）的 **3.74** 盖过；而 301607 富特（0.33×ATR）低吸 R **2.73** 完胜其突破档 **0.25**、300378 鼎捷（0.17×ATR，止损用 08-31 大阳中点 37.25 而非 EMA10）低吸 R **5.27~6.82** 完胜突破档 **1.76**。⇒ **同一只票低吸/突破都要算，取 R 高的那条，别按喜好选。**
- **「挂均线价等回踩」这个动作本身零超额、且带逆选择，必须配「收回确认」才有正期望**（2026-09-23 实证：`data/` 池 45 只 + 4 只点名票，400 根日线）：全池 n=6306（上升趋势，收>MA20）—— 直接买中位 **+0.14%** / 均 **+1.36%**，挂 MA5 等回踩中位 **0.00%**（成交率仅 **55.9%**，未成交计 0）/ 均 **+0.71%**；且**没等到的那批更强**（若直接买中位 **+0.26%** vs 成交批 **+0.15%**）⇒ 逆选择在「是否给到成交」这一层就存在。但**按成交折价分档，收益单调递增**（折价 = 买入日收盘 ÷ 实际成交价 − 1）：折价 <0（成交价比当日收盘还贵 = 弱势票）中位 **−1.83%** / 胜率 41%；0~0.5% **+0.92%** / 55%；1~2% **+1.21%** / 57%；2~5% **+3.42%** / 64%；>5% **+8.41%** / 78%。⇒ **低吸的价值不在「价低」、在「回踩后能被收回」**，正确形态是「价格进入买区 **且当日收盘收回买区上沿**」的确认型，不是「价格到了就买」。**回测限价单成交价必须按 `min(开盘价, 挂单价)` 建模**（跳空低开时成交价更优），否则会高估低吸成本、「等回踩」显得毫无价值。
- **叠加概率七层计数 `confluence.py`（2026-09-25 新增）——只排序与解释，不作准入闸门、不缩放仓位**。把「这单对上了几层」变成可比的数：**整体市场 → 板块主题 → 领导者 → 催化剂 → 形态 → 量能 → 执行**（顺序固定 = 推文漏斗顺序）。判据全部锚在既有规则上，不引入新标准：市场 ≥55（`market_sentiment.LEVELS` 的「偏强」线）；板块 = 池层共振 ≥2 只；领导者 = 本票 20 日涨幅 ≥ 同板块最强者；催化剂 = 定性（缺省不计）；形态 = `recommend=True` 且 mode 非 wait/none；量能 = RVOL20 ≥1.0 且涨/跌均量比 ≥1.0（<1/1.15 = 派发）；执行 = `R→t1` ≥1.5 且风险 ≥0.25×ATR。
  **两条硬口径**：① `unknown` 层**不进分母但必须显示出来**（终端 / HTML / 池层段落三处都打印），「部分缺数据」**不得**当成「不达标」（量能、执行各缺一项时只按有的那项判）—— 分母静默缩水 = 把「没数据」伪装成「对上了」；② 顶部标志 K 线**是否决权，单独放 `veto` 字段，不与七层相加**。「全对齐」不等于可买，「少对齐」也不等于不能买。
  出口：`battle_analyze` → `summarize()` + `analysis.json["confluence"]`；`report_render` 首屏区块（板块/催化剂单票算不出，靠 `notes.json` 的 `sector_count` / `sector_bucket` / `sector_peers` / `catalyst_has` / `catalyst_note` 注入）；池层 `watch_cn` / `pool_us` 末段【叠加计数】（**池层是唯一拿得到板块共振的地方**）。市场层自动读 `data/sentiment_<基准日>.json`，**日期必须严格一致**，隔日不许冒充当日。回归 `python test_confluence.py`（100 项）。
- 日常口述、复盘口气、事后感想不改代码。只有用户明确要求改代码、改规则、入池或出池时才动文件。
用户纠正后：把新规则写成**一行具体指令**追加到本节；已有条目则收紧，不堆抽象空话。
- **长期均线若落在「最近一根大阳的最低点」（证伪线）下方，它就不构成回踩买区——不是「更深的机会」而是「结构已经死了的价」**（2026-09-24 跑 300661 圣邦股份暴露）：09-18 大阳 开 118.20 / 高 127.47 / **低 116.50** / 收 125.13（+7.44%），**证伪线 = 116.50**；而 MA10 **115.12** / MA20 **113.10** / MA50 **109.79** 三个 **全在证伪线下方**（MA20 低 **2.92%** / 3.40 元）。相对现价 122.45 的完整降序是：MA5 121.67（−0.64%，−0.13×ATR）→ EMA10 117.37（−4.15%，−0.86×ATR）→ **116.50 证伪** → MA10 115.12（−5.99%，−1.25×ATR）→ MA20 113.10（−7.64%，−1.59×ATR）→ MA50 109.79（−2.15×ATR）。⇒ **要摸到 MA20，必须先把证伪线 116.50 跌破**；那一刻「突破→回踩均线」的计划已作废，正确动作是**出池**，不是把买点/止损往下挪一格（挪 = 换锚改宽，铁律禁止）。配套三条实证：① 触及概率（自有 400 根，从「收>MA5 且近20日内有≥5%大阳」出发、均线滚动）——前瞻 5 日摸到 MA20 只有 **36%**、10 日 **47%**、20 日 **63%**；② 在 MA20 买到底值不值（收盘首破 MA20，n=**14**）——后 5 日中位 **−1.31%**、上涨仅 **43%**（买早了先亏），后 10/20 日中位 +7.97% / +11.31%、71% 上涨，**但这段样本恰好全在个股上升趋势里，反弹是趋势自带的、不是 MA20 这个位置的功劳，n=14 又太小 ⇒ 严禁把它当作「MA20 低吸有效」的证据**，且尾部 −23.4% / −24.5%；③ 退一步看证伪线**上方**的唯一合法回踩位 EMA10 117.37，距 116.50 只有 0.87 元 = **0.15×ATR < 0.25×ATR** ⇒ 按上一条阶梯宽度判据，**低吸档根本放不下止损**。⇒ 综合结论：圣邦只剩两个分支 —— **守住 116.50**（结构活，但低吸档无处放止损，只剩 T0 过昨高 124.29，且需美股转好）或 **跌破 116.50**（证伪出池）；**MA20 113.10 不属于任何一个分支**。将来若在 P0/P1 低点 105.67 / 104.22 一带重新筑底并重新站上均线，那是**新标的、新计划**，必须重跑引擎，不许旧计划改锚续用。
- **宏观（利率 / 收益率 / 加息概率）只作背景与波动系数，不作入场闸门（2026-09-25 老罗纠正）**：**加息不代表不涨 —— 能不能买一律以个股与板块自身为准**（趋势 / 结构 / 量能 / 赔率）。⚠️ **已删除**旧规则「高久期/高估值（SaaS 长成长、AI 硬件、半导体）持续承压 ⇒ 硬件线（ANET/HPE/DELL/ALAB/SMCI/MU/SNDK）新建仓默认暂停」—— 老罗原话：「**加息不代表不涨，这个移除，以个股板块为准**」。⇒ **禁止**用宏观对整条产业线做「默认暂停新建仓」式的全局否决；宏观偏紧时用「**等回踩 + 仓位缩小**」承担波动，不是禁止买（与「68% 分位」同一分工：波动系数 ≠ 准入闸门）。**唯二能当闸门的是标的级与硬约束级** —— 个股自身的事件/结构闸门（财报窗口、顶部已确认、结构已坏）与钱不够 / 涨停买不到。
- **出「最佳价格」时必须同时出「开盘作战方案」（2026-09-25 老罗要求）**：老罗原话「下次给最佳价格时，需要制定开盘详细作战方案，向上，向下，刚好符合，竞价买不买等」。⇒ **挂单价只是计划的一半**，另一半是「次日开盘价 O 落在哪、我该怎么办」；不预先写死，临场就靠情绪决策，那正是手痒模式的入口。用 `open_playbook.py`（报告插槽 `OPEN_PLAYBOOK_BLOCK`，CLI `python open_playbook.py <analysis.json> --notes <notes.json> [--md]`），它把上面第 65/67/70 条那些零散的开盘经验**固化成六档一张表**：① 涨停/一字 ⇒ 买不到、撤单；② 跳空高开 >+2% ⇒ 不追、**不上移**挂单价、不回踩就当日放弃并次日重跑引擎；③ 小高开/平开在挂单价上方 ⇒ 不成交等回踩；④ ★刚好符合 ⇒ 按计划成交；⑤ 深低开仍在止损上方 ⇒ 成交但辨缩量/放量、**不因便宜加仓**；⑥ **低开破止损 ⇒ 不接飞刀、开盘前撤单**。⚑ ⑥的判据是硬的：**A 股 T+1，当日买入当日不能卖 ⇒ 当日买入 = 当日不存在止损腿**，所以「低开破止损」不是更便宜而是无法止损 ⇒ 硬约束禁买。集合竞价**默认不买**（竞价无分时结构 + T+1 锁死止损腿 + 开盘价常是当日极值一端）；挂单价在现价下方走隔夜预挂限价即可，根本不用在竞价里抢。⚠ **档位默认取 `odds_recommend`，人工改过锚的档位必须在 notes 的 `open_playbook` 里覆盖** `{"last","entry","stop","target","shares","upper_entry"}` —— 不覆盖会拿引擎默认锚生成一份错的方案（新强联 300850 引擎默认止损 24.44 已被判为 3.91×ATR 过宽不可用，正确锚是 MA20 27.92）。A 股仍是上面六档。美股改走价梯，见下一条。回归 `python test_open_playbook.py`。
- **盈亏比推荐档 A 股与美股分开（2026-09-26）**：A 股当日买入不能卖，仍在可预挂回踩档里取 R 最大的一条，不改这套。美股可随时离场：现价或现价上方的档自己 R≥1.5 就推荐追涨，不因为下面还有 R 更大的回踩而改去等。追涨 R 不到 1.5，才退回回踩档。
- **盈亏比的目标不拿「轻轻一捅就过」的近端压力（2026-09-26）**：距入场 ≤0.5×ATR 的压力位是要穿过的门，默认穿过之后还有一段。下降趋势线突破注明的前高、旗形和 W 底头顶的平台高点同样是门，哪怕超过 0.5×ATR。突破是 T1 主做，因为穿过之后空间打开：门后有更远的摆动高点就用那一档，没有就用入场+6×ATR，不用近端测量涨幅把盈亏比封死。`targets()` 的目标1用门后的下一档；没有下一档、且还没创新高，用入场+2×ATR。平台/W 底的测量涨幅不得把目标再压回这扇门里。更远的真墙仍是目标。**创新高且门后无墙**：上涨空间已打开，目标1改用入场+6×ATR（计算口径，不是预测涨幅），现价盈亏比因此可以很高，不得再按 2×ATR 封死。
- **主线启动猛干领头羊（2026-09-26 中科飞测）**：国产替代把半导体设备打成主线时，领头羊不降级成等深回踩。站上均线的当天或次日买、过前高买（飞测 245）都算主升起来之前的仓，允许猛干；吃到后面任意一段即可。假突破多，只约束还没形成主线的普通个股。引擎落地：① T0 接管时挂 `t0_tail`——次日未过昨高、收盘仍站上三线且高于硬止损 ⇒ 尾盘按收盘成交（飞测 06-15）；② `line_ride` 改道后买区 `fills_policy=limit_reclaim`、限价挂在线上，日内回升触及即成交，不是「开盘必须落在买区内」（兆易 04-29）。不取消改道、不放回窄止损过昨高。回归 `python tests/entry/test_momentum_follow.py`。
- **机构成长股按波浪或沿线做，不按连续大阳去追（2026-09-26）**：中际旭创、东山精密、长飞光纤、剑桥科技、中微公司、长川科技、中科飞测是机构成长股，操盘是波浪或沿线上涨。医药双创里的小盘连拉大阳不是这一类。判别用两条：某只 ETF 里权重 ≥5%，或机构家数远高于连拉小盘。100 家切不开（2026-06-30 东财：七只最低剑桥 626 家；百普 186、奥浦迈 239 已过 100）。数 ETF 只数也没用，小盘会躺在十几只指数 ETF 里、权重只有 0.1%。长飞最大 ETF 权重 2.1%、剑桥 2.2%，这两只靠家数过。
- **股性再分钝和活（2026-09-26）**：中微、中际旭创、长川是钝的，大阳或涨停少，主升沿均线拿，不要等大阳。东山精密、长飞光纤、剑桥科技、有研硅、有研新材股性好，经常大阳或涨停。有研硅、有研新材只按股性归到活的一边，不改成机构股。同一窗口（2025-06-09 至 2026-09-24）：中微涨停 0、10% 以上 8 次；中际涨停 0；长川 10% 以上 10 次。东山涨停 18、长飞 32、剑桥 26、有研新材 15、有研硅 20cm 涨停 7。
- **沿线上涨识别周线和月线（2026-09-26 源杰科技）**：日线成线判据原样用在周线、月线上，结果挂 `ma_ride_week` / `ma_ride_month`，不改日线 mode、recommend、止损。季度及以上不看。周线收盘没破 10 周线、日线却多次破 MA20 的，日线等待不等于周线没在涨。周线回测用源杰科技 688498（2025-06-06 至 2026-06-26，55 周收盘不破 10 周线，引擎认出 50 周，前 5 周到 07-04 还在刚收复）。月线回测用松发股份 603268（2025-07 起收盘不破 5 月线，引擎每个月都是沿线或新高）和南亚新材 688519（2025-02 至 2026-06 收盘不破 5 月线，引擎除 2025-02 为中间态外各月都是沿线或新高，2026-07 收盘才破）。回归 `python tests/entry/test_ride_frames.py`。
- **周线顶最多短线，月线顶从腰斩到三折（2026-09-26）**：周线大阴见顶后不再期待趋势，最多短线。月线一旦见顶（高点当月的墓碑/射击之星/大阴/上吊，或其后的同类月线），跌幅从腰斩到三折都有，而且可以阴跌很久不见底，套牢盘不计其数，再走出一波大行情基本上不可能。中际旭创：高点 2026-06-22 的 1416.88，7 月大阴开 1263.50 收 902.01。茅台 600519 当前前复权：2021-02 月线墓碑（实体 0.013、上影 0.89），高点 2021-02-18 的 2385.65，2026-06-29 低 1151.01，跌掉 52%。赛力斯 601127 前复权最高是 2025-09-30 的 173.55，不是 10 月、日线里也没有 172.63；10 月大阴（开 172.39 收 154.39）就宣布见顶，11 月再一根大阴收到 125.39。2026-09-24 收 46.33，约为高点的 27%，9 月低点 43.61，月线还在跌。`higher_top` 的普通月线顶不改日线 level、不否决日线买点。月线超大阴线例外：大阴线且相对上月**收盘**跌幅 ≥50% 直接拉黑，`recommend=False`，看都不要看（德明利 001309，2026-07 相对 6 月收盘 −59.07%，开 945 收 385.90）。不看月内最低点。闪迪 SNDK 2026-07 收盘相对 6 月收盘 −46.57%（约 47%），月内低点到过 −56%，不过 50% 线，不拉黑。月线超长射击之星或墓碑线一根就拉黑做多、只做空，不需要第二根；其后月线高点创新高才失效。AAOI 2026-05 一根已够（上影 0.79）。ORCL 2025-10 单月上影 0.66。日线射击之星的下影门槛不动，这两根在日线口径是上影十字。相邻两个月合成后是墓碑或射击之星，同样拉黑做多：ORCL 2025-09 与 2025-10 合成开 222、高 345.72、低 218.79、收 262.61，实体 0.32、上影 0.66，比墓碑线实体稍厚。口述高点 341.8 这套日线里没有，最高是 2025-09-10 的 345.72。114.5 是 2026-07-28 的低点，不是 8 月；8 月最低是 08-03 的 130.21。日线形态阈值不动。石头科技 688169 前复权高点是 2021-06-21 的 383.54，不是 374.24；2022-10-31 低 75.59，不是 77.56，约为高点的 20%。4 月大阳、5 月跳空开后收进 4 月实体、6 月才见高点，5 月和 6 月合成不是黄昏之星。2021-08 月线射击之星上影 0.69，一根拉黑，其后没有越过 383.54。
- **美股做多五档，和做空同一套（2026-09-26）**：止损和目标固定后，R≥3 强烈推荐、2≤R<3 推荐、1.5≤R<2 合格、R<1.5 不推荐、价格≤止损放弃（`open_playbook.price_grade`，只在 `market=US`）。前三档买，后两档不买。到价就做，不必死等精确挂单价。盘前、正规时段、盘后、夜盘同一条价，到价的盈亏算数，流动性差只影响滑点。日线结构只认正规时段收盘。半夜不好盯盘：正股拿着睡觉，起来后在盘后确认是否打到止损再处理；2 倍 ETF 睡觉前无论盈亏都平仓。
- **飙股三判据自动化 `gates/surge_pick.py`（2026-09-26）**：飙股 = ①业绩超预期（**大加分项、非必要**）②股性好易大涨/涨停 ③板块领头羊。工具一次给出全部所属板块（剔指数/风格/资金/地域/事件标签、按成分家数从少到多；**只有成分 8~400 家的才用来定龙头**）+ 各板块动量分排名与主标的分位 + 业绩预告代理。**三条都是加分/排序，不是闸门**；**板块 20 日为负时必须写明「排名靠前也不算真领头羊」**；**分位 ≥0.8 且动量分为正才算「是」**（688361 在半导体设备内第 3/13、分位 85%，但板块 20 日 −2.4% ⇒ 只够「边缘」）。动量分口径沿用 `research/screen_wind_dual_2026-09-25.py`：`(个股20日 − 板块20日) + 距60日高 + max(0, 距MA20)`，距 60 日高是负数直接相加、不许取负变加分。业绩只能取预告（本仓库无卖方一致预期），**必须注明是代理口径、不是超预期**；同一报告期优先 `PREDICT_FINANCE_CODE=="004"`（归母净利润），否则会把「扣非续亏/营收预增/归母扭亏」揉成假的「混合 −16.8%~19.4%」。排名是**抽样**的（先按东财 60 日涨幅取前列再算 20 日），必须随输出说明。报告与【结论】顺序固定为 ①业绩 → ②股性 → ③板块领头羊。CLI `python gates/surge_pick.py <code> [--no-net]`；回归 `python tests/screen/test_surge_pick.py`（24 项）。
- **周/月线见顶的「失效」比较必须含未走完的当月（2026-09-26 修）**：月内（周内）高点只可能往上走，**当下部分月的高点就是月底高点的下界** ⇒ 拿它判「其后创新高 ⇒ 失效」是可靠的；但**未走完的当月不得当信号候选**（不能宣布见顶）。旧实现把未走完的当月整根丢掉，于是行情正在创新高的票仍被标「拉黑，只做空」——方向说反。实测 300647 超频三：2026-01 月线超长射击之星高 7.79、窗口前高 8.42，2026-09（未走完）高点已 9.94 ⇒ 修前报「拉黑」、修后正确失效；本地 52 只池拉黑数 27→26→（连同下一条）21。同一修法用于 `_frame_top`（周线/月线见顶的波峰也在含当根的全序列里找）。**信号候选另有约束：`month_one_star` 等要求 i ≥ 1（没有前序月线就谈不上「顶部」，i=0 时「史上最高点」恒成立）**。
- **周/月线序列缺帧 ⇒ 静默关闭整个附加项（2026-09-26 修）**：月线 K 线的 OHLC 只有在「这个月每根日线都在」时才成立，缺月（取数缺口/长停牌/合成数据）时的「月线形态」是伪形态。实测代价是**漏报顶部**，收益是不再误报「拉黑」——方向上一律选前者。反例原型 `tests/report/test_review_fixes.py::_reversal_bars`：2026-03 之后直接跳到 2026-08，只有 4 根日线的「8 月」被当成完整月，判出一根假超长射击之星，把整条 `plan_entry` 打成「拉黑｜等待」。回归 `python tests/top/test_month_top.py`。
- **成交口径（`t0_tail` / `fills_policy=limit_reclaim`）必须进人读输出（2026-09-26 补）**：引擎（rule123）早就产出这两条，但渲染层/摘要/探针一行都不打 ⇒ 读不到等于没落地。现四个出口齐了：`report_render.build_mode_rows`（模式卡新增「成交口径（沿线限价）」「T0 尾盘补救腿」两行）、`build_summary`（有才写一行「成交口径」）、`battle_analyze.summarize`、`probe_intraday` 首屏。回归 `python tests/report/test_battle_report.py`。
- **牛旗（上升旗形）与平台突破并列 T1，且排在 W底 之前（2026-09-26 老罗定，MRNA 案例）**：老罗原话「增加识别突破后的旗形整理案例-牛旗的图形 MRNA，**过牛旗即是买点**」「牛旗形态的突破，应该和平台突破**并列是 T1 买点**」。改 `_plan_entry_core` 内序为 **平台 ⟷ 牛旗 → W底 → 沿线**。理由两条：① **平台与牛旗只是整理段上沿「水平 vs 下倾」的两种几何**，都是「大阳/爆量 → 自然回踩整理 → 过整理上沿」的第二买点；② **形态尺度** —— MRNA 2026-09-17 的 W底（08-28~09-10，9 根）**嵌在旗面内部**（旗面 08-19~09-16，19 根），大结构压小结构；旧序取了 W底颈线 **156.42**，而「过牛旗」的真实触发位是旗面线 **145.28**（更低、更贴「不追高」）。改后当日 `mode=flag_tl_break`。⚠️ 改序会动到 `tests/entry/test_breakout_modes.py` 与 CRSP 那套顺序记录，改前先跑全量。
- **★ `flag_len` 必须是「旗面自身长度」，不是「旗杆末端到今天」（2026-09-26 修）**：`n-1-pe` 会随时间无限增长，撞上「旗面 ≤20 根」闸门后**在突破后第二天把整个形态判死** —— MRNA 旗面 19 根：09-17 过线当天 flag_len=20 通过 → 09-18 变 21 → `detect_bull_flag` 返回 None，**形态在最该被看见时消失**；更要命的是它与下游 `flag_tl_break` 要求的 `days_above_tl ∈ [1,3]` **自相矛盾**（要在线上连续 3 根，flag_len 至少 21 ⇒ 该窗口对 ≥18 根的旗面不可达）。新口径 = **旗杆末端 → 突破前最后一根**（`flag_end = n-1-days_above_line`），新鲜度只由 `days_above_tl` 管；同时给旗面线锚点加守卫「**必须落在旗面内**」（突破后的新高也会进 `Hs`，被选成 `pa` 就把线拖到突破价上 —— TEM 家族「形态在突破当天自我消失」）。改后 MRNA 旗面长度恒为 19。
- **「大阳后缩量回踩」与「牛旗突破」是同一件事的两个阶段（2026-09-26 合并口径）**：老罗原话「突破放量或爆量，自然回踩是非常正常的……**大阳之后缩量不代表次日一定能涨，但是如果涨了过了牛旗就是买点**」。⇒ **阶段一（未过整理上沿）= `impulse_pause`**，缩量到位只说明**整理成立**、属**预测性买点·试错仓**；**阶段二（过沿）= T1 接管**（上沿水平→`platform_break`、下倾→`flag_tl_break`）。⇒ **「缩量」不是买点，「过沿」才是**。落地：`impulse_pause` 的 `digest` 分支写 `z["confirm_edge"]` / `confirm_kind` / `confirm_tier`（`_confirm_edge()` 在「活平台沿（水平）」「牛旗面线（下倾）」里取现价上方最近的一条）并在 note 里点名确认线；两档不会同时给矛盾买区（T1 判定排在前，真过沿了走不到该分支）。
- **「过牛旗」要能提前挂，不是顶着突破那一刻追（2026-09-26 新增 `pre_breakout_flag_order`）**：老罗「很多时候我们并不能第一时间买到突破」。触发价 = **下一根**旗面线值 +0.05×ATR（线在下移，用**当日**线值会把单挂高），硬止损 = 线下 1.0×ATR（同斜线版口径；平台版是 0.5×ATR）。接入 `pack()` 出口的 `pre_breakout` 候选三选一（平台沿 / 泛化斜线 `down_tl` / **旗面线 `flag_tl`**），按 `dist_atr` 取最近、其余并列播报并**去重**（MRNA 09-15 旗面线 = down_tl = 148.98 只报一条；09-16 旗面线 146.39 **低于** down_tl 148.16 ⇒ 旗面线才是更优埋伏价）。`PRE_BREAKOUT_LABEL` 必须把 `None` 映射成「活平台沿」—— 平台版**不写 `anchor` 字段**，漏了会让提示退化成默认文案。
- **单票独立出口 `gates/bull_flag_check.py`（2026-09-26）**：`python gates/bull_flag_check.py <code> [--us] [--json] [--data x.json] [-v]`；退出码 **0=无牛旗 / 1=成型未过线（给埋伏单）或 过线已久（不追）/ 2=已过牛旗（买点成立）/ 3=取数失败 / 4=旗形无效（跌破旗杆大阳低点 或 整理 >20 根）**，多票取最大。取数走 `bars_source` 三级（与 battle_analyze / probe 同链路），**不另写取数**（否则同一票两套基准日）。⚠️ 它同时暴露了一个**既有隐患**：`attach_stops_targets` 只保证「两腿都低于现价」，**不保证 hard < struct**；MRNA 09-17 就出现 `硬止损 146.43 > 结构止损 145.28`（盘中腿先触发、收盘腿形同虚设）⇒ CLI 已显式打 ⚠️ 告警，但**引擎侧尚未加闸门**，遇到时按更紧的那条执行。回归 `python tests/entry/test_mrna_bull_flag.py`（**36 项**，fixture = 225 根真实日线）。
- **★「贴线待突破」`flag_knife_edge`：不能放宽守卫，要加量能条件（2026-09-26 天能重工 300569 案例）**：老罗「2026-09-23 收地量十字星，次日就有极大可能变盘，果然次日就画了 N 字，那么**次日过牛旗的点位就是买点**」。**缺陷**：`pre_breakout_flag_order` 的守卫 `if last_c >= line_next: return None` 注释写「已站上 → 交给 flag_tl_break」，但**线每日下移** ⇒「收盘贴线」必然踩中：300569 09-23 当日线 4.99 > 收 4.94（**真的未站上**）、**次日线 4.940 = 收 4.940** ⇒ 判成「已站上」，埋伏单**在最接近突破那天整档消失**（越贴线越容易中招 = 恰好在最该给价的那天失效）。**但实测不能放宽**（153 组真实日线、旗面成型未过线日去重 n=706）：贴线档（n=213）次日「过线率」71.8%、过线后 5 日中位 **−1.81%**、胜率 43.8%（正常档 n=493 = +1.30%/56.1%）——「线自己贴上来」**不需要价格涨**，是**廉价过线**。真正的分岔是**量能**：贴线∧≥1.5×近5日均量 n=19 **+2.76%/52.6%**、贴线∧缩量 n=134 **−1.83%/42.5%**；正常档同口径 放量 n=28 **+3.65%/67.9%** vs 缩量 n=95 +0.70%/52.6%。⇒ buy-stop 表达不了量能条件，**这一档不给挂单价**，改出「**次日开盘不低开（≥线值）且量 ≥1.5×近5日均量**」的盯盘预警（老罗 2026-09-26 定：加预警 + 放量确认，**不放宽守卫**）。落地：`flag_knife_edge()` 挂 `result["knife_edge"]`（与 `pre_breakout` 几何互斥：线仍在价上方走埋伏单、线已贴到价上走本档）、`apply_top_signal_gate` 顶部否决时一并作废、`evaluate` 透传并算「今日开盘是否 ≥线值」、CLI 状态 `knife_edge`（退出码仍 1）。回归 `python tests/entry/test_300569_bull_flag.py`（**27 项**，fixture = 150 根真实日线）。
- **★「地量小实体（十字星）」`dry_small_body` = 变盘临界，但**只作标注、不作闸门**（2026-09-26）**：老罗口径，阈值取**实体 ≤0.15×ATR**（老罗在「根幅 ≤0.20 / ATR ≤0.15 / 两者并集」里选的 ATR 口径 —— 少毫厘边界，且收得进 300569 的 0.12×ATR）。实证（1511 个「大阳后缩量」实例）：实体 ≤0.15×ATR 组次日 P(涨≥2%) **31.3%** vs 26.5%、过线后 5 日中位 **+1.00%** vs −0.18%（滞后 5 日 t=+2.4）；各档（0.10/0.20/0.35）方向**单调**、**与振幅无关**（再叠「振幅 ≤0.5×ATR」反而变差，n=74、次日中位 0.00%）⇒ **只留小实体一条，不要叠振幅**。⚠ 分档后样本小（贴线组内 n=40）⇒ 只进 note / 报告 / CLI，**不改 recommend**。
- **⚑ 不得用一套买法的期望去否定另一套买法的闸门（2026-09-26 老罗纠正）**：我曾用「`still_falling` vs `digest` 的次日/后5日 t 都 ≈ −0.3（零区分力）」建议把「近 3 根仍创新低 ⇒ wait」这道闸门降级。老罗否掉：「**不创新低当然更强，但是另一套买法了，不创新低是趋势的买法，这是突破旗形的买法**」。⇒ 每道闸门只在自己的买法内被检验；跨买法拿单一期望指标横比 = 用错分母。**该闸门保持原样**，本次不动。
- **月线拉黑无法区分「真顶」与「反弹顶」—— 已知边界，不因单案例改规则（2026-09-26）**：300569 被 `month_one_star` 判拉黑（2026-03-31 月线超长射击之星，高 8.95、上影 77%、body_r 0.13），解除条件是后续月线高点收回 8.95。老罗澄清「这个**并不是它的真顶部，真顶部在 2021-12 月**，现在只是做短线，我举例而已，**不是要买这个股**」⇒ **不因本案例加期限/失效条件**；`tests/entry/test_300569_bull_flag.py::test_monthly_blacklist_is_still_there_unchanged` 只钉住现状。
- **牛旗两条硬条件（老罗 2026-09-26：「旗形整理最好不要跌破大阳线的最低点，否则无效，然后整理天数不要太长（不超过 20 日）」）**：① **旗面收盘不得跌破旗杆「最后一根大阳」的最低点** —— 大阳用 `is_yang_bar` 定位（取**最后一根**：紧贴旗面那根启动阳；老罗在「最后一根 / 实体最大 / 旗杆末根 K 线」三选一里选的这个，三者实测过滤强度约 **47% / 16% / 69%**），最低点复用 `yang_floor`（普通大阳=当日最低价、真一字/跳空=缺口下沿）。**口径 = 收盘破才算破**（老罗在「盘中破 / 收盘破」里选的后者：实测「盘中破收盘守」那组 n=284 过线后 5 日中位 +0.19%/胜率 50.7%，与全程守住组 +0.28%/51.0% 无差别 ⇒ 砍它是白砍；也与仓库既有「结构止损=收盘破」一致）；盘中插破只记 `undercut` 供报告提示。② **整理天数（旗面自身根数）≤ 20**（`FLAG_LEN_MAX`，此前硬编码 20，现常量化）。**实现**：`detect_bull_flag(..., diagnostic=True)` 不再返回 `None`，而是回 `{valid:False, invalid_reason, flag_len, flag_low, flag_min_close, anchor, ...}`（`invalid_reason ∈ {no_pole, no_flag_line, flag_too_short, flag_too_long, retrace_too_deep, flag_break_pole_low, flag_not_below_pole_hi, flag_back_to_pole_mid, no_tl}`），CLI 靠它说清「为什么不算牛旗」；**默认调用方（`diagnostic=False`）行为不变、依旧只拿到 None**，一个「无效件」都不会漏出去。实证（153 组真实日线 / 2478 个牛旗实例 / 过线后 5 日）：被这条砍掉的那组中位 −0.01%、保留组 +0.28% —— **方向对但幅度极小、不显著**，所以它是**形态定义（老罗要的），不是统计上可上线的筛选器**，不得据此说法「必然更差」。**已知边界**：旗杆里一根大阳都没有的慢牛堆叠（连续小阳推升）会退回整段旗杆低点，此时该条件几乎不触发（样本里 2478 例中仅 11 例走到这个兜底）。
