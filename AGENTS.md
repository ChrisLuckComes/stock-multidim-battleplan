# AGENTS.md · stock-multidim-battleplan

本仓库的跨模型行为合同。换模型也按这里执行，不按模型默认习惯发挥。

股票规则只维护本文件、`SKILL.md`、`references/`。不要读、不要改上级目录 `D:\code\AGENTS.md` 里的股票条款。

判据以本文件的表格和脚本输出为准。案例、t 统计、阈值推导在 `references/`，不拿故事改判据。

---

## 0. 任何模型都一样

1. 中文。第一句就是能执行的判断：买、不买、或不用再看，并说清是哪一类。禁止两头堵。低 PE、高增长、模式还在，都不能把这句话说软。依据放在判断后面。不奉承，不铺垫。
2. 用户前提错了，先指出错在哪。
3. 路径、命令、测试结果、字段名、价格不得编造。没跑过就写「未跑」。
4. 两种合理解释而且会改产出时，先问，不要选一个做完。
5. 只改用户这次要的范围。能跑的代码不重构、不顺手优化。非本次请求的历史代码，先问再改。
6. 口述、复盘、事后感想：不改文件。只有明确要求改代码、改规则、入池或出池时才动文件。
7. 用户纠正一条规则：已有条目就收紧那一行；没有就追加一行具体指令。不写案例故事。
8. 提交：仅当本次任务是审查或修复，且相关测试已通过，才 `commit` 并 `push`，不要再问。其他任务不提交、不推送，除非用户当次明说。
9. 提交前看 diff：业务代码不得留临时 mock 或调试输出。测试里的正式 fixture 可留。提交说明用中文。
10. 临时文件和一次性脚本用完即删，不入库。行情输出、缓存、`out_*.json`、`notes_*.json` 已在 `.gitignore`。还要落在磁盘上的草稿只放 `scratch/`（已忽略）。可复跑的实证留在 `research/`，不要和一次性脚本混放。

## 1. 先判断任务，再动手

| 用户在做 | 动作 | 不要做 |
|---|---|---|
| 说「跑一下」 | 按第 3 节出计划，交付 `report_render.py` 的 HTML。当次明确指定别的产出时才改 | 只交 JSON、只报耗时、手写 HTML |
| 问一只票该不该买、怎么买、止损止盈 | 按第 3 节出计划。细则读 `SKILL.md`，再按它的表打开 `references/`，不要一次读完 | 手写 HTML；把 `rule123.py --out` 当报告 |
| 点名个股但没说「跑完整计划」 | 只做龙头轻量对照（1–3 只：阶段涨幅、趋势、优先做谁） | 默认同行也跑 rule123、分钟线、扫雷、双轨打分 |
| 改某个模块的 bug | 只改该模块，并跑 `tests/` 里对应子目录 | 通读全部 references；顺手改相邻文件 |
| 改了引擎字段 | 同时改 `scan_all/scanner.py` 导出和 `scan_all/report.py` 渲染 | 只改 CLI，以为扫描报表会自动跟上 |
| 复盘、感想、没说改 | 回答即可 | 改代码、改规则、改池 |

- 回测与实证在 `research/`，备用取数在 `fetch/`，闸门脚本在 `gates/`，盯盘在 `watch/`，资料库同步在 `library/`。根目录只留引擎和出报告：`rule123.py`、`probe_intraday.py`、`battle_analyze.py`、`report_render.py`、`bars_source.py`、`fetch_market.py`。
- 测试：`python -m pytest tests -q`。没有 pytest 时，直接 `python tests/<目录>/test_<名>.py`。

## 2. 栈与配置

- Python 3.8+。A 股东方财富，美股 Yahoo。无付费数据源。
- 探测：`python probe_intraday.py <code>`。日线快照落盘后，probe 必须 `--data` 复用，禁止再调 `bars_from_us` 取日线。
- 钱（账户、主力/备用、单笔硬顶、风险预算%）只在 `env` / `.env`，经 `account_config.py` 读。代码里不许写 `account=50000` 这类字面量。
- 策略常数（ATR 倍数、通道阈值、均线闸门）在代码里，不进 `.env`。
- 美股仓位只受 `US_ACCOUNT` 全额约束（默认 $5000）。没有额外的单笔比例上限。
- A 股总上限 = 主力 + 后备。`ASH_ACCOUNT` 只是风险预算的分母，不等于可动用资金。报告同时给「占总上限 %」和「占主力层 %」。

## 3. 出计划（顺序固定）

1. 确认市场、代码、时点、是否已收盘。未收盘不得下全天派发结论。
2. 主标的日线只取一次。快照缺 `as_of` 时用文件 mtime：末根同一自然日且早于收盘 = 盘中半日 bar，不得当收盘日线。
3. 先过硬否决和扫雷，再跑股性与买法，最后算赔率、仓位、止损、目标。
4. 用户说「跑一下」：交付物是 HTML（`battle_analyze.py` → `report_render.py`）。首屏第一句用第 0 节的判断口径，不写两头堵。当次明确说只要时间、只要 JSON、或只要对照时，才改产出。`analysis.json` 是中间产物。模型只写 `notes.json`。6 位数字是 A 股，其余是美股 ticker。
5. 主标的研究检索和分钟 probe 并行。默认不搜同行财报。
6. 点名个股必须做 1–3 只同板块龙头的轻量对照。龙头 = 近期涨得最好、趋势最干净，不是市值最大。

### 硬否决（命中即不做）

只有这些能否决一笔交易：

- 钱不够，或涨停买不到
- 结构已坏（含跌破最近大阳最低价这条证伪线；线下方的均线不是更深买点，出池，禁止把止损往下挪）
- `python gates/top_signal_check.py <code>` 退出码 2（已确认顶部）。退出码 1 只预警缩仓，0 不拦。反包（`invalidated`）回落成 0，不得报成预警
- `python gates/pre_runup.py <code> --date <公告日>`：公告前 5 日 ≥10% 或 10 日 ≥15%（强抢跑，公告日禁止追入，含 13:00）
- 扫雷命中且窗口临近：大额解禁、减持、立案、利好公告黑历史（上次同类公告高开低走或放量收阴）、其他重大利空。午间消息先扫黑历史和「公告前已带量下跌」，任一命中当日不做

### 软约束（只换做法或缩仓，禁止写成「环境否决」）

- 情绪分 `python gates/market_sentiment.py`：只缩放仓位。&lt;45 写「因环境降仓」，不写「不许做」。
- 宏观（利率、加息）：只作背景。禁止用宏观对整条产业线暂停新建仓。偏紧时等回踩并缩小仓位。
- 选股三因子（总市值、股东户数、距 52 周高点）：参考，不是门禁。趋势为准。见 `references/stock-selection.md`。
- 估值、区间涨幅、利润同比：参考，不是门禁。材料涨价、技术突破、产业趋势这类叙事，禁止单独写成不买。买不买先看盈亏比，再看硬否决。R≥1.5 且硬否决未命中，轻仓可以博。估值、涨幅、叙事、薄止损只缩仓。
- `stock_character.py` 只决定买法，不决定买不买。样本 &lt;3 不得否决。公告抓取不早停。
- `confluence.py` 七层只排序、不否决、不缩仓。`unknown` 不进分母但必须显示。顶部否决放 `veto`，不与七层相加。情绪分日期必须是当日文件，隔日不得冒充。

### 买法

- 只做六种 `mode`，禁止自拟第七种。T0（均线收复 + 过昨高）优先于 T1。T1/T2 不是目标1/目标2。
- 赔率优先于形态，也优先于估值。R≥1.5 且硬否决未命中：轻仓可以博，禁止改写成不买。R&lt;1.0 才禁止追入。买点前移只走 `pre_breakout`（与 mode 并存）和 `line_pullback`。盘中通道 A/B/C 只可小仓试错。禁止用事后突破倒推「昨天就该买」。
- 出计划前跑 `python gates/stock_character.py <code>`，服从它顶部两句结论。赔率 ≥1.5 且延续率 ≥50% 才可突破买；赔率 &lt;1.0 或延续率 &lt;35% 则突破当日禁止追入，只放回踩。
- 5 日 ≥6% 或 10 日 ≥10% 的抢跑：只做开后 ≥5 分钟的第二结构，仓位减半。消息判据是否决理由，不是买入理由。禁止消息开盘瞬间抢单。写不出硬止损 = 这笔不做。当日卖出后，消息不是回补理由。
- T0 前先看 `ma_ride_state()`：`line_ride` / `new_high`（无锚，不做回踩）/ `fresh_reclaim` / `mixed`。回踩买不是独立型号：先有 `line_ride`（沿选中的那条线上涨），才有这条线的回踩买。没有 `line_ride`，不得把「回踩 MA5」写成买点。当日已有突破且 `recommend=True` 时不改道。是否改道只看 `ride_redirected`，不看 `mode == line_pullback`。`line*` 是选中的那条线，`ma5*` 恒为 MA5，不得混用。
- 沿线回踩锚为 MA5/EMA10/SMA20 时，须 `regime=continuation` 或收盘 &gt; SMA20，否则 `recommend=False`。
- `recommend=False` 不给可执行下单指令。离买位 &gt; 2×ATR 不追。低开本身不是买点。
- 低吸要同时满足：阶梯宽度 (MA5−EMA10)/ATR ≥ 0.25，且触发概率 ≥ 40%。然后和突破档比 R，取得分高的那条。挂均线等回踩必须当日收盘收回买区上沿，价格碰到不算买。
- 回踩档 `hard_dist_atr < 0.10`，或买价 ≤ 结构止损：开仓即止损，不作主推，改走 T0 或 `pre_breakout`。
- 引擎 `targets.t1` ≤ 触发价时，负的 R 是口径错误，不得据此否定计划。改用触发价之上的真实阻力重算，并写明「引擎 t1 已失效」。
- 禁止把「基准日实体中点」(开+收)/2 当买档。那是断头铡刀出场参考。合法的大阳中点是 (高+低)/2。
- 双轨打分：投资价值与短线博弈各 0–10，写理由。禁止合成总分，禁止用涨停次数抬投资价值分。
- 美股 `session=Open` 才把今日未收盘 bar 合成进结构。盘前、盘后、`Closed` 不合成。
- 顶部形态、跳空、断头铡刀的阈值不要改。跳空和断头铡刀不接否决。细则 `references/top-signals.md`。单票结论只读 `top_verdict()`，不要各自解释 `plan["top_signal"]`。

### 止损与竞价

- 硬止损 = 锚 − 0.10×ATR。已经收盘站上突破位的突破单：结构止损仍是突破位（收盘跌破才算失败）。硬止损不拿突破位本身，正常回踩会被扫掉。候选为大阳下沿、下沿距突破位超过 1×ATR 时的大阳中点、MA5、MA10、上升趋势线，只留买区下沿下方的，取最窄的一条。一条都没有时才退回阳线下沿。还没突破的，锚仍是 MA5、阳线下沿、大阳中点 (高+低)/2。结构止损和硬止损分两档，禁止合成一个价。
- 破了禁止换锚改宽。噪声带在建仓当天检查：突破类距买区下沿 &lt; 0.25×ATR 才叫贴噪声；回踩类的硬止损本身就是 0.10×ATR 滤网。
- 止损不许按成本 ± X% 写。
- 盘中破位且 3 分钟不收回才走。
- 集合竞价买单只有上限。09:25 前禁止预挂触发价上限（低开会买成最差一档）。上方突破买 09:30 后手动打。下方买点才可以用竞价。
- 集合竞价卖单只有下限。无条件出局：09:15–09:20 挂跌停价占位，保证按开盘价成交。高开不卖：09:19 按结构锚（如 MA5）决定撤不撤，09:20 后深市不可撤。门槛必须是具体价格，不许写「不上攻就走」。
- 开盘流程固定：09:15 挂占位单 → 09:19 按门槛撤或不撤 → 09:25 出开盘价。留仓不得放宽止损。

## 4. 改代码时的口径

- 新字段从引擎出来后，扫描导出和报表渲染一起改。
- 顶部闸门包在 `plan_entry` 出口、T0 之后。`live_last=True` 时确认只看 `bars[:-1]`。
- 筛选排除走 `top_signals.screen_exclude`，顶分型走 `top_signals.top_fractal`。不要在各脚本里再写一套。
- 报告与池表读 `ride_redirected` 判断有没有改道。
- 改形态阈值或位置口径前，先跑 `tests/top/`。那些 fixture 就是为了拦住静默改口径。
- 回归目录：`tests/entry` 买法与 T0，`tests/top` 顶部，`tests/screen` 情绪/股性/抢跑/叠加，`tests/levels` 多空位与做空，`tests/data` 取数，`tests/report` 报告，`tests/library` 持仓同步。
