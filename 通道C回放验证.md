# 通道 C（分钟级回踩确认）落地与回放验证

日期：2026-09-16 ｜ 状态：代码已实现、回归全绿、**未 commit**（按约定等人工提交）

## 一、它解决什么问题

老罗的原话：*「这里面的策略基本上都是要回调买入，我重点是要知道盘中买入怎么处理，
否则我永远无法临盘抓住大涨，都是被动。」*

前四条通道都能给「可执行价格」，但都有一个共同的时点特征：**它们都在等回落**。
而强势股不给日线级回调，只给**分钟级**。

通道 C 的答案不是「放宽追高」，是**换一个更快的时钟** —— 已启动之后，等 5 分钟结构
回踩启动点，不破 + 未爆量 + 收阳抬低即入场。

## 二、实现位置

| 文件 | 内容 |
|---|---|
| `probe_intraday.py` | `pullback_confirm()` 主逻辑；A 股「二·C」、美股「四·C」两个调用点 |
| `test_review_fixes.py` | `test_pullback_confirm_states`（六状态 + 2 条 bug 回归）、`test_pullback_requires_launch` |
| `watch_us.py` | `signals_of` / `line_of` / `card_of` 接入，盘中出信号会响铃 |
| `SKILL.md` | 「通道 C」章节重写（含两个坑的说明与实测表） |

常量：`PULLBACK_BAND_ATR=0.30`、`PULLBACK_VOL_MULT=2.0`、`PULLBACK_STOP_ATR=0.10`、
`PULLBACK_FRESH=2`。

## 三、回放逼出来的 4 个真 bug

| # | Bug | 后果 | 修法 |
|---|---|---|---|
| 1 | 前置条件写成「通道 B **当前新鲜**」 | C 只在能买 B 的时候才出现，等于没用 —— 而 C 专为「错过了第一根」而设 | 改成「A/B **曾放行**」 |
| 2 | 把「收盘落在回踩带内」当作回踩 | ILMN 9/15 21:45 是**上涨中**的根（收 213.51、量 34,043），被误判为「回踩段放量 = 出货」 | 回踩根必须**相对前一根下跌** |
| 3 | 缩量基数只用「启动根量」 | 通道 B 的启动根可能是低量根（ILMN 9/15 为 19,819 < 当日均量 23,396），拿它当分母，任何正常回踩都「超标」 | 基数改 `max(启动根量, 当日此前均量)`，阈值 2.0 |
| 4 | `pb_ctx` 在 `else` 分支内初始化 | kb 为空时 `UnboundLocalError` —— 实测 601233 直接崩 | 移到分支外初始化 |

## 四、回放实测

### ILMN 9/15 —— 通道 C 的价值（同一天、同一个启动）

命令：`python probe_intraday.py ILMN --us-account 5000 --date 2026-09-15 --until 21:50`

| | 入场 | 止损 | 风险/股 | 当日收盘 222.29 |
|---|---|---|---|---|
| 通道 B（21:40 触发） | 213.99 | 209.65 | 4.34 | +8.30 = **1.91R** |
| 通道 C（21:50 企稳） | **213.60** | **212.17** | **1.43** | +8.69 = **6.08R** |

**风险降低 67%，R 倍数高 3 倍。** 入场还便宜 0.39。

### ILMN 9/16 —— 通道 C 否定追高

| 通道 | 结果 |
|---|---|
| 关键位突破 | 21:40 收 234.94 触发（上限 242.83） |
| **回踩确认** | **21:50 收 231.93 跌破带下沿 232.19 → failed，当日作废** |

10 分钟内就告诉你「这次突破没站住」—— 佐证昨日 213.60 那笔才是对的入场。

### A 股样本

| 代码 | 通道 C 输出 | 是否合理 |
|---|---|---|
| 002961 | `pending`（一路不回踩，直接封板 20.97） | ✓ 正确不给信号 |
| 603228 | `failed`（13:25 收 105.46 破带下沿 105.50） | ✓ 正确拦截 |
| 688002 / 601233 | 未突破 K / 无 K 可突破 | ✓ 通道未启用 |

## 五、诚实交代

- **A 股的 ok 分支没有在真实数据上跑到过**，目前只被合成用例覆盖。
- 四个常量**只有 1–2 个样本支撑**。尤其 `PULLBACK_VOL_MULT=2.0` 是按「与通道 A 的
  2.5 倍爆量门槛同量级」推的，不是回测出来的 —— 属待验证参数。
- 通道 C 的入场价**未必低于通道 B**（回踩极浅时可能更贵）。比较必须看**风险/股**，
  不是看价格。
- 「一路不回头就不给信号」是设计而非缺陷：3124 个大阳样本里永久踏空率 20.2%，
  这是策略的固有成本。

## 六、用法

```bash
# 盘中/盘后：看第四·C（美股）或 二·C（A 股）
python probe_intraday.py ILMN --us-account 5000
python probe_intraday.py 002961 --qty 1500 --asof 2026-09-16 --replay

# 回放某个时点（美股 --until 用北京时间）
python probe_intraday.py ILMN --us-account 5000 --date 2026-09-15 --until 21:50

# 让它替你盯（出信号响铃）
python watch_us.py --watch
```

回归：`python test_review_fixes.py` → 127 断言全绿。
