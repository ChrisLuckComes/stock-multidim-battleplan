---
name: stock-multidim-battleplan
description: 美股/A股个股的多维度分析 + 作战计划。当用户要求多维度分析、买卖计划/作战计划、该不该买/怎么买/止损止盈时使用。入场模式：T0 均线收复+过昨高（最高优先，盈亏比最高）、预备突破单埋伏（优先T1）、平台突破、W底颈线突破、旗形下降趋势线突破、沿线回踩（以上优先T1）；大阳后缩量回踩、下降趋势线突破（次优先T2）。叠加赔率优先与买点前移（环境适配：流畅趋势仍做标准突破；高波动震荡市优先关键支撑低吸 / 紧凑盘整第一次启动）。叠加六维研究、扫雷、同板块/概念龙头对照（优先做龙头）、强制双轨打分、尾盘确认、不追高、止损 ATR+结构。强制产出入场/止损/目标1-目标2。
disable-model-invocation: true
---

# 个股多维度分析 + 作战计划（stock-multidim-battleplan）

> **何时用**：用户要求对某只股票做「多维度分析」「作战计划」「该不该买 / 怎么买 / 止损止盈」。
>
> **取数**：WorkBuddy 有 `wb-finance-skill` 时优先用它；否则用本仓库 `fetch_market.py`（A 股东财、美股 Yahoo）。结构判定只用 `rule123.py`。不用 Gamma / GEX / 期权墙。

研究回答「市场为什么买」；计划给出「在什么价买、止损在哪、目标1/目标2卖多少」。禁止「逢低布局、注意风险」。

**命名**：模式优先级 **T1 / T2** ≠ 止盈档 **目标1 / 目标2**。研报里必须写全称，禁止只写「T1」让人猜。

**做票优先做龙头**：用户点名个股时，必须同时找出同板块/同概念的领头羊并跑本 skill；开仓优先龙头，不优先跟风杂毛。

---

## 同板块 / 概念龙头对照（强制）

用户给出一只个股并要求跑本 skill 时，**不得只分析这一只**。必须完成：

1. **定主线**：该股当前归属的 1–2 个主板块或主概念（如核级锆、固态电池、消费电子、AI 算力；美股则同业 peer）。写清依据（业务/近期驱动，不是随便贴标签）。
2. **找领头羊**：在该板块/概念里找出 **1–3 只龙头**（领头羊可以不止一只）。  
   **龙头定义（强制）**：不是市值最大，而是近期**涨得最好、主动性最强、趋势最好**的标的。综合看：阶段涨幅与相对强度、是否领涨/带节奏、量价主动性（先于板块动、回撤浅、突破干净）、日线趋势结构是否更干净。市值只能当备注，不能当入选标准。
3. **对龙头执行本 skill**：至少对每只入选龙头跑 `fetch_market` + `rule123`，并给模式卡、扫雷、双轨打分、简版计划（可略短于主标的，但不得省略 mode / 止损要点 / 双轨分）。主标的与龙头对比写进同一份对话结论；龙头可另落 HTML，或在主报告里设「龙头对照」专节。
4. **开仓优先级**：  
   - 主标的是跟风/杂毛、龙头更强且也有可执行 mode → **优先做龙头**，主标的降级为观察或更小试错。  
   - 主标的本身就是龙头之一 → 正常执行，仍列出其他龙头作对照。  
   - 龙头 `wait`、主标的「看起来便宜可买」→ **不因为便宜改做杂毛**；等龙头出现买点或承认本板块不做。  
5. **输出必须有**：`主线概念` · `龙头名单（1–3）+ 为何是龙头（各一句）` · `主标的 vs 龙头：优先做谁`。

**禁止**：用市值/名气冒充龙头；只写「板块有 XX」却不跑 rule123；发现主标的不是龙头仍按同等仓位推荐买入。

---

## 只做六种买法（强制，由 `rule123.py` 的 `mode` 给出）

研报禁止自拟第七种买法，禁止同时把两种模式写成「现在就买」。读 `evaluate()` 的 `mode` / `priority` / `buy_zone`。

判定顺序：**先看 T0（均线收复+过昨高，出即接管），再看优先 T1，再看次优先 T2，都不成立则 wait。** T1 内部顺序：平台突破 → W底颈线突破 → 旗形下降趋势线突破 → 沿线回踩。**唯一例外（「强上加强」，老罗 2026-09-17 定）**：平台破位但量能不足时，若同时明显沿线主升（贴轨 ≥4 次、收盘在线上 ≤1.0×ATR），两结构互证 → 不拦截，直接按沿线回踩放行，verdict 标「强上加强(平台+沿线主升)」。两种 T2 同时可做时，**大阳后缩量回踩优先于下降趋势线突破**。T1 可做时不执行 T2。已识别旗形时，不用泛化的 `downtrend_tl_break`（走 `flag_tl_break`）。

**量能不再作突破闸门（2026-09-17 用户定：「RVOL 不重要，价格说明一切」）**：
- **下降趋势线家族**（`flag_tl_break` / `downtrend_tl_break`）**完全按价格确认**：收盘站上线 + 收盘落在当日振幅上半区 + 收盘不低于前收（见脚本 `price_conf`）。RVOL 只写进 note（放量=加分项 / 无量=「非放量突破，仓位打折」），**不再一票否决**。理由：这类形态本身就是「缩量收敛 → 突破」，而 20 日均量又被此前的恐慌/暴涨量能污染（ILMN 9/15 RVOL 用均值算 1.70、TEM 9/15 只有 1.03），用 RVOL 卡会系统性漏掉最好的一段。
- **平台 / W底**：同口径改为**价格确认**（脚本 `price_conf` = 收盘落在当日振幅上半区 **且** 不低于前收），排除长上影插针式站上。`RVOL>1.5` 与「宽幅强阳」（实体 ≥1.2×ATR）都**只作加分标注，不再一票否决**：放量/强阳分支给正常仓，价格确认但无量分支给「半仓/试错仓」（verdict 带「·非放量」）。收盘落在下半区或收低于前收 = 插针式站上 → wait（该分支仍保留「强上加强」豁免）。


| 模式 | 优先级 | `mode` | 买什么 | 止损 |
|---|---|---|---|---|
| 平台突破 | 优先 T1 | `platform_break` | 收盘站上平台沿 / 前高 R1；**买区 = 突破位 ~ 突破位+1.0×ATR（单边向上）**；离买位 >2×ATR 不追；**价格确认，量能只作提示**（非放量 → 半仓/试错仓） | 结构止损=收盘破**活平台沿**；硬止损=阳线下沿或大阳中点 −0.10×ATR（距买区下沿 <0.25×ATR 时让位给更宽的合法锚） |
| W底颈线突破 | 优先 T1 | `w_bottom_break` | 收盘站上双底颈线；买区 = 颈线 ~ 颈线+1.0×ATR；离买位 >2×ATR 不追；**价格确认，量能只作提示** | 颈线下 |
| 旗形下降趋势线突破 | 优先 T1 | `flag_tl_break` | 主升旗杆后，收盘站上旗面下降趋势线；买区 = 该线 ~ 该线+1.0×ATR；**价格确认，量能只作提示** | 该旗面趋势线下 |
| 沿线回踩 | 优先 T1 | `line_pullback` | 回踩那条肉眼可见的上升线（默认摆动低点趋势线）；**买区 = 该线 −0.05×ATR ~ 该线+1.0×ATR（下沿只留毛刺）** | 结构止损=收盘破**那条回踩线本身**（MA5 只在该线已不可用时备选，且须低于现价）；硬止损=MA5 −0.10×ATR，MA5 不可用时退阳线下沿 |
| 大阳后缩量回踩 | 次优先 T2 | `impulse_pause` | 缩到近期最低量 + 近3根不创新低（一字板则回踩缺口） | 普通=大阳低点；一字=缺口下沿（前收） |
| 下降趋势线突破 | 次优先 T2 | `downtrend_tl_break` | 收盘站上下降趋势线（无旗杆的泛化反转）；买区 = 该线 ~ 该线+1.0×ATR | 该趋势线下 |
| **★ 均线收复+过昨高** | **T0（最高）** | `ma_reclaim_break` | 信号日收盘站上 **MA5/MA10/MA20 全部三条** + 左侧平台度 ≥50%，上方 25 根内有阻力位（距收盘 ≤ 板块上限）；**次日过昨高即买**（触发价 = 昨日**最高价**，在现价上方） | {MA5, MA10, MA20, D0 实体中点, D0.low} 中 **< 触发价且最近**的一条 |
| **预备突破单（埋伏）** | **优先 T1** | `pre_breakout`（`mode` 之外的**独立入口**，非第七种形态） | **在突破位「下方」提前挂 buy-stop 埋伏**，触发即突破确认；与当日买点并存、先到先做 | 平台版 = max(沿−0.5×ATR, MA5−0.10×ATR)；斜线版 = 线下 1.0×ATR |
| 等待 | — | `wait` | 不买 | — |

**★★ T0：均线收复 + 过昨高（2026-09-20 用户定稿，优先级高于 T1 买突破）**

用户自研战法，定级理由原话：「**这一套战法的优先级是 T0，比 T1 买突破还要更高，因为盈亏比最高**」。
与 T1 的本质区别：T1 在**位已破之后**追；T0 在**位未破之前**、均线刚收复、贴着墙蓄势时进场
—— 同一道墙，买点低一截、止损窄一截 ⇒ 盈亏比更高。是「预判买优于顶着买」的同一逻辑，但锚更明确。

```
D0（信号日）：
  ① 收盘站上 MA5、MA10、MA20 全部三条（**只看站上/未站上，与排列无关**）
     ★ 用户定：「不要做均线下方的股票」—— 均线下方 = 还有套牢盘压着，压力更大。
     实测（23 只 A 股全历史）：≥2 条 → 胜率 32.4%/硬止损 12.7%；全部三条 → 37.2%/9.9%。
  ② ★ 左侧必须有锚（**平台成形**）—— 平台度 ≥ 50%
     平台度 = 窗口内收盘落在「中位价 ±10%」的根数占比。
     ★ 用户定（联瑞新材 688300 案例）：「**根本就没有锚啊，平台都没有形成**，
       最高都跑到 297.31 了，这种肯定不能做了」
     实测八案例：赛分 100% / 东富龙 100% / **浩瀚深度 100%** / 科泰 80% / 纳微 76%
                 / 成都先导 72% / 康龙 60%                                    ← 通过
                 **联瑞 44%**（6 月 297.31 崩塌 −66.8% 后的报复性反弹）      ← 拦下
     阈值敏感性（科泰实测）：±5% → 60% ／ ±8% → 72% ／ ±10% → 80%（都是 PASS，不敏感）
     ⇒ 本质：T0 需要「**左侧平台 → 前方前高 → 次日过昨高**」三段结构。
       左侧是崩塌/无盘整的，头上没有可参照的墙，不适用本规则。
  ③ 阻力位 = 双锚取**更高者**（同一日线平台的两个测点 → 平台沿是高那个）
     锚1 = 近 25 根最高价（含长上影）；锚2 = 同窗口枢轴高（左右各 2 根都更低）的最高者
     ★ 用户定：「这两个高点应该取更高者，因为都是属于同一个日线平台」
     ★ 窗口 25 根：成都先导的锚 41.78 距信号日 24 根，20 根窗口刚好切掉它。
     要求 ≥ D0.close，且距收盘 ≤ 板块上限：主板(60/00) 10% ／ 创业板(30)·科创板(68) 20%
     ★ 双锚已被三次独立验证：成都先导 41.78 ／ 科泰电源 27.12(09-09) ／ HOOD 125.25(09-08)
D1（次日）：
  ★★ 「昨高」= D0 的**最高价 D0.high**（含上影线插针），**不是 D0 收盘价**。
     2026-09-20 用户提问「昨高指的是收盘价还是最高价」→ 定稿 **最高价**。
     判据本身也一贯：P1 分支是 `D1.high > D0.high`，全程比 high 不比 close。
     ── 为什么必须是最高价 ──
     突破的定义是「越过昨天全部成交的价位」。D0 的上影线里挂着真实抛压，
     用收盘价当触发线 = 把 2/3 的墙拆掉、提前进场。
     实测七案例（误用昨收会让触发价全线大幅下移）：
       浩瀚 18.50 → 18.43(−0.38%) ｜ 科泰 25.58 → 25.44(−0.55%)
       纳微 41.87 → 41.40(−1.12%) ｜ 康龙 40.43 → 39.92(−1.26%)
       成都 37.19 → 36.62(−1.53%) ｜ 赛分 29.02 → 28.30(**−2.48%**)
       东富龙 14.79 → 14.36(**−2.91%**)
  P1  D1.high > D0.high → entry = max(D1.open, D0.high)   ← 任意时点过昨高都买
        · D1 开盘已在 D0.high 之上（跳空）→ 直接用**开盘价**，无需等触发
          （科泰电源 300153：09-17 开 26.01 > D0.high 25.58 → 开盘即入场）
        · 否则挂 D0.high 等触发（需盯盘，券商无 buy-stop）
  P3  D1.high ≤ D0.high → entry = D1.close（尾盘 14:57 定夺，破位不买）
        ⚠️ P3 的入场价是 **D1 收盘**，那是「没追到时的补救价」，
           不是把「昨高」定义成收盘价 —— 两个 close 不是一回事，别混。
止损锚 ★ = { MA5, MA10, MA20, D0 实体中点 (o+c)/2, D0.low } 中
         **低于触发价且离触发价最近** 的那条（用户定：「取全部锚中离触发价最近」）
```

**★★ 八案例共性提炼 —— 这才是规则的真正内核（2026-09-20）**

八案例的「实体/ATR」从 **0.22x 到 2.43x**、「D0 涨幅」从 **+0.66% 到 +14.05%**，跨度极大：

| 案例 | 信号日 | D0 涨幅 | 实体/ATR | 振幅 | 收盘位 | 平台度 | 距墙 | 风险 |
|---|---|---|---|---|---|---|---|---|
| 浩瀚深度 688292 | 09-16 | **+0.66%** | **0.51x** | 6.34% | **94%** | **100%** | 5.91% | **1.41%** |
| 科泰电源 300153 | 09-16 | +3.84% | 0.72x | 5.55% | **90%** | 80% | 6.60% | 2.31% |
| 成都先导 688222 | 09-16 | +14.05% | **2.43x** | 16.94% | **90%** | 72% | 14.09% | 7.50% |
| 东富龙 300171 | 09-17 | +4.13% | 1.17x | 8.05% | 61% | **100%** | 10.58% | 4.12% |
| 纳微科技 688690 | 09-08 | — | 1.81x | 14.51% | **91%** | 76% | 13.04% | 3.20% |
| 赛分科技 688758 | 09-15 | — | **0.22x** | 4.27% | 41% | **100%** | 12.97% | **1.79%** |
| 康龙化成 300759 | 08-04 | — | 0.85x | 7.93% | 82% | 60% | 5.21% | 4.16% |

**⇒ ★ 结论：「大阳线」不是判据。** 浩瀚深度 D0 只有 **+0.66%（几乎平盘）**、赛分实体仅 **0.22×ATR**，
两个都成立。真正八案例一致的只有三条：

1. **收盘站上全部均线**（八案例全中）
2. **平台度 ≥ 50%**（实测 60%~100%；反面案例联瑞仅 44%）
3. **★ 收盘位置在当日振幅高位** —— 八案例除赛分 41% 外**全部 ≥61%**
   （浩瀚 94% / 纳微 91% / 科泰 90% / 成都 90% / 康龙 82% / 东富龙 61%）

**内核 = 「贴着平台沿、收在当日最高附近」，而不是「涨得多」。**
涨得多（成都 +14%）与涨得少（浩瀚 +0.66%）都能成立 —— 因为两者都是
**收盘贴在平台沿上、没有上影抛压**。判断时问的不是「今天涨多少」，
而是「**今天收盘有没有收在最高附近**」。

**⚠️ 别被「大阳」这个词误导**：用户口语常说「大阳线突破」，但八个实测案例证明
真正的必要条件是「**收在当日高位 + 站在平台沿**」。搜索时不要加实体门槛，
否则会漏掉浩瀚深度（+0.66%）这类最优质的样本（2 天 17.38R）。

---

**★★ 重复信号：「一次成功不了得来几次」—— 联想集团 00992.HK 双案例（2026-09-20）**

**这是目前最重要的样本**：同一条规则、同一只票、相隔 15 个交易日给出**两次**信号，
第一次被扫、第二次吃到整段主升浪。

| | 第一次 07-09 | 第二次 07-31 |
|---|---|---|
| D0 收盘 | 24.040（+7.71%） | 23.860（+9.75%） |
| mode | `downtrend_tl_break` | `ma_reclaim_break` |
| 平台度 | 84% | **96%** |
| 触发 / 止损 | 24.30 / 23.27（实体中点） | 24.32 / 23.68（实体中点） |
| 计划风险（距 D0 收盘） | 4.24% | 2.63% |
| **D1 跳空** | **+8.82%**（开 26.160） | **+0.08%**（开 23.880） |
| **实际入场** | **26.160**（跳空后用开盘价） | **24.320**（挂触发价成交） |
| **止损距入场 = 1R** | **11.05%**（4.20×） | **2.63%** |
| D1 日内最大回撤 | −6.57% | −1.34% |
| **结果** | **−1.00R**（07-13 被扫） | **+12.22R**（峰值 +18.50R，含 08-13 +20.18%） |

**★ 机制：跳空把入场价推高，止损线却不动。**
两次的触发价（24.30/24.32）与止损锚（都是实体中点）几乎一样，差别**全在 D1 怎么开盘**：
- 07-09 跳空 +8.82% → 入场被抬到 26.160 → 1R 从计划的 3.20% **被撑大到 11.05%（4.20 倍）**
  → 同一条止损线从「保险」变成「随手就被扫」；D1 当天就回撤 6.57%，07-13 顺势破位
- 07-31 平开 +0.08% → 入场就在触发价上 → 1R 保持 **2.63%**，止损线纹丝不动

**⇒ 「挂触发价等触发」永远优于「跳空后追开盘价」** —— 与浩瀚深度（17.38R vs 4.09R）
是同一个结论的再次验证。**跳空越大，这笔单越不该追。**

**★★ 执行推论（这条比案例本身更重要）：**
1. **止损被扫 ≠ 规则失效**，是「这次没成」。同一只票后续再出 T0，**照做**。
2. 前提是**每笔严格按 1R 控制仓位** —— 只有这样 −1.00R 的代价才承受得起，
   才有资格去等 +12.22R 的那一次。**单笔风险失控的话，第一次被扫就出局了，等不到第二次。**
3. 两次合计 **+11.22R**。若因第一次被扫就放弃这只票/这条规则，第二次整段拿不到。

**★ 事前唯一可见的差异（只能当人工复核项，不可写成硬闸门）**：**D0-1 的形态**
- 07-09 前面是 07-08 **+6.90%** —— **连续第二天大涨**（两天累涨 15.1% = 情绪透支），
  D1 高开 +8.82% 就是这段情绪的**最后一口气**
- 07-31 前面是 07-30 **−5.40% 且收在振幅下沿 22%** —— 刚被砸过、洗过，
  07-31 的 +9.75% 是「调整后第一根放量长阳」（反包）= **重新启动**
- ⚠ 此模式在 8 个正案例里**无一致性证据** → **不设阈值、只提示人工复核**。
  这正是此前反复确认的原则：**不为了修好一个案例去改参数**。

**★ 港股数据通道（引擎目前不支持）**：`fetch_us` 对港股代码三源全失败；
东财 `push2his`（`secid=116.00992`）可用但**易触发限流 RemoteDisconnected**；
新浪港股返回 `Service not valid`；Yahoo 隧道 502。
**可用源 = 腾讯**：`https://web.ifzq.gtimg.cn/appstock/app/fqkline/get?param=hk00992,day,,,320,qfq`
（返回 `data.hk<code>.qfqday` 或 `.day`，字段顺序 = 日期/开/收/高/低/量）。

---

**★ 为什么止损锚要放开到全部候选（成都先导案例）**：

成都先导 09-16 是一根 **+14.05% 的大阳**，把均线远远甩在下方：

| 锚 | 值 | 距触发价 37.19 的风险 |
|---|---|---|
| MA20 | 33.26 | 10.57%（旧口径选它，被 >8% 闸门拦下） |
| MA5 | 32.60 | 12.34% |
| MA10 | 32.33 | 13.07% |
| **实体中点** | **34.40** | **7.50%** ← 唯一进闸门 |

⇒ **大阳日实体中点才是「获利盘兑现压力」的分界**，比均线更贴切。
⇒ 旧口径排除 MA5 的理由（「长期贴价必落噪声带」）**在大阳日恰好相反**——MA5 被拉高反而离得远。
   故改为单一规则「离触发价最近」，不再对 MA5 先验排除。

**★ 距墙分档只是「参考」，不是「警告」**：

| 标的 | 距墙 | 你的判定 |
|---|---|---|
| 康龙化成 | 5.21% | ✓ 实盘 +17.5% |
| 东富龙 | 10.58% | ✓ |
| 赛分科技 | 12.97% | ✓ |
| 纳微科技 | 13.04% | ✓ |
| 成都先导 | **14.09%** | ✓ 赚 6% 阳线 |

**五个案例距墙 5.2%~14.1%，多数在 10% 以上。** 而无选股层的全样本回测里「半路」期望 ≈ 0
——**差距来自板块/题材/基本面筛选**（你第 12 条：「我会选板块和题材，选主流板块叠加基本面」）。
⇒ **不因距墙远而否定信号**，只标注位置供人工取舍。分档：贴墙 ≤2% / 半路 2~10% / 远端 >10%。

- **接管规则（用户 2026-09-20 定：「全部接管」）**：只要出 T0，不论贴墙/半路，均**接管当日首选**
  （原买法被压为次选，记 `t0_superseded_mode`）。理由：用户定级 T0 高于 T1，不做二次取舍。
- **均线排列不作闸门**：纳微 9-08 突破时 MA10/MA20 仍纠缠，后面调了几天，不影响最终结果。
  仅记录 `ma_aligned` 供复盘。
- **板块共振是真维度**：「对板块强度有要求，能提高胜率，大部分是同时启动的」。
  实证 TEM/SDGR/ILMN（AI 制药）+ 赛分/纳微 于 2026-09-14~15 同日共振。
  引擎单票判不了板块 → `sector_resonance` 由批处理层（`watch_cn.py` / `scanner.py`）按主题聚合注入。
- **执行（关键）**：P1 触发价在**现价上方** → 券商无 buy-stop/条件单 ⇒ **必须盯盘手动打**；
  P3 买点在**下方** ⇒ **可预挂限价**。两条路径分开写清，不得混为一谈。
- **五个实证案例**（全部走 P1）：

| 案例 | 信号日 | 触发 | 止损 | 风险 | 阻力位 |
|---|---|---|---|---|---|
| 赛分科技 688758 | 09-15 | 29.02 | 27.54·MA10 | 5.10% | 29.80 (08-20) 距墙 5.30% |
| 纳微科技 688690 | 09-08 | 41.87 | 40.53·**MA20** | 3.20% | 46.80 (08-20) 距墙 13.04% |
| 康龙化成 300759 | 08-04 | 40.43 | 37.98·MA10 | 6.06% | 42.00 (07-15) 距墙 5.21% |
| 东富龙 300171 | 09-17 | 14.79 | 14.18·MA20 | 4.12% | 15.88 (08-20) 距墙 10.58% |

  康龙用户实盘：**买 40 → 卖 47 = +17.5% / ≈2.7R**。东富龙为**试仓演示，用户未入场，不作成功案例**。
  纳微止损锚为 MA20（该日 MA20 40.53 > MA10 37.62，离触发价更近者胜出）——**锚规则是「更近的那条」，不是固定 MA10**。

**陷阱提醒（写报告时必须带上）**：
- 全样本层面（无选股）T0 期望 ≈ 0 —— **不是「闭眼照做就能赚」的系统**。撑起期望的是 24% 能吃到 ≥2R 的单。
- 离场时点与收益单调相关：第 2~3 根走 +0.80R ／ 第 7~10 根 +1.98R ／ **第 11~20 根 +3.27R**
  ⇒ 与用户趋势派框架一致：**用移动止损让利润奔跑，不设目标位**。
- 破锚生存率：买入当天被大阴吞回 14.1% ／ 3 根内 32.8% ／ 5 根内 46.4% ／ 10 根内 76.4%。
- **引擎会系统性低估你的实际表现**，因为它没有板块/题材/基本面筛选层（你第 12 条已指出这点）。

**★ 埋伏单是 T1 级入口（2026-09-19 用户定级，实证支撑）**

它不属于上表任何一个「形态」（形态描述**在哪买**，它描述**什么时候把单挂上去**），所以不是新买法；
但**必须单独占用一个 T1 优先级**，因为它与当日 `mode` **并列争夺「先成交者」**：

- 当日 `mode` 的买点若在现价**上方**（突破/追高类），而埋伏单在**下方** → **埋伏单先触发**，它才是实际首选。
  引擎已加 `pre_breakout.beats_current_mode = true` 显式标出这种关系。
- 反过来（买点在下方 = 回踩类），两者不冲突，埋伏单只是「提前在突破位挂了张单」。
- 实测赛分 688758（`_sf_915*.py`）：9-14 `downtrend_tl_break`（T2）+ 埋伏单 28.97；
  9-15 主板 `mode=wait`（大阳后未满 3 根）**当日无买点，只有埋伏单**；9-15 高 29.02 ≥ 28.97 成交 → 至 9-18 收 **+13.22%**。
  **若只等 `mode` 信号，9-15~9-18 那 15.9% 整段踏空。**
- 同预算下：埋伏 28.97 / 风险 3.62% vs 当日顶着买 28.30 / 风险 5.41%
  → **1R 效率 1.46 倍**（买价还贵 2.37%，靠止损更窄赢回来）。
- 字段：`setup_kind="pre_breakout"` / `tier="T1"` / `priority=1` / `role="primary_entry"` /
  可选 `beats_current_mode`。扫描器渲染时**与 T1 同档展示，但须单独一行**，不可被当日 T2 的买点盖掉。

**买区口径（全库统一）**：买区自**防守位 / 突破位单边向上**，带宽 **1.0×ATR**，下沿不低于该位——突破类用突破位，沿线回踩用被回踩的那条线，大阳后缩量回踩用大阳低点 / 缺口下沿。突破模式的定义就是「收盘站上突破位」，在尚未突破的价位挂买单自相矛盾；这同时保证硬止损恒落在买区下沿之下。**跳空例外**：基准上移到**缺口上沿（= 突破根低点 / 大阳低点）**，整段买区不得落进缺口（另见第 5 节「大阳后缩量回踩」）。

**买区不变式（2026-09-16）**：`buy_zone` 带下单带（`primary_lo`/`primary_hi` 非空）**当且仅当**该 verdict 确实给出了可挂单价位。以下情形**一律空区**（`primary_lo`/`primary_hi` = `None`）：
- 大阳后收盘跌破防守位（`broke_yang_low`，`invalid=True`）
- 站上突破位但**价格未确认**（收盘落在当日振幅下半区，或收低于前收 = 上影插针式站上）→ 空区 + verdict「·价格未确认」，note 写明「不算突破」。**量能不足本身不再是空区理由**（平台/W底同此口径）
- 「未形成平台/W底/旗形突破、沿线回踩、大阳后缩量回踩或下降趋势线突破」的兜底 `wait`（结构不成立）

理由：这些分支原先不传 `buy_zone`，`pack()` 便回退到 `bz_line`（活平台沿线位），打印出一个 2×ATR 宽的区间。区间摆在「买区」列、而 verdict 写着「作废 / 不买 / 未形成」——**这是「看到买区、其实没有买点」的误判源**。活平台沿的位置由报表「活平台/R1」列独立承载，不靠 `buy_zone`。有值即代表有挂单价位，这条不变式必须守住。

**可执行闸门（突破类，三档）**——「买区位」与「能不能执行」是两件事，不得用前者卡死后者：

| 现价距突破位 | `recommend` | 含义 |
|---|---|---|
| ≤1.0×ATR（买区内） | `True` | 可执行 |
| 1.0–2.0×ATR（已出买区上沿） | `True` | **仍可执行，但只能挂回踩单（上沿一带），禁止市价追**；`buy_zone.chase_only=True`，报表标「≥上沿·只挂单」 |
| >2.0×ATR | `False` | 不追，`mode=wait`、verdict「突破已延伸·等回踩」 |

即：出买区不等于不能买，只等于不能市价追。唯一的不追线是「不追高」那条——**离买位 >2×ATR 不买**。

**盘中口径（`intraday=true`，2026-09-17 INTC 案例）**

美股日线源盘中**不返回当日半根**。旧实现只把实时价存进 `spot_quote`，结构仍按昨收算，于是**突破当天引擎看不见突破**：INTC 9/17 已站上平台沿 107.57（现价 110.7），输出的 `mode` 却是昨收口径的 `line_pullback`，买区 99.45~104.03 整段落在突破位之下 —— 用户实况：「突破了平台，却让我 104.03 买」。更糟的是三条盘中通道也都判「过期/不给信号」，**输出只剩「等回踩」，而价格一路不回**。

现改为：`session=Open` 时用 Yahoo 5 分钟线合成今日未收盘 bar 并入结构（`merge_intraday_bar`）。判定要点：

- `mode` / `buy_zone` / `days_above_platform` **一律按盘中刷新后的结构读**，不是昨收口径。突破位一旦被站上，买区基准立刻从回踩线抬到突破位（INTC：104.03 → 107.57）。
- 但**决策点仍看尾盘**：输出带 `confirm_at`（北京时间 03:45–04:00 / ET 15:45–16:00）。盘中价非收盘价，收盘仍成立才动手 —— 这是执行纪律，**不是「不做」**。区别在于：买区已随突破上移且现价在区内，读数是「可执行、待尾盘确认」，而不是「只能等回踩」。
- **盘前 / 盘后不合成**：成交稀疏，盘前价越过平台沿不能当「收盘站上」。`session=Closed` 时日线本身完整，也不合成。
- 合成 bar 的 `v` 是**量比口径**（累计量按已走时段折算成预计全日量），`v_raw` 才是累计量。不折算就会把放量突破误标成「非放量」而错压仓位（INTC 12:27 ET：累计 85.5M 直读 RVOL=0.94 → 折算后 2.07 = 放量）。
- A 股不受影响：东财日线盘中同样不返当日半根，`merge_intraday_bar` 直接不合成，A 股走 `in_ash_session` + 盘中三通道。

**预备突破单（`pre_breakout`，2026-09-17 SDGR / TEM / ILMN 案例）**

未突破 ≠ 没有单可挂。`evaluate()` 除当日 `mode` 外**额外**输出 `pre_breakout`。两种挂法，同时够格时取**距价更近**的那个：

**① 水平平台版**（`platform_from` = 平台成形日）：`platform.kind ∈ {pressing, near, broken}`（价格顶在活平台沿下方 / 刚贴近 / 刚站上但未确认）且距沿 ≤1.2×ATR、非极度缩量（RVOL≥0.8）、非下跌段且收盘站上 MA20。字段：`order` / `level` / `trigger` / `hard_stop` / `risk_per_share` / `dist_atr` / `platform_from` / `cushion_note`。

> **2026-09-19 起放宽**：原闸门只认 `pressing`，导致「方向已明但价格还没贴到沿」的日子**无单可挂** → 只能等次日顶着买。实证见下节《★ 预判买 vs 顶着买》，故放宽到 `near`/`broken`。

**② 下移斜线版**（`anchor=down_tl`，2026-09-17 ILMN / TEM 案例，即「画斜线突破下降趋势线」的可执行形式）：价格贴在**下降趋势线**下方、收盘距线 ≤1.2×ATR 且 `regime≠downtrend` 时，在**线上方**挂 buy-stop。字段：`order` / `anchor` / `level` / `trigger` / `hard_stop` / `risk_per_share` / `dist_atr` / `line_from`。
- 触发价取**下一根**的线值 +0.05×ATR：线在下移，用当日线值会把单子挂高。
- 止损 = 线下 **1.0×ATR**（平台版是 0.5×ATR）：ILMN 20 笔样本显示贴沿 0.55×ATR 的止损有 **94% 在 10 日内被扫后又涨回**（错杀），1.0×ATR 才是期望最优。
- 实测：ILMN 9/11 挂 210.13 → 9/15 成交（当日开盘 208.95 直接跳过整条旗面线）→ 至 9/17 收 245.21 = **+16.7%**；TEM 9/10 挂 62.92、9/11 挂 62.18 → 9/14–15 成交 → 至 9/17 = **+26.4% / +27.9%**。

- 它是**独立于当日 `mode` 的 T1 级入口**（不是第七种买法：形态说的是「在哪买」，它说的是「何时挂单」），
  与当日买点**并存、先到先做**，不改变 `mode` / `recommend`。**优先级与 T1 同级**，且当买点在上方、
  埋伏单在下方时（`beats_current_mode=true`）**它就是首选**。详见本节末尾「埋伏单是 T1 级入口」。
- **`triggered` / `status`（2026-09-17 INTC 起）**：盘中口径下，脚本拿**昨收口径**算出的那张埋伏单，去比今日合成 bar 的最高价。越过触发价 → `triggered=true`、给 `fill_px`（跳空则按开盘价成交），`verdict` 前置「**命中预案单 X**」，note 写明「已成交，不再回头等回踩」；未越过则 `triggered=false` 并写明「继续挂着」。**这一条专治「明明挂单成交了、输出还在喊等回踩」**：TEM 9/17 命中 73.18、ILMN 命中 233.85、INTC 命中 106.68，都是当日实际成交的埋伏单。
- 挂法：次日开盘前，在平台沿**上方** `trigger`（= 沿 +0.05×ATR）挂 **buy-stop**（停损买单）；触发即突破确认。
- 止损 = max（平台沿 −0.5×ATR，MA5 −0.10×ATR）；跌回平台沿下方 = 假突破，次日盘中破即走。
- **为何必须有**：SDGR 2026-09-14 活平台沿 21.40 距收盘仅 0.64×ATR，当日却只输出「大阳回踩 18.84–19.86」——该回踩从未发生（9/15 最低 20.34），次日跳空越过买区，整段错过。按本单 21.45 成交 → 至今 **+29.9%**。
- 回测（SDGR 近 13 个月）：10 笔独立挂单成交 6 笔，平均止损仅 **−2.82%**，固定持有 5/10/20 日 **+3.06% / +3.45% / +5.24%**；同期原回踩逻辑有 **3/10 笔在 5 个交易日内完全没成交**。
- 立论：买点是**突破位本身**，不属追高；buy-stop 需触发才成交、假突破自动不成交，故与「不追高」闸门不冲突。
- 注意：**反转初期 `c2` 必为 False**（SDGR 9/14 即如此），故本单的结构守卫用 `regime + MA20`，不得改用 `c2`——否则会把要覆盖的场景整个挡掉。

**强突破例外（`buy_zone.next_day_chase`，2026-09-17 SDGR 案例）**

`chase_only=True`（现价出买区上沿 1–2×ATR）时，若 **RVOL ≥ 2.5**（强突破），次日往往**不给回踩**：SDGR 9/15 RVOL=3.85，次日 23.29 直接跳空越过回踩单 22.59，之后再未回落 —— 只挂回踩单等于整段吃不到。故此时在 `buy_zone` 内额外输出 `next_day_chase`：

| 字段 | 值 | 说明 |
|---|---|---|
| `limit` | 突破位 + 2.0×ATR | **与「不追高」同一条线，不越线** |
| `size_ratio` | 0.5 | 半仓——追入风险更大，用仓位对冲 |
| `stop` | 突破阳 (H+L)/2 | 比结构止损紧，收紧风险敞口 |
| `valid_for` | 仅次日 | 不成交即作废，不延长 |

- 执行：次日开盘后 **≤ `limit`** 才可限价买入；超 `limit` 不追。
- SDGR 实测：9/16 开 23.29 ≤ 23.78 → 成交，止损 22.02，至今 **+19.62%**。
- 大样本（SDGR，收盘创 20 日新高 + 次日开盘买入，n=8）：RVOL≥2.5 组 3 次，5/10/20 日 **+4.58% / +7.78% / +6.84%**，短期优于 1.5≤RVOL<2.5 组（+0.36% / +2.22%）。**但仅 3 次样本，方向一致、不足以当主力——它是次优保底，不是首选路径。**
- 与预备突破单的分工：**能提前埋伏就埋伏（买在突破位，最优）**；没埋伏上、且突破日 RVOL≥2.5 或宽幅强阳，才启用本条（买在延伸位，次优、半仓）。

### 1. 平台突破（优先 T1）

横盘/箱体/前高被收盘打穿。买的是**突破位本身**，不是均线。

- 触发：近 **3 根内**收盘才站上**活平台沿**（脚本字段 `platform`），且**价格确认成立**（收盘落在当日振幅上半区 **且** 不低于前收）。尾盘/盘后确认即买。`R1` 仍是 123 的 P0–P1 反应高，**T1 用 `platform` 不用已死的旧 R1**。刚突破后近 3 根，沿仍是被破的那条，不是突破阳的最高价。
- 已站上超过 3 根 = 突破已结束，改判沿线回踩、大阳后缩量回踩或 wait，**不再标平台突破**。
- **量能不再一票否决（2026-09-17 用户定：价格说明一切）**：`RVOL>1.5` 或宽幅强阳 → 正常仓，note 标「放量确认」；价格已确认但量能未放 → 仍给买区，verdict 标「平台突破(优先T1)·非放量」、note 写「半仓/试错仓，次日不破当日低点再加」。（ILMN 9/17 正是此类：收盘 245.21 站上平台沿、价格确认，此前因「量能不足」被判 wait 而踏空。）
- **价格未确认**（收盘落在当日振幅下半区 / 收低于前收 = 上影插针式站上）：wait，不算突破。**例外（强上加强）**：同时贴轨沿线主升（触及 ≥4 次、收盘在线上 ≤1.0×ATR）→ 两结构互证，按沿线回踩放行，verdict「强上加强(平台+沿线主升)」；收盘已跌破该线则不豁免，仍 wait。
- 布林上轨：贴轨走 + 带宽扩张 = 趋势，可当突破确认；单日爆量刺穿次回内侧 = 派发，不追。
- 123 三项全过（破下降趋势线 + 未创新低 + 过前高）= 本模式的加强版，仍标 `platform_break`（优先 T1），备注下降趋势线已破。

### 2. W底颈线突破（优先 T1）

双底（近似等深摆动低）后，收盘站上两底之间的反弹高（颈线）。买的是**颈线突破**，不是均线。

- 触发：脚本识别 `w_bottom`（左底/右底/颈线），近 **3 根内**收盘才站上颈线，且**价格确认成立**（收盘在当日振幅上半区 + 不低于前收）。
- 结构止损看颈线下；硬止损锚优先突破阳下沿或长阳中点，再下 0.10×ATR gap。
- **价格未确认**（收盘落在下半区 / 收低于前收 = 插针式站上）：wait，verdict「W底颈线突破·价格未确认」。**量能不足不再是 wait 理由**（同平台突破，量能只写进 note）。已站上超过 3 根不再标本模式。
- 与平台突破同时可做时，平台突破优先。

### 3. 旗形下降趋势线突破（优先 T1）

主升之后的**旗形整理**：先有短促旗杆大涨，再有下降高点连成的旗面；收盘站上旗面下降趋势线即买。

- 触发：脚本识别 `bull_flag`（旗杆 + 旗面下降趋势线），近 **3 根内**收盘才站上该线，且**价格确认成立**（收盘在当日振幅上半区 + 不低于前收）。**量能只写进 note，不作闸门**（放量=加分项；无量=「非放量突破，仓位打折」）—— 2026-09-17 用户定：RVOL 不重要，价格说明一切。
- 旗面线若已离价 >2×ATR（形态过陡 / stale），评估时会落到下降趋势线（T2）处理，不会因「存在旗形」硬撑一个不可执行的买区。
- 结构止损看旗面趋势线下。这是升势里的整理突破，标优先 T1，不用泛化的 T2 下降趋势线突破。
- 已识别旗形时，**禁止再标 `downtrend_tl_break`**（脚本 `test_flag_blocks_generic_dtl` 守住此规则）。
- ⚠️ 旗面下降线的锚点取**近端局部高点**（`detect_down_trendline`），不用 `pivots(w=3)`：单调下移段内 w=3 根本不产生枢轴高（TEM 8/27 高 71.89 因左侧 8/24 高 72.02 更高而被丢弃），会取不到锚点。旧实现的「旗杆后右半段最高点」近似也有致命 bug —— 突破那根自己会成为右半段最高点，线被拖到突破价、`days_above_tl` 归零，**形态在突破当天自我消失**（TEM 9/15：9/14 线 62.18 正确，9/15 变成 68.94）。

**旗形 / 平台 / 下降通道 三分判据（2026-09-17 CRSP 案例）** —— 同样是「涨一段 → 整理 → 再突破」，先看整理段的**几何形状**再定归类：

| 整理段特征 | 归类 | 优先级 | 突破位 |
|---|---|---|---|
| 高点**逐级下移**（斜线），且**回撤 ≤2/3 旗杆** | 旗形 `flag_tl_break` | T1 | 旗面下降线 |
| 高点**基本水平**（不下移），收盘站上活平台沿 | 平台 `platform_break` | T1 | 水平沿 |
| 高点下移但**回撤 >2/3 旗杆** | 只算**下降通道 / 斜线** → `downtrend_tl_break` | T2 | 那条斜线 |

- **回撤深度是「旗形」与「通道」的分水岭**：脚本 `detect_bull_flag` 的硬闸门是「回撤 > 0.66×旗杆高 → 不是旗形」。旗面要的是**浅回撤的紧凑整理**（约 1/3~1/2）；回吐超过 2/3 说明抛压已把旗杆吃掉，属深度回调。
- **CRSP（2026-09-17）是「引擎正确拒绝旗形」的案例**（与 TEM/ILMN 的**误拒**相反，那两个是检测器 bug、已修）：脉冲 7/28@45.89 → 8/21@62.53（+36.3%），整理段最低 9/14@51.33。
  - 按整段脉冲算回撤 **67.3%**（超 66% 上限）；按脚本选出的 12 根旗杆（51.51→62.53）算回撤 **101.6%** —— **旗面最低直接跌破了旗杆起点**，形态上根本不是旗。
  - 整理段高点 62.53 → 58.96 → 53.80 **逐级下移**，没有水平沿 → 也不是平台。
  - 故归 `downtrend_tl_break`（T2）。2026-09-17 引擎给的正是「破斜线 2.33×ATR → 突破已延伸·等回踩」。
- **头顶旧高是第二重判据**：CRSP 上方还压着 8/21 高 **62.53** 与 7 月中旧高 **63.45**（`living_platform` 报 `kind=pressing`、`days_above=0`）= **平台压迫但未突破**。这与 TEM/ILMN 突破后**创新高、上方无套牢盘**是完全相反的位置 —— 同样是 +11% 大阳，前者的上方是**供给**，后者的上方是**真空**。评估时必须把「水平高破了没有」单独说清，别只看斜线。
  → 推论：CRSP 若后续**收盘站上 62.53~63.45**，即转入 `platform_break`（T1，且是创新高）；在那之前按 T2 试错仓对待。

### 4. 沿线回踩（优先 T1）

已经在主升里，沿着某条**肉眼可见的线**上升，买回踩**这条线**的点，不追离线过远的价格。

- **默认画线**：用最近两个上升摆动低点 P0→P1 连线，外推到今日，得到买区。这就是「沿趋势线主升」的线。脚本 `buy_zone.anchor=hl_trendline`。
- **加速线豁免（2026-09-18 老罗指出 BE 实例）**：斜率 **>0.25×ATR/根** 的 P0→P1 线视为**加速轨迹线，不是支撑线** —— 它每天自行抬升的幅度已接近标的正常日波动，外推时必然「追上」价格，只要横盘一两根就自动变「跌破」，与价格是否真的走弱无关。此时**该线既不作回踩锚、也不作否决依据**，改由均线锚接手（`rule123.tl_slope_atr()` 计算，阈值 `TL_SLOPE_MAX_ATR`）。
  - BE 2026-09-18：P0=9/1 低 197.50 → P1=9/14 低 249.05，斜率 **6.44 元/根 = 0.341×ATR/天**（实测 34 只样本第 **94** 百分位；50% 分位 0.008 / 75% 0.088 / 90% 0.242）。外推到当日 = 274.83，而**按昨线 268.38 看价格 269.64 仍在线上方** —— 外推一天就翻转了「是否跌破」的结论。当日「跌破」的 5.19 元里，**6.44 元是线自己涨上去的**。
  - 同一豁免也适用于第 5 节「大阳后缩量回踩」的升势过滤（`still_uptrend`）：加速线不作否决，升势只由 P1 与 `c2` 把关。
  - 实测影响面：34 只样本仅 2 只锚变更（BE → `ma5`；山东黄金 600547 斜率 0.258 → `ema10`）。
- **例外（很明显贴着某条均线走）**：近 8 根至少 4 次低点贴着 EMA10 / MA5 / SMA20，当 P0→P1 上升趋势线不可用（加速线豁免或 P0/P1 点数不足）时，才以该均线兜底。趋势线可用时优先趋势线。禁止用 VWAP、禁止「谁 tag 多谁上」把均线/VWAP 选成默认买区。
- **短均线锚趋势闸门（2026-09-17）**：锚为 `ma5` / `ema10` / `sma20` 时，还必须 `regime=continuation` **或** 收盘 > SMA20，否则模式可标沿线回踩但 **`recommend=False`**（note 写「短均线锚·趋势偏弱」）。弱票阴跌/横盘也会反复蹭 MA5，肉眼是下跌趋势却被当成 T1——反例科华数据 002335。`hl_trendline` 不受此限。强上加强豁免同样要过此闸。
- 触发：结构完好（收盘>P1 且自 P1 未创新低）+ 现价距该线 **≤1×ATR** + 缩量尾盘。结构止损 = 收盘破**所回踩的那条线本身**（贴轨时用那条已选均线 EMA10/SMA20/MA5；2026-09-18 起不再统一换成 MA5 —— 见第 6 节「铁律零」）。入场写死。
- 距该线 **>2×ATR** 或 1–2×ATR 未到位：本模式不买，**让给 T2**（看大阳后缩量回踩或下降趋势线突破）；两者都没有才写「等回踩该线」。
- 画不出上升趋势线（P1 不高于 P0）且也没有明显贴轨：不编造买区。

### 5. 大阳后缩量回踩（次优先 T2）

已经在上升趋势里，但还没回到那条上升线。买的是**大阳线之后的缩量回踩**，不是回踩上升趋势线。上升趋势线在此模式里只过滤「还在不在升势」。

- **升势过滤（两条通道，任一成立即放行）**：
  - **常规**：P0→P1 低点连线，收盘仍在该线之上（且未破 P1）。
  - **反转放量修复**（放宽档，2026-09-16 起）：强势票最常见的启动形态是「先破位洗盘 → 一根放量大阳收复 P1」，此时 `c2=False`（自 P1 曾创新低）却已站上 P1。原门控会把这种**真信号整段丢弃**（ILMN 9/15：+6.76%、量 1.70 倍、收 222.29 站上 P1 206.82，却只给 `wait` 且买区为空）。放宽后四条**必须同时**成立：① 收盘站上 P1；② 大阳量 ≥ 其前 20 日均量 ×1.5；③ 大阳实体 ≥1.2×ATR **或** 涨幅 ≥4%；④ 现价未回吐到大阳体下半部。任一不满足即退回常规通道，宁缺勿滥。
  - 放宽放行时输出 `buy_zone.gate="reversal_yang"`、`relaxed=True`，note 前置「【反转态·放量大阳·已放宽】」——**报告里必须显式标注**：这类买点建立在「破位后修复」之上，比常规升势回踩脆，仓位按试错仓走，破防守位不得再看一天。
- **找最近一根大阳线**（近 10 根内）：实体阳、收在上沿，实体或振幅够大（约 ≥0.8×ATR 或涨幅 ≥3%）。一字涨停也算（实体为 0，但相对前收跳空）。
- **大阳当日不追**。后面缩量回踩，同时满足：量缩到**这波调整里的近期最低**（允许约 10% 容差，且低于大阳当日量）；**最近 3 根不再创新低**（每一根低点都不下破此前回调低点，允许 0.1×ATR 噪声）。后者优先：说明主力还在控盘，不是自由落体。比急杀再走 N 字更强。未满 3 根或近 3 根仍在创新低 = 不买。
- 不要用「大阳量的 70%」那种比例。
- **买区（下单带）的统一口径**：贴防守位（大阳低点 / 缺口下沿）**单边向上 1.0×ATR**，与突破类买区同构。**大阳体「低点~高点」是「未走坏的有效性边界」，不是买区**——它常宽达 2~2.5×ATR，落在这样的区间里等于没有价位：挂在区间下沿和挂在上沿是两笔完全不同的交易，无法执行。判断「是否还在调整区」用大阳体，「挂在哪个价」用买区，两件事分开。
- **普通大阳（有真实日内振幅）**：买区 = **大阳低点 ~ 大阳低点+1.0×ATR**。防守 = **大阳低点**。收盘跌破大阳低点 = 作废。
- **跳空高开、但大阳有实体/光脚**（开盘远高于前收，低点≈开盘，如石头 8/25 开 125 收 135、前收 112.5）：缺口在大阳低点下方。买的是**缺口上沿 = 大阳低点**一带（买区 = 大阳低点 ~ +1.0×ATR），**不买回补缺口**。防守仍是大阳低点；跌进缺口 = 破防守。
- **一字板 / 振幅极小的跳空**（当天几乎没有低点，板价不能当防守）：买区 = 回踩缺口，取 **前收 ~ min(板价, 前收+1.0×ATR)**。防守 = **缺口下沿（前收）**。收盘跌破前收 = 缺口回补失败，作废。不要把涨停价/一字价写成止损。
- 大阳后继续创新高延伸、没回到买区 = 不追。量没缩到近期最低、或近 3 根仍在创新低 = 不买。
- **「回到买区」= 回到上面那个 1.0×ATR 的下单带，不是「回到大阳体内」。** 回踩到大阳中部 / 上半部不算到位，仍属「等回调整区」。
- T1 沿线回踩已经贴线可做时，不执行本模式。

### 6. 下降趋势线突破（次优先 T2）

下跌段里、**没有可识别旗杆**时，收盘站上**下降**的高点连线。买趋势线突破位。这是反转里较早、也较容易失败的一档，所以是 T2：仓位用试错仓，目标先看到前高/平台沿；过了前高则升级为平台突破（T1）。

- 触发：**下降趋势线（斜线）**近 **3 根内**才被收盘站上，且**价格确认成立**（收盘在振幅上半区 + 不低于前收）。量能只写进 note，不作闸门。
- **线的画法（2026-09-17 修）**：取**近端局部高点**中「依次下移」的那条，按「被最多触碰、被最少收盘穿越」评分选出（`detect_down_trendline`）。**不得用「P1 之前两个枢轴高」** —— TEM 2026-09 那样会取到 8/12@57.03 与 8/21@72.96（一条**上升**线）并外推到 97.99→111.65（当期价格仅 60-70），`declining=False`，本模式永不触发，连 123 的条件1（站上下降趋势线）也永远为 False。
- 未破线：不抄底。**但若价格已贴到线下 ≤1.2×ATR，并给出 `pre_breakout`（`anchor=down_tl`）的 buy-stop 埋伏单 —— 那才是本形态的首选入口**（买在突破位，不追高）。
- 已破线超过 3 根、又还没过平台：不追趋势线，等平台突破或等回踩。
- 与 T1 同时成立时，执行 T1，本模式只作备注。
- 已识别 `bull_flag`：改走旗形突破（T1），本模式不标。

### wait

没有可执行的 T1/T2，或距离买位 >2×ATR。只出预案，结论写「现在不买」。

---

## 赔率与买点前移（环境适配，2026-09-18）

**原则不变式**：寻找最强方向 → 等待风险收窄 → 在赔率对自己有利时下注。  
突破 / 支撑低吸 / 紧凑启动，只是实现它的不同形式，**不是互相取代的信仰**。

### 什么叫「标准突破」

本 skill 口语里的标准突破 ≈ 一年/阶段新高、杯柄、高位平台、颈线/旗面被收盘确认那一类——对应脚本的 `platform_break` / `w_bottom_break` / `flag_tl_break` /（部分）`downtrend_tl_break`，以及盘中「关键位突破 / 突破预案单」。

它**没有失效**。在流畅单边里（突破→上涨→整理→再突破），确认往往比提前几个点更重要，仍应大量使用。

> **唯一交叉项：`flag_tl_break`（旗面突破）。** 旗面本身是紧凑结构，突破点既是「突破确认」也是「波动收窄后的第一次启动」——它归入哪一类，取决于**止损到结构失效位的距离**（越近越算前移），不取决于模式名。别把这条交叉当成前后矛盾。注意 `downtrend_tl_break` 不是交叉项：脚本在「已有旗形」时会明确不指派它（`test_flag_blocks_generic_dtl`）。

### 为什么要买点前移

近年更多行情是：快速上涨 → 快速释放 → **突破后高波动** → 漫长震荡。此时标准突破的尴尬是：

- 止损贴近 → 容易被正常 4%–8% 振幅洗掉（AEHR 类：突破后次日深撤仍可再新高）。
- 止损放到真正结构支撑 → 单笔风险常到 8%–10%，而预期空间只有 15%–20% → **赔率不够划算**。
- **A 股更吃亏**：T+1 不能当日纠错，为抗洗把止损放宽的成本更高。

**赛分科技案例（688758，2026-09-18，A 股 T+1 的赔率教训｜已用引擎回放核实）**

- **9/16 收盘结构**（`probe_intraday.py 688758 --asof 2026-09-16`，即 9/17 开盘前该挂的单）：`mode=w_bottom_break`、`recommend=True`，买区 **28.87–30.72**，基准日收 29.77 **已在买区内**；预案单 = **限价买 29.50**（可预挂 ✓，27.86~29.77 区间隔夜有效、不用盯盘），止损 结构 28.87 / 硬 27.86，目标 31.97，**RR 1.51:1**，数量 457 股（账户 27.0%）。
  - **9/17 实际最低 29.40 < 29.50 → 这张限价单当天必然成交。** 拿到 29.50 的成本，9/18 现价 31.80 = **+7.8%**，且距结构止损 28.87 仍有 −7.2% 缓冲。
- **9/17 收盘结构**（`--asof 2026-09-17`）：收 **31.18** 已升到买区上沿**之上** → 引擎改口「**只能挂回踩单在 30.75 一带，禁止市价追**」，并给突破预案单：**触发 站上 32.16（K=31.97 摆动前高 +0.1×ATR）/ 硬止损 31.41（K−0.3×ATR）/ 目标 35.73 / RR 4.76:1**。
- **9/18 实际**：开 31.55 → 高 **33.33** → 低 **31.16** → 现 31.80（均价 **32.31**，量比 4.97，振幅 6.96%）。回踩单 30.75 **未成交**（最低 31.16）；33.33 越过前高 31.97 后收回其下 = **收盘口径未确认**。
- **实盘 32.1 追入的代价**：①**成本比 9/16 那张能成交的 29.50 高 8.8%**；②当日最低 **31.16 已跌破预案单硬止损 31.41**，但**当日买入 T+1 卖不掉** → 止损只能推到次日、隔夜风险自担。对照：9/16 口径的持仓今天距止损还有 7% 缓冲，33.33 那波 **+6.9%** 也只有前一日持仓吃得到。
- ⚠ **为什么「RR 4.76:1」在这里是假的**：突破预案单的止损 31.41 距触发价仅 2.3%（分母窄 → R 好看），但**A 股当日买入时该止损不可执行**。这与本 skill 已有纪律一致——**只看扣成本后的账户口径，不看 R**；窄分母带来的高 R 在 T+1 下兑现不了。

→ **结论：「前移」的主要收益不是「买得更便宜」，而是「成本更低 + 止损落在可执行的当天」。** A 股尤其如此——**当日买入 = 当日不设防**。这就是"等尾盘突破成本更高，所以只能买点前移"的机制解释。

要点补充：**前移不是"提前猜突破"**，而是走引擎已给出的**可预挂限价单/`pre_breakout` 预案单**（本例 9/16 的 29.50）。但**若前一日是 `wait`（无有效买区），前一日依旧不该买**（见下方强制决策 4）。

### ★ 预判买 vs 顶着买：买突破的第一性选择（2026-09-19 实证，用户提出）

用户命题（2026-09-19 原话）：「**买突破 setup，预判突破的盈亏比更高**……我昨天买英特尔确认突破了，还是被扫出去了，但是如果在英特尔即将突破那天 9-17 日立刻追涨买入，就有浮盈可以扛住小幅度回撤。所以我以后的目标应该改为**追求盈亏比 + 小幅度止损**，而不是直接顶着买突破，**尤其是 A 股取得先手主动权更重要，因为是 T+1**。」

**结论：命题成立，且 A 股侧的差距比美股更大。** 以下是引擎逐日回放的实测（口径：结构止损用 `buy_zone.struct_stop`，T+1 盘中最低价触及即计「被扫」）。

#### 一、核心数据（配对样本：同一事件里两种买法都成交）

| 标的 | 事件数 | A 顶着买（T日收盘） | B 预判埋伏（T触发价） | 配对样本差距 |
|---|---|---|---|---|
| 赛分科技 688758 | 35 | T+1 被扫 **49%**（17/35） | T+1 被扫 **13%**（2/15） | 73% → 13% |
| 康龙化成 688621 | 88 | T+1 被扫 **52%**（46/88） | T+1 被扫 **2%**（1/44） | 57% → **2%** |
| TEM / ILMN / SDGR / INTC / BE / MU / HPE（美股 9 只合并） | 102 / 54 | 平均 R **+3.28** | 平均 R **+3.76** | 收益差 B−A **+6.62%** |

**美股全样本（突破类模式，剔除回踩类）**：

| 入场 | 样本 | 平均收益% | 平均R | 截断均R | 胜率% | 止损数 |
|---|---|---|---|---|---|---|
| A 顶着买（T收盘） | 102 | +5.24 | +3.28 | **+0.30** | 15.7 | 84 |
| B 预判埋伏（T触发价） | 54 | +13.71 | +3.76 | **+1.07** | **31.5** | 37 |
| C 次日开盘（T+1开） | 83 | +7.17 | +17.00 | +0.51 | 18.1 | 65 |
| D 回踩买 | 16 | +0.26 | −0.63 | −0.63 | 6.2 | 15 |

> **裸均 R 会被单笔 100R+ 的离群值污染**（MU 曾出 +152R），故必须看**截断均 R**（R 上限 +10）与**胜率**：B 路截断均 R **+1.07 vs A 路 +0.30（3.6 倍）**，胜率 **31.5% vs 15.7%（2 倍）**。这才是真实差距。
> **D 回踩买（-0.63R）是四种里最差的** —— 「一直等回踩」不但会踏空，回测期望还是负的。

#### 二、机制：为什么预判买赢

不是「预测得准」，而是**同一根止损线，买点更低 → 1R 更小 → 同样收益换算成 R 更大**。配对样本实测：

- **平均买入价差 = +2.45%（中位 +1.98%，+0.41×ATR）**：预判买永远买在更低的位置。
- **止损美元距离缩短**：同预算 $70.42 下，A 路平均可买 **94.6 股**、最坏敞口 **$797.8**；B 路平均 **27.0 股**、最坏敞口 **$159.1**，扣掉当日收盘浮盈垫后 **$122.1**。A 路「止损看着窄」是假象——**买在最强势当日收盘，次日一次正常回撤就触发**。
- **A 股 T+1 的放大效应**：信号日买入者**当天无法离场**，只能承受次日回抽。实测「信号日收盘落在振幅上 30%（强收盘，引擎偏好的突破）→ T+1 被扫 57%；落在下半区 → 36%」。**引擎越认可这个突破，顶着买越危险** —— 因为强收盘本身就意味着当日已涨到振幅顶部。

#### 三、执行规则（强制）

1. **突破类`mode`（platform_break / w_bottom_break / flag_tl_break / downtrend_tl_break）出信号时，首选入口永远是 `pre_breakout` 埋伏单，不是当日收盘价追入。**
2. **A 股**：T-1 收盘后（15:00 后）确认 `pre_breakout` 存在，即用券商**条件单 / 限价单**在 `trigger` 挂买；次日在 T 日触发成交。**T 日绝不能「确认后顶着收盘价买」**——T+1 无法当日纠错，被扫率 49~52%。
3. **美股**：`pre_breakout` 的 `trigger` 在现价**上方** → 致富证券无条件单，**不可预挂** → 只能盯盘；美股盘中在北京深夜 ⇒ **实际不可执行**。故美股遇到突破类信号时，**优先在池里选「买点在下方（回踩类可预挂限价单）」的标的**；当日突破只能选「不加仓」。这条与 A 股不是相反，是同一个约束。
4. **仓位折扣**：预判埋伏单按**全仓**（1R = trigger − hard_stop，风险预算照常）；**顶着买只给半仓**，且必须在 T 日收盘前完成、不隔夜追。
5. **失效条件**：T 日收盘价仍低于 `trigger`（未触发）→ 单子继续挂，但**不得改为收盘价买入**；价格跌破 `hard_stop` → 埋伏单作废（假突破）。
6. **`cushion_note` 必读**：触发成交后当日收盘多半已带浮盈垫，**这个垫子就是 T+1 低开的缓冲** —— 它是「先手权」的物理形式，也是 T+1 市场里唯一能替代「当日止损」的东西。

#### 四、与「不追高」的关系（不冲突）

`trigger` = 突破位 + 0.05×ATR，买的是**突破位本身**，不是延伸位；且 buy-stop 需真被触发才成交，假突破自动不成交。这正是「不追高」的执行形式，不是它的例外。

### 用户说「月线大杯柄」时：先证伪形态，再算量度是否已兑现（赛分 688758，2026-09-19）

用户视觉上认定的「杯柄」经常是**拼接错觉**。收到「杯柄 / 大底 / 新高突破」这类形态判断时，按固定三步走，
**顺序不能反**：

**第 1 步 — 证伪：把形态拆成不重叠的段，逐段命名。** 杯柄的辨识标志是「杯」与「柄」**连续、同级别、时间相当**。
赛分实际是**三段**：
| 段 | 价格 | 时间 | 真实性质 |
|---|---|---|---|
| ① | 29.00 → 14.21 | 3 个月 | **上市后崩盘**（IPO 估值消化/解禁），不是「杯」 |
| ② | 15.85 ~ 23.40 | **12 个月** | **长期底部盘整平台（base）** ← 主体 |
| ③ | 28.59 → 19.20 → 33.33 | 2.5 个月 | 平台突破 + 一次 **shakeout**，不是「柄」 |

→ 正确命名 = **12 个月底部平台上破（Stage 1→2 转折）**，不是 O'Neil 杯柄。

**第 2 步 — 逐条比对 O'Neil 硬指标**（赛分五条里四条不符）：
| 要求 | 赛分实际 | 判定 |
|---|---|---|
| 杯宽 7~65 周（多数 3~6 月） | 48 周 / 11.5 月 | ⚠ 偏长 |
| 杯深 12%~33%（熊市可 40%+） | **47.6%** | ⚠ 偏深 |
| 柄深 ≤ 杯深 1/3 且 ≤8~12% | 33.33→19.20 = **42%** | ⚠ 过深 |
| 柄在杯上半部 + **量能萎缩** | 7 月**放量**、不在上半部 | ⚠ 不符 |
| 突破放量 | 9/18 RVOL **1.98** | ✅ 符合 |

**第 3 步（最容易漏）— 先算「经典量度是否已兑现」，再说空间。**
```
杯柄量度 = 突破点 + 形态高度 × 1.0   →  赛分: 23.40 + 7.55 = 30.95
```
赛分现价 **32.80 已越过 30.95 达 +6.0%** ⇒ 正确答案是「**这个级别的量度已经走完**」，
剩下的不是杯柄空间，而是**平台突破后的常规趋势延伸**。
**绝不能说成「前面还有 30% 空间」** —— 把已兑现的量度当成未来空间，是这类提问最常见的错答。

**量度口径必须并列给出**（选不同的突破点/起涨点，结果差 15% 以上），并标注哪个已被越过：

| 口径 | 赛分数值 | 现价 32.80 相对 |
|---|---|---|
| 突破点 23.40 + 杯深 7.55（×1.0） | 30.95 | **已越过 +6.0%** |
| 起涨点 19.20 + 杯深 7.55 | 26.75 | 已越过 +22.6% |
| 突破点 ×1.618 延伸 | 35.62 | +8.6% |
| 平台再投射半量（31.97 + 3.78） | 35.75 | +9.0% |
| 平台再投射 ×1.0（31.97 + 7.55） | 39.52 | +20.5% |

**⚠️ 与用户趋势派纪律的接口**：用户纪律是「**突破单不设目标位、用移动止损让利润奔跑**」。
所以量度表只能作为**参考**，**措辞不能变成「到 35.77 就走」** —— 用前高/量度当目标位 = **框架错配**
（2026-09-06 已因同类错误被纠正过一次）。

**⚠️ 空间之外必须叠加的约束（不许只讲上涨空间）**：
1. **隐形减持雷**：持股跌破 5% → 后续减持**不再公告 = 不可监控抛压**（赛分 2026-09-16 一致行动人降至
   4.9999997%）。**止损是唯一防线**，不能因形态好看而放宽。
2. **止损是否落在噪声带内**：赛分 9/18 单日振幅 **2.17 元** > 距用户止损 **1.64 元**
   → **一次正常日内波动就能打掉止损**。这是买入时埋下的，**不是现在该改的** ——
   绝不能因此下移止损（手痒模式高发点），只能接受它。
3. **两档止损要分清口径**：用户自设 31.16（收盘破）vs 引擎结构止损 31.97（更紧 0.81）——
   盘前必须二选一写死，不许盘中摇摆。


所以要问的不是「形态够不够教科书」，而是三件事（研报/对话必须能回答）：

1. **在哪里知错？**（结构失效位）  
2. **错了亏多少？**（相对入场的 % / R）  
3. **对了能赚多少？**（目标1 空间 vs 上述风险）

### 两种更常优先的入场（仍只映射到现有六种，禁止自拟第七种）

标的筛选**不变**：强行业里的强票、相对强度够、有量、最好已证明「有人要」。不是改成抄弱势底。

| 表象 | 找什么 | 映射到本 skill |
|---|---|---|
| 关键支撑附近低吸 | 强势股回撤到前期突破位 / 重要均线 / 平台上沿 / 明确密集区；买的是**可定义的风险**，不是「跌够了便宜」 | 优先看 **`line_pullback`**；大阳后缩到防守带看 **`impulse_pause`** |
| 紧凑盘整后第一次启动 | 振幅与成交逐步收缩、K 线变紧、回撤变浅；供给衰减后**第一次明确启动**，不必等一年新高/所有前高被扫完 | 优先看 **`flag_tl_break`**（旗面=紧凑结构，见上）与 **`impulse_pause`**；「临近启动」的真正渠道是 **`pre_breakout` 预案单**——它**不是第七种买法**，而是与 `mode` **并存**的挂单渠道（活平台 `platform.kind=="pressing"` 或下降斜线距价 ≤1.2×ATR 时，挂 buy-stop 在突破位、不追高）。**前移的第一主渠道就是它**：SDGR 2026-09-14 活平台沿 21.40 距收盘仅 0.64×ATR，当日 `mode` 只给「大阳回踩 18.84–19.86」（从未发生），按预案单 21.45 成交 → 至今 **+29.9%** |

> 盘中的 **通道 A/B/C** 不在此表的"优先"之列：它们是分钟级通道，单笔 R 确实更小，但**本 skill 自己的回测口径下净账户仍全为负**（预案单 −0.019%/笔；通道 A −0.001%~−0.014%；通道 B −0.172%；通道 C −0.151%~−0.152%，见「盘中通道回测结论」节）。所以盘中通道**只可小仓试错、必须自带失效条件，不得当默认优先**——「别死等日线新高」不等于「换成无证据的盘中追单」。

两者底层同一句：**风险已经收窄，但行情还没有被充分交易。**  
- 支撑低吸 ≈ **空间上**风险收窄（离失效位近）。  
- 紧凑启动 ≈ **波动上**风险收窄（振幅/供给收缩后的扩张）。

### Agent 强制决策（出计划时）

1. **先算标准突破路径的赔率**：若按「等收盘站上 / 突破预案」入场，硬止损要放到何处才不会被常态波动扫掉？单笔风险是否已经接近目标1 空间的一半以上？若是 → 在对话里标「标准突破·赔率偏弱」。
2. **同时扫描前移路径**：同一只强票，是否已有可执行的 `line_pullback` / `impulse_pause` / 旗形·紧凑启动？若有且 **知错更快、单笔风险更小、到目标1 的 R 更好** → **优先写前移买点**，标准突破降为「若错过前移、仅在流畅趋势里的备选」。
3. **环境开关**（可量化口径，别用"感觉很强"）：`regime=continuation` **且**突破后 **2 个交易日内收盘脱离突破位 ≥1×ATR**、且回撤未跌破突破位 → 判为流畅主升，可主做标准突破，不必为了「提前几个点」去猜支撑；反之（突破后次日即深撤、日振幅 ≥4%、或止损被迫放宽到 ≥8% 才能抗洗）→ 转前移口径。
4. **禁止用事后结果改写规则**：例如赛分当日盘中冲新高、**收长上影（假突破）** → 不能倒推「昨天就该预判突破、在 32 下随便买」。昨天能买的前提是：**昨天已有前移类 mode + 有效买区**；若昨天是 `wait`，事后便宜 ≠ 当时纪律允许。反过来，若昨天确有有效买区而当时没买，那要复盘的也不是「该预判突破」，而是**为什么没执行既有买区**。
5. **禁止**把「我觉得会突破」写成突破模式的买区下沿之下挂单——突破类买区仍必须在突破位之上（见买区不变式）。想买突破之前，只能走支撑/盘整启动对应的 mode。
6. **本节只改呈现与推荐顺序，不改引擎输出**：`mode` / `priority` / `verdict` 仍由六种买法决定（`wait` 就是 `wait`）。前移节的作用是决定**先写哪条路径、标注哪条赔率偏弱**，以及**是否用 `pre_breakout` 预案单替代临场追单**——它不是一条能绕过六种模式的通道。

### 与双轨打分

短线博弈分评估赔率时：若只有标准突破且止损不得不放宽到 ≥8%，而预期空间 <2× 该风险 → 博弈分不宜高于 5.5，并写明「突破后高波动·赔率差」。存在清晰前移买点且 R 更好 → 可上调博弈分理由里的「赔率」一项。

---

## 分析六维（必须逐项覆盖，数据带来源+时点）

1. **基本面**：最新季报（营收/净利/EPS 同比环比）、业务结构与壁垒、一次性损益还原、资产负债与 ROE。
2. **估值**：选 PE/PEG/PS 或 EV-EBITDA；拆「股价已计入什么预期」；历史分位 + 同业。
3. **技术面**：先报 `mode`，再写均线/量能/RSI/布林/β。支撑阻力只用价格结构、均线、VWAP、斐波那契。
4. **资金面**：成交额/换手/量比、回购增发、机构与内部人。
5. **催化剂**：业绩/产品/监管、下次财报（本地+北京，盘前/盘后）。
6. **风险 + 扫雷（强制）**：短期 vs 中期分开；**每只票分析前必须扫雷**，不得只写技术面就给买入。至少逐项核实并写进研报（无则写「未查到 / 近 N 日无公告」，带来源+日期）：
   - **大额解禁**：近 1–3 个月限售解禁规模（股数/市值占比）、解禁方类型（大股东/机构/员工）。
   - **减持 / 清仓式卖出**：大股东、董监高、5% 以上股东已公告或进行中的减持计划与进度。
   - **立案调查 / 监管处罚**：证监会/交易所立案、问询函、ST/*ST、退市风险、财务造假传闻的官方进展。
   - **其他重大利空**：股权质押爆仓风险、定增/可转债大额摊薄、商誉减值预告、诉讼仲裁、债券违约、美股对应的 SEC 调查 / Going Concern / 大规模稀释发行。
   - **处理规则**：近 2 周有大额解禁或减持窗口、或立案调查未结案 → 默认降仓/不做，或结论改为 wait，并在计划卡写明；不得假装没看见仍按六种买法满仓执行。

---

## 强制双轨打分（各 10 分，供用户综合评判）

每份研报/对话结论必须给出两套分数，**禁止只打一个总分糊弄**。分数是独立判断工具，不替代 `mode`；`mode=wait` 也可以高博弈分（弹性大但现在不买），`mode` 可买也可以投资价值很低（只配试错）。

### A. 投资价值分（0–10）：适不适合当中线/配置仓

综合基本面质量、动态估值、增长确定性、预期是否已被股价计入、扫雷。默认锚点：

| 分 | 含义 |
|---|---|
| 8–10 | 质地好、估值有安全边际或增长清晰未被透支，可作 Conviction/中线核心 |
| 6–7.5 | 可做，但要折价/催化剂验证；仓位中等 |
| 4–5.5 | 故事或弹性在，估值偏贵或兑现不确定；最多试错/卫星仓 |
| 0–3.5 | 贵、逻辑弱、雷未清，或主题已高潮透支；不适合当投资仓 |

打分时必须各写一行依据（可短）：业绩质量 · 估值（PE/PEG 等 +「股价已计入什么」）· 未来 2–4 季预期差 · 壁垒/竞争 · 扫雷。**估值已透支高增长时，即使业绩好看也不得超过 6 分。**

### B. 短线博弈分（0–10）：适不适合按六种买法做弹性交易

综合 `mode` 清晰度、波动/弹性、量价结构所处阶段（吸筹/主升/高潮/派发/反弹箱）、赔率（距止损 vs 目标1）。默认锚点：

| 分 | 含义 |
|---|---|
| 8–10 | 模式清晰、结构干净、盈亏比好，可按计划执行（仍守仓位纪律） |
| 6–7.5 | 可做试错，但上方有压/量能一般/结构未完全确认 |
| 4–5.5 | 弹性有，但位置差（高潮后中继、假突破风险）或模式勉强 |
| 0–3.5 | 无模式、假突破、或主题退潮杀估值期，不碰或只守已有仓止损 |

**禁止**：把「今年涨停次数多 / 弹性大」直接加成投资价值分；涨停堆在主升高潮里，通常降低而非提高投资价值分。  
**禁止**：用博弈分抬高投资价值分，或反过来。两套分可以明显背离（例：投资价值 4.5、短线博弈 6.5）。

对话摘要与 HTML 首屏必须出现：`投资价值 x/10 · 短线博弈 y/10` + 各一句理由。

---

## 共用纪律

- **【正向·选股准入·池子构建】只找「前期未充分爆炒 + 走势良好 + 有新叙事」的标的（与下条总否决负向过滤配套，构成完整选股闭环）**：① **前期未充分爆炒**——近 12 月无数倍级主升浪、或已充分换手且未把最乐观预期 price in（与总否决①②③互斥，命中即不进池）；② **走势良好**——`rule123` regime=continuation 或结构 intact、均线多头排列、量价健康（涨放量跌缩量、非派发顶）；③ **有新叙事**——存在未被充分定价的产业催化（国产替代 / 新业务 0→1 / 技术迭代），且有真实订单或业绩托底信号、非纯题材。三者齐备才入池；入池后每日复盘**先过总否决负向过滤（爆炒透支/低PE陷阱一律否），再过六种买法**定买点。老罗 2026-09-20 总结固化。
- **【总否决·标的级·最高优先级】已爆炒、预期被严重透支的票，放弃不做（入场前先过这道，优先级高于六种买法）**：**本质=估值锚不匹配**——标的曾被爆炒，市值已把最乐观的未来业绩预期 price in 甚至透支；即便表面增长率很高，利润绝对值（数亿级）也配不上爆炒出的市值（千亿级），属「严重高估」，不参与。判定路径（任一满足即判「已爆炒」）：① 回看 6–12 个月出现过数倍级单边主升浪（近 12 月最大单波 ≥ +100% 且陡峭无回调），现处爆炒后修复/博弈段（现价距该顶回撤 <50%，或现价 ≥ 前高 70%、上方全是套牢盘即「第二浪修复中段」）；② 爆炒期市值相对当时/预期利润明显透支（如宏和 603256 市值曾爆炒至千亿、年化利润仅数亿，最乐观业绩也匹配不上）。③ **低PE陷阱 / 盈利质量差（反向的估值锚不匹配，假便宜）**：表面动态PE低、增长率看似高，但利润本质是周期顶点的一次性利润（囤货涨价 / 周期套利），业务无真技术壁垒与定价权（如存储模组厂，颗粒依赖海外原厂、吃涨价利差；对照真原厂「三海美闪」=三星/海力士/美光/闪迪，有产能与定价权、能平滑周期），周期拐点利润成倍回吐、经营现金流差甚至为负。市场给低PE正是用「盈利顶点对应估值低点」定价，非价值洼地——杀估值阶段直接暴跌，不参与。识别锚：业务是「原厂 / 真技术壁垒」还是「模组 / 贸易 / 周期套利」，后者即便低PE也**看都不要看一眼**，随它后面怎么折腾。满足任一（①或②或③）即**直接放弃 / 降为 wait**，即便 `rule123` 给出可买 `mode`（如 continuation / T0 均线收复+过昨高）也不执行——价上看是套牢盘压制再涨空间，根上是预期已透支、再走主升基本不可能。**唯一例外**：放量**有效突破前高**（站上前高且收盘确认、量 ≥ VMA20×1.2）才视为新主升确认，可从 wait 转可观察/建仓；突破前不碰。与「强共振后不接一致预期」「T0 跳空追买看盈亏比」正交（那两条管板块/买点节奏，这条管标的本身历史与估值锚）。实证：海星 603115（2026-09-20 老罗否决：PE109x + 已走完 +313% 主升 + 现 93.68=前高71%、上方40.8%套牢区 → 放弃）；宏和 603256（市值曾爆炒至千亿、利润数亿，最乐观业绩也配不上市值 → 不做，老罗 2026-09-20 补）。
- **赔率优先于形态信仰**：先回答知错位 / 错亏多少 / 对了赚多少；标准突破与前移买点按「赔率与买点前移」节切换，不把某一种买点当唯一正确答案（尤其 A 股 T+1）。
- **尾盘确认**：突破与回踩都以收盘为准（A 股契合 T+1）。盘中尖刺不追。
- **不追高**：离买位 >2×ATR 不买。大阳当日不追。不追的是尖刺和延伸，不是「趋势不能买」。
- **强共振后不接一致预期（T0 级买法专属，连涨走满只观察不买）**：同板块多只核心票（≥5 只）近期同步上涨、板块 β 共振强，只作「信号有板块背书」的加分项，不代表此刻能追。**连涨 ≥3 天且累计涨幅走满**（现价相对 MA20 溢出 >10% / 距目标墙 <3%）的票，追高即接板块一致预期——止损锚不动、入场价远离 K 把 1R 撑大、潜在亏损幅度涨、盈亏比塌 → 这类票**只加自选观察、不买**；正确动作是等**温和调整**：回踩不破结构位（大阳中点 / 活平台沿 / MA5）且缩量企稳，才转为买点。与「不追高」同源：管的是「别在一致预期最满时接盘」。实例：富瀚微 300613 与星宸 301536 同属安防SoC 强共振，富瀚微连涨 +18.89%、距 MA20 溢出 +10.7%、距墙 2.13% → 降级只观察，回踩实体中点 78.32 附近企稳再评估（2026-09-20 记）。
- **T0 跳空追买看盈亏比（T0 级买法专属）**：T0 次日开盘已在 K（昨高）之上（跳空）→ 可以用开盘价追，但**必须先算盈亏比**：入场价 = 开盘价，止损锚不动，1R = 开盘价 − 止损。**已远离均线（距 MA20 溢出 >10%）或连涨走满（累计涨幅过大）的票，跳空后 1R 被撑大、盈亏比塌 → 撤单不追**；乖离率／涨幅尚合理、RR 仍达标的小跳空可接。与「强共振后不接一致预期」同源：管的是「别在一致预期最满 + 价格已远离结构位时接盘」（2026-09-20 订，替代原先「跳空即撤不追」的一刀切口径）。
- **缩量**：**回踩类**（沿线回踩 / 大阳后缩量回踩）要缩量；放量滞涨或缩量「站回」= 假确认（MDB 8-21 类）。**突破类不看量能**（2026-09-17 起）：下降趋势线家族与平台/W底**一律按价格确认** `price_conf`（收盘上半区 + 不低于前收），量能只作加分标注（非放量 → 半仓/试错仓）。
- **低开 ≠ 低吸**：跳空低开只是「便宜」，不是「回踩到位」——它本身是弱势信号。低吸必须同时满足：① 有企稳证据（缩量 / 长下影 / 收回关键位），② 买入价落在买区内，③ 买入价 **> 硬止损**。买在硬止损之下 = 开仓即止损，再便宜也不做。反例：兆龙互连 300913 2026-09-17 开 42.80（低开 −2.1%），买区下沿 43.32、硬止损 43.19，三条全不满足，开盘首根买入当日即 −4%。
- **止损**：两层必须同时写出具体价，禁止合成一个价。结构止损=收盘破才走；硬止损=盘中破则走（定价与触发见「硬止损怎么定」）。结构位：平台突破在突破位下；W底在颈线下；旗形在旗面趋势线下；沿线回踩默认 **MA5**（入场前写死，不用 MA10 除非计划另声明）；下降趋势线突破在该趋势线下；大阳后缩量回踩在大阳低点下（一字板在缺口下沿/前收下）。禁止整数关/心理位。逻辑止损：催化证伪、板块转弱、龙头负反馈。时间止损：短线 5–10 个交易日未验证则降仓。
- **Conviction vs Swing**：用户说「好生意/核心名/信早信」→ 可付溢价提前买，但仍要小仓 + invalidation。用户说「技术/回踩/不追高」→ 严格按六种模式。禁止一把梭。

`rule123.py` 辅助字段：`regime`（continuation / reversal / mixed）只描述位置，**不单独否决**。HH 两两比较只当备注。不用 MA60。

---

## 盘中先手（Probe）：不踏空上板 / 大阳

**原则：先手靠「盘前挂单」，不靠「盘前判断」。** 收盘后买区已定，次日开盘前把单挂好，让价格来找你；等它涨起来再判断，那一刻价已经出了买区。盘中主动出手只在**量能突变**与**关键位突破**这两个信号上开口，且仓位更小、止损更近。

> 与「尾盘确认」的关系：预案单是**计划内**、挂在买区内的单，不是盘中追尖刺。「尾盘确认」约束的是临场市价追单。两者不冲突。

### 一、预案单（先手主渠道）

- 前提：买区有效（`invalid=False`），`primary_lo~primary_hi` 已定；`recommend` 不分真假，`wait` 也挂。
- 挂单：次日开盘前，在 **买区上沿 `primary_hi`** 挂限价买单，数量 = **风险预算法**（见第三条）。
  - 挂上沿而不挂下沿：下沿常被刺一下就收回，等不到；上沿能吃到「开盘就已在买区内」的全部情形。
  - 开盘价已在买区内 → 以开盘价成交（更优），不浪费。
- **挂单有效期 = 5 个交易日，不是 1 天。** 回测 3124 个大阳样本：T+1 成交 63.4%，但等到 T+5 升到 **79.8%** —— 「当天没成交」里 **44.9%** 是 T+2~T+5 才回落。**唯一撤单理由是收盘跌破防守位**（结构作废），不是「挂了一天没成交」。
- 止损当天写死：结构 = 防守位**收盘**破；硬 = 防守位 −0.10×ATR，**次日盘中破即走**。
- 撤单条件：开盘跳空高于买区上沿 **+1.0×ATR**（已属追高）→ 撤，不追。
- 昨日 `wait` / 大阳作废 → 无预案单可挂，看第二条与第六条。**但若 `pre_breakout` 非空**，仍有一单可挂：按 `anchor` 区分——水平平台沿（`trigger` = 沿 +0.05×ATR）或**下移斜线**（`anchor=down_tl`，`trigger` = 下一根线值 +0.05×ATR），都在该位**上方**挂 **buy-stop**，止损 `hard_stop`。回踩单赌回落、突破单赌突破，**两者同时挂、先到先做**——SDGR 2026-09-14 只挂回踩单错过整段；ILMN 2026-09-11 挂斜线 buy-stop 210.13，9/15 开盘 208.95 跳过整条旗面线成交，至今 +16.7%；TEM 9/10–9/11 挂 62.92/62.18，9/14–15 成交，至今 +26% 以上。**无量不是不挂的理由**（价格说明一切）。

**买入上限闸门（禁止为了成交率抬高挂价）**

买区贴防守位（`floor+1.0×ATR`）的代价就是要求深回踩。想靠抬高挂价换成交率，先算上方还剩多少空间：

```
买入上限 P_max = (T1 + rr × hard) / (1 + rr)        rr 取 1.5
（由 (T1 − P) / (P − hard) ≥ rr 反解；T1 = 上方最近未破摆动高）
```

**任何挂单 / 追单不得高于 `P_max`**，`probe_intraday.py` 已直接输出。瑞达 002961（9/16 收 20.97／硬止损 19.02／T1 21.54）：

| 挂单价 | 距收盘 | 单股风险 | 到 T1 空间 | 盈亏比 |
|---|---|---|---|---|
| 19.46（+0.5×ATR） | −7.20% | 0.44 | 2.02 | 4.59:1 |
| **19.84（+1.0×ATR）** | −5.39% | 0.82 | 1.64 | **2.00:1** |
| 20.06（+1.3×ATR） | −4.34% | 1.04 | 1.42 | 1.37:1 |
| 20.21（+1.5×ATR） | −3.62% | 1.19 | 1.27 | 1.07:1 |
| 20.58（+2.0×ATR） | −1.86% | 1.56 | 0.90 | **0.58:1** |

→ 瑞达 `P_max = 20.03`。挂 20.58（「只跌 1.86% 的小调整」）盈亏比只剩 0.58:1 —— 赚 0.90 亏 1.56，不做。

**T+1 高开处置（9:25 竞价后可判）**

| 开盘 vs 基准日收盘 | 判断 | 动作 |
|---|---|---|
| ≤ +1% | 买区仍有效 | 预案单原样挂，等回踩 |
| +1% ~ +3% | 回踩概率下降 | 只挂不追，**挂价不得高于 `P_max`**；`P_max` 已被开盘越过 → 本笔放弃 |
| > +3% | 回踩单作废 | 撤单，只保留盘中量能突变（第四条），仓位降 1/5 |

**踏空的量化边界**（3124 个大阳样本，挂 `floor+1.0×ATR`，5 日窗）：

| T+1 开盘 | 样本 | T+1 成交 | 5 日内成交 | **永久踏空** | T+5 收盘 |
|---|---|---|---|---|---|
| 低开 >2% | 605 | 89.4% | 95.2% | 4.8% | −0.49% |
| 低开 0~2% | 964 | 75.6% | 87.8% | 12.2% | +1.97% |
| 高开 0~2% | 938 | 58.0% | 76.5% | 23.5% | +2.40% |
| 高开 2~4% | 344 | 36.6% | 63.7% | 36.3% | +4.26% |
| **高开 4~7%** | 198 | 15.2% | 51.5% | **48.5%** | +8.65% |
| **高开 >7%** | 75 | 13.3% | 44.0% | **56.0%** | +11.49% |

- **「连续高开高走一定踏空吗」→ 高开 >4% 时约一半永久踏空，>7% 时超过一半。**
- **涨停大阳（瑞达同类，1160 样本）更极端**：T+1 成交仅 **42.7%**、永久踏空 **32.4%**。
- 而真踏空那批，**91.7% 在 T+5 收盘仍高于大阳收盘** —— 不是「等到了更好的价」，是「再也没回来」。
- 结论：高开高走吃不到的部分，**是风险定价的必然结果，不是执行力问题**。能做的只有两条 ——
  ① 把挂单窗口从 1 天拉到 **5 天**（拿回 16.4 个百分点）；
  ② 承认剩下 20~32% 不属于本体系，**不追**。

### 一·B、突破预案单（setup 就绪 → 美股挂 stop 单 / A 股盯盘或条件单）

**「一」只覆盖一种形态：等价格回来。** 当强势票贴着前高收（setup 就绪）时，它必然失效，而且是双错 ——

> SDGR 2026-09-15 盘前，系统输出：模式 `wait`、买区 18.84~19.86、挂单 **19.80**、目标 **21.4**。
> 当日实际：开 20.78 → 最高 **23.71** → 收 23.25。
> ① 挂 19.80 距全天最低 20.34 还差 2.7% —— **永不成交**；
> ② 目标写 21.4，而 21.4 恰恰是当天被**突破**的位 —— 同一个价格，系统当阻力卖，实际是突破买点。

所以补一条反向路径：**不等人回头，挂条件单接突破。**

#### ⚠ 可预挂性：A 股没有 buy-stop（2026-09-18 老罗指出）

本引擎原先把突破触发价也写成「挂单」，在美股成立（券商收 stop 买单），**搬到 A 股就是不可执行的方案**——A 股券商只有限价单与市价单，同花顺之类的「条件单」是软件端辅助工具（需软件在线 + 券商支持），不是交易所原生能力。

**铁律：写买卖价之前，先判这张单能不能在开盘前挂好然后去睡觉。**

| 买点位置 | 限价单行为 | 能否预挂 | 只能用 |
|---|---|---|---|
| 有部分落在现价**下方** | 挂低价 = 等回落，自动成交 | **✓ 可预挂** | 限价单（隔夜有效） |
| 整体在现价**上方** | 挂高价 = 立即撮合（市价+价格上限） | **✗ 不可预挂** | 同花顺条件单 / 盘中盯价 / 尾盘 14:57 判定 |

`probe_intraday.py` 已输出 `可预挂 :` 一行（函数 `prehang_verdict`）。

**判据必须基于买区，不能基于引擎打印的上沿挂价。** 反例（康龙化成 300759，2026-09-17）：
买区 42.33–44.61、现价 43.75 已在买区内，引擎打印的挂价是上沿 **44.61 > 现价**；若只看这一个数就会误判「不可预挂」——但买区下沿 42.33 远在现价之下，在 **42.22~43.75** 之间挂限价单照样隔夜等回落，完全可挂。所以 `prehang_verdict(c0, 下沿, 上沿)` 看的是区间、不是单点。

**推论（选标的时直接用）**：同一板块里，「回踩买」天然享执行优势（可预挂），「突破买」必须有人在盘前。无法盯盘时，优先做买点在下方的那只。

**判定（只用日线，盘前可算）**：取 `key_break_level` 给出的 K（上方 0.2–1.5×ATR 内最近的突破位），按「基准日收盘距 K 的距离」分两档：

| 档 | 条件 | 处置 |
|---|---|---|
| **normal** | 距 ≤ 门槛×ATR（**A 股 0.50** / 美股 0.80） | 可挂，给全套数字 |
| **far** | 有 K 但超过门槛 | **只报位置，不给挂单价**（不挂 = 不追） |

> **strong 分级已于 2026-09-17 删除（老罗决定）。** 原来的 strong 档要求「距 ≤0.80×ATR **且** 基准日实体 ≥1.0×ATR、量 ≥1.5×前 20 日均量」，但在全样本 121 条 A 股突破单里**一次都没触发过**（`BO_NEAR_STRONG` 四个取值结果一字不差）—— 分级没有产生任何信息，只增加了解释成本。
> 删掉分级后两轨门槛**不同**：A 股 0.50（有 T+1 隔夜风险，只接贴得极近的），美股 0.80（T+0 可当日出，容许远一点，SDGR 原型距 0.64×ATR 靠它保住）。实测把 A 股门槛也放到 0.80，会把 0.50–0.80 那一带整片放进来，而那一带是**负贡献**（净账户从 +0.029%/笔 稀释到 +0.008%/笔）。

**给什么数字**：

- 触发 = `K + 0.10×ATR`（**美股**：stop 买单；**A 股**：无 buy-stop，只能条件单或盘中盯价 —— 见上「可预挂性」）；止损 = `K − 0.30×ATR`（**成交后才生效**）
- 目标 = 近 **60 根**内 K 上方最近阻力；找不到就用 `K + 2.0×ATR`
- 股数按风险预算法（A 股 `lots_for`、美股 `us_lots`）
- A 股额外两道：触发价**距涨停 <1.5% 放弃**；**T+1 → 止损是次日口径**

**开盘撤单（必须执行）**：

| 情形 | 动作 |
|---|---|
| 开盘价 ≥ 触发 + 0.60×ATR | 跳空太大，条件单会以开盘价成交 → **撤**，改等回踩 |
| 开盘价 ≤ K − 1.0×ATR | 结构已坏 → **撤** |
| 其余 | 有效，等它站上触发价 |

**未触发前跌破止损位不影响挂单** —— 止损只在成交后生效。

**实测（4 个信号，2026-09-15 / 16）**：

| 标的 | 日期 | K | 触发 | 止损 | 最大亏 | 当日结果 | R |
|---|---|---|---|---|---|---|---|
| SDGR | 9/15 | 21.40 | **21.50** | 21.09 | $48 | 收 23.25 → +$203 | **4.27R** |
| ILMN | 9/15 | 212.18 | **213.02** | 209.65 | $37 | 收 222.29 → +$102 | **2.75R** |
| 002961 | 9/16 | 19.27 | **19.34** | 19.07 | 54 元 | 封板 20.97 → +316 元 | **4.94R** |
| 603228 | 9/16 | 106.01 | 106.70 | 103.94 | 276 元 | 收 106.41 → **−52 元** | −0.17R |

- ILMN **9/16 盘前正确地不给信号**（K = 231.81 距昨收 1.04×ATR，落 far 档）—— 那天追 235 本就是错的。
- 002961 **10:10 触发 19.39**，比旧「量能突变」通道（10:20 挂 19.50）早 10 分钟、便宜 0.11 —— 而 10:20 那根爆量 808 万股直接封板。
- 603228 是温和失败样本（触发后回落，收 −0.49%，**未触及止损**）—— 追突破的固有成本，不是 bug。

**待验证**：三档阈值（0.80 / 0.50 / 实体 1.0 / 量 1.5）目前只有 **4 个样本**支撑，未回测。

### 二、盘中主动试仓（三条通道 · A 股 / 美股双轨）

盘中主动出手只在下面三条通道上开口，且都要**更小仓位、更近止损**。

三条通道的分工，是「不踏空」的全部秘密 —— 它们覆盖**不同的启动形态**：

| 通道 | 覆盖形态 | 发令枪 |
|---|---|---|
| A 量能突变 | 横盘后**一根爆量的首爆** | 分钟量能（不看价格） |
| B 关键位突破 | **开盘即启动** / 跳空后继续走 | 5 分钟收盘站上 K |
| C 分钟回踩确认 | 已启动、**等了日线回调却不来** | 5 分钟级回踩不破 |

> **为什么必须有 C**：预案单和次级观察位要的是**日线级回调**，而强势股只给**分钟级回调**。把观察粒度从日线降到 5 分钟，才是「临盘抓大涨」的正解 —— 这不是放宽纪律，是**换一个更快的时钟**。

#### 双轨参数（唯一的分歧点：当天能不能纠错）

| 维度 | A 股（T+1） | 美股（T+0） |
|---|---|---|
| 通道 B 追高容忍 | K + **0.60×ATR** | K + **1.20×ATR** |
| 时间窗 | 开盘后 **≥15 分钟**；**无收盘前限制**（挂单只看价不看钟） | 开盘后 **≥10 分钟**；无收盘前限制 |
| 止损语义 | **次日**口径（今天买今天卖不掉） | **当日**口径，破位即走 |
| 仓位 | **同为风险预算法**（不再用「计划仓 1/3」这种与风险无关的比例），单笔上限 = **绝对额 5 万**（30% 百分比闸门已移除）；按申报单位取整（主板/创业板 100 的整数倍、科创板 200 起 1 股递增） | 风险预算法，上限 = **账户全额**（无额外比例闸门） |
| 专属过滤 | **距涨停 <1.5% → 不买** | 无涨跌停，无此约束 |

**一句话**：A 股当天纠不了错，所以只能小追；美股破位就能走，可以多追一档，但止损必须当场执行。

#### 通道 A：量能突变确认

**发令枪是分钟级量能，不是价格。** 强势启动往往在一根 5 分钟里完成——瑞达 9/16：10:20 单根 5 分钟量 **15.5 倍**于开盘基准、占全天成交量 **43%**，价 19.50→20.40，5 分钟后即封板。等收盘确认，价已经在板上。

四条**同时**成立才可试仓，缺一条只观察：

| # | 条件 | 量化 |
|---|---|---|
| 1 | 时间窗 | **≥10:00**；开盘 30 分钟内一律不许（区间未走完、分位失真） |
| 2 | 量能突变 | 当根 5 分钟量 ≥ **前 5 根（=25 分钟）均量 × 2.0** |
| 3 | 蓄势位置 | 启动前当日振幅 **≤2.5%**，且价在**近 5–10 日箱体下沿 / 前低上方 ≤1.0×ATR**（是启动，不是半山腰接力） |
| 4 | 未追高 | 触发价 ≤ **当日开盘价 + 1.0×ATR**；超过即追在半山腰，放弃，改挂回踩单 |

> **量能基准为什么改成「前 5 根均量」（老罗 2026-09-17 改的定义）。**
> 旧口径是「≥ 当日此前**最大** 5 分钟量的 2.5 倍」。它在这类票上**永远不可能触发**：开盘第一根往往就是全天最大量，基准被顶到天上，之后任何量都只是零头。全样本实测（43 只 × 21 个交易日 = 903 个标的日）旧定义只触发 **3 次（0.33%）**，且放宽倍数救不回来（2.0→9 条、1.5→24 条，成绩反而更差）—— 根因不是倍数，是基准选错了。
> 换成「前 5 根均量」后基准随行情漂移，与「开盘那根最大量」解耦；同样 2.0 倍门槛下触发率回到可用区间（`BURST_MA_BARS` 取 3/5/10 结果接近，属弱参数）。
>
> **美股：收盘竞价根不参与判定（2026-09-18 修，`US_BURST_DROP_TAIL = 1`）。** 北京 04:00 = 美东 16:00 那根是**收盘集合竞价**，量属制度化巨量（占全天 8–15%），与「盘中启动」毫无关系；且它永远是序列最后一根，`fresh` 恒为真 → **每只美股都会在收盘凭空多出一次「✅ 允许先手」**。实测 2026-09-17 三只同时在同一根触发：MU **19.1×**、SNDK **10.1×**、HPE **14.2×** —— 三只不同股票在同一天同一根 K 上同时「启动」，触发的只能是市场制度节奏，不可能是个股信号。改法：`vol_bursts(mins, drop_tail=1)`，基准窗口不受影响，`fresh` 改用 `n_eff`。
> ⚠ **遗留（老罗 2026-09-18 决定：暂不补，保持现状）**：剔除收盘竞价后，三只信号**全部落在 03:55**（收盘前最后一根可交易根）—— 修后 MU **2.9×**、SNDK **2.6×**，勉强越过 2.0 门槛。美股尾盘 15:50–16:00 量本就系统性放大，2–3 倍于常规根属常态，故第三通道**仍对「尾盘」系统性敏感**；且 03:55 入场距收盘仅 5 分钟，T+0 也来不及管理。候选改法是与 `US_BURST_OFFSET = 30`（开盘后 30 分钟不试）**对称的尾部时间窗**，本轮不实施；若日后回测显示尾盘假信号显著拖累胜率，再评估。

- 四条全满足 → 按**风险预算法**定量（见第三条；在爆量那根的价上**挂单**，不用市价扫）。
- 条件 2 成立但价已超条件 4 → **不买**，改为在回踩位挂单。
- 加分（不强制）：同板块多数红盘、指数同步向上。

#### 通道 B：关键位突破（盘前定 K，盘中站上即触发）

**解决的是「开盘即启动」这一类。** 反例即证据（ILMN，2026-09-15）：从整理上沿起爆，开 208.95 → 收 222.29（+6.76%），次日跳空到 235 附近。当天两条老通道都赶不上——

- 通道 A 旧口径要求「≥当日此前最大量的 2.5 倍」，而这类票**开盘第一根往往就是全天最大量**，基准被顶死后通道 A 再也不触发（已于 2026-09-17 改成「前 5 根均量」，见通道 A 一节）；
- 预案单依赖**收盘**判定的门控，反转态下买区被整段丢弃、要到次日才放宽，而次日已经跳空。

**盘前定 K**（只用日线，不看当日），取「上方最近的可突破位」，两者中取**较低**者：

| 候选 | 定义 | 为什么需要它 |
|---|---|---|
| 近期整理上沿 | `bars[-4:-1]` 的最高价 | ILMN 9/15 突破的正是 9/9 高点 **212.18**；它左侧更高（9/8 的 219.45），进不了 `pivots` |
| 摆动前高 | `pivots` 上方最近的高点 | 没有近期整理时的兜底 |

过滤：`K − 昨收` 必须落在 **0.2–1.5×ATR**。太近没有突破意义，太远当日不可及。

> **最关键的约束**：候选一只取 `bars[-4:-1]`，天然 **≥2 根之前**形成。这一条排除了「昨天刚创的高点」，否则通道会滑成追加速段。实测 ILMN **9/16 因此不出信号**（9/15 高点 226.76 是最后一根，进不了候选）。

**盘中触发**（5 分钟，四条同时成立）：

| # | 条件 | 量化 |
|---|---|---|
| 1 | 时间窗 | 开盘后 **≥10 分钟**（集合竞价噪音未过，不许触发） |
| 2 | 站上 K | 5 分钟收盘 > **K + 0.10×ATR** |
| 3 | 不追高 | 触发价 ≤ **上限**（见下方「锚的选择」；A 股 K+0.60ATR / 美股 K+1.20ATR），超过即放弃 |
| 4 | 新鲜度 | 触发须在**最近 2 根**内；已过去更多根 = 信号过期，不追 |
| 5 | 离涨停（A 股专有） | 触发价距**涨停价**必须 **≥1.5%** —— 贴板买不到，买到即开板 |

**追高上限的锚必须是 K，不能是开盘价**（2026-09-16 修正）：

- 开盘 **≤ K**（尚未跳空越过）→ 上限 = **K + D_max×ATR**，D_max：A 股 0.60 / 美股 1.20
- 开盘 **> K**（盘前已完成突破）→ 上限 = **开盘 + 0.60×ATR**（此刻追风险最高，收紧）

旧口径 `min(K+0.60ATR, 开盘+0.80ATR)` 有致命缺陷：**当跳空不足、而突破位较远（开盘 ≪ K）时，第二项会把上限压到 K 以下** —— 上限比突破位本身还低，触发价必然越线，通道就永远不可能成交。ILMN 9/16 实测：K = 231.81、开盘 223.70，`min(237.32, 231.05) = 231.05 < K` → 09:40 收 234.94 被判「追高放弃」。**这是「策略永远抓不住大涨」的机械原因，跟判断力无关。** 换成 K 锚之后，同一天上限 = **242.83**，21:40 触发 → 试仓 234.94 / 止损 229.06 / 10 股 / 最大亏 $59。

- 止损 **K − 0.30×ATR**（跌回 K 下方 = 突破失败）。
- 目标：K 上方最近的 `pivots` 高点；没有则用测量目标 `K + (K − 启动前低)`。
- 触发根量能「≥当日此前均量 × 1.5」是**加分项、不是硬门槛**——ILMN 9/15 触发根只有 0.8 倍，硬卡会漏掉它。**该系数目前只有 1 个样本支撑，属待验证参数。**

**ILMN 9/15 实测**（`--date 2026-09-15` 回放）：K = 212.18（9/9 整理上沿，距昨收 0.47×ATR）；**09:40 收 213.99 触发** → 止损 209.65，风险 4.34/股（**2.03%**），目标 221.76，盈亏比 **1.79:1**，11 股 = $2,354。当日收 222.29（+3.9%），次日现价 232.63（**+8.7%**）。同一天通道 A 要等到 **10:05、可挂价 214.75** —— 晚 25 分钟、贵 0.76。

**通道 B 在 9/16 的表现（上限换锚后）**：K = 231.81（8/27 摆动前高），上限 = **242.83**（K + 1.20×ATR）→ 21:40 收 234.94 **触发**，试仓 234.94 / 止损 229.06 / 10 股 / 最大亏 $59。

> **诚实交代**：当天随后回落到 229.5 —— 买了是**浮亏**。这说明「能不能追」和「该不该追」是两件事：通道只负责给出**可执行的价格**，值不值得下注要看形态与量能（该触发根量能只有当日均量的 0.8 倍，属偏弱）。**别把放行当成推荐。**

#### 通道 C：分钟级回踩确认（强势票唯一给得起的入场点）

**这是「等回调」这句话的正确版本。** 你已经看到启动，但不追第一根 —— 等它**回踩 5 分钟结构**：

| # | 条件 | 量化 |
|---|---|---|
| 1 | 已有启动 | 当日通道 A 或 B **曾放行**。**不要求 A/B 当前新鲜** —— C 专为「错过了第一根」而设，若要求 B 新鲜，它就永远只在能买 B 的时候才出现，等于没用 |
| 2 | 回踩不破 | **相对前一根下跌**的 5 分钟根，收盘落回 **S ± 0.30×ATR**，且**不破 S − 0.30×ATR**（跌破 = 突破失败，当日作废） |
| 3 | 未爆量 | 回踩段各根量 < **max(启动根量, 当日此前均量) × 2.0** |
| 4 | 企稳 | 回踩后出现一根 5 分钟**收阳**且**低点高于前一根低点** |
| 5 | 新鲜度 | 企稳根须在**最近 2 根**内；更早 = 过期，不追 |

- 入场 = 企稳那根的**收盘价**；止损 = **回踩低点 − 0.10×ATR**；目标同通道 B。
- A 股同样受「距涨停 ≥1.5%」限制；**不受收盘时间限制** —— 挂单/回踩只看价不看钟，时间不是否决理由。美股 T+0，破位即走。
- **条件的两个坑（2026-09-16 实测修正）**：
  - 条件 2 的「回踩」**必须是相对前一根下跌**。只判「收盘 ≤ S+0.30ATR」会把「继续上涨但仍在带内」的根当成回踩 —— ILMN 9/15 21:45 正是这种根（收 213.51、量 34,043），旧逻辑判成「回踩段放量 = 出货」，属假信号。
  - 条件 3 的基数**不能只用启动根量**。通道 B 的启动根可能本身就是低量根（ILMN 9/15 为 19,819 < 当日均量 23,396），拿它当分母，任何正常回踩都会「超标」。取两者较大者，阈值 2.0 与通道 A 的 2.5 倍爆量门槛同量级。
- **不成立的常见情形**：一路不回头（ILMN 9/16 09:30→09:45 直冲 236，全程无回踩）→ 这条也不给信号，**那就不做**。机会成本是策略的固有成本，不是失误 —— 3124 个大阳样本里永久踏空率 **20.2%**，这个数字必须被接受。

**ILMN 9/15 实测**（`--date 2026-09-15 --until 21:50` 回放）：通道 B 于 21:40 触发（收 213.99、止损 209.65、风险 **4.34/股**）。通道 C 在 21:45 等到回踩（收 213.51、低 213.01、未爆量），21:50 收阳 213.60 且低点抬高 → **入场 213.60、止损 212.17、风险 1.43/股（0.67%）**。

| | 入场 | 止损 | 风险/股 | 当日收盘 222.29 |
|---|---|---|---|---|
| 通道 B | 213.99 | 209.65 | 4.34 | +8.30 = **1.91R** |
| 通道 C | **213.60** | **212.17** | **1.43** | +8.69 = **6.08R** |

同一个启动、同一天：**风险降低 67%，R 倍数高出 3 倍**。这就是「换个更快的时钟」的全部价值。

**三条通道的优先级：C > A > B。**
理由：C 的**风险最小**（止损贴着回踩低点，1.43 vs 4.34）；A 靠量能确认（次之）；B 天然要追一段（最贵、止损最远）。**C 出现时，不要因为「B 已经触发过」就放弃 C。**

> 注意 C 的入场价**未必**低于 B —— 它是「回踩后企稳根」的收盘，回踩极浅时可能反而略贵（A 股 002961 那种一路不回头的就直接判 pending）。**比较要看风险/股，不是看价格。**


### 三、仓位定量（A 股 / 美股同构：风险预算法）

**一条公式，两轨共用：**

```
股数 = min( 账户 × 1.5% / (入场 − 硬止损) ,  仓位天花板 / 入场 )
```

- 再用**最小申报单位**向下取整：主板 / 创业板 = **100 股或其整数倍**（深交所交易规则 3.3.8）；科创板 688/689 = **不小于 200 股，超出部分按 1 股递增**（如 201、202 股合法，上交所科创板特别规定）。美股 1 股。
- 仓位天花板：A 股 **单笔绝对额 5 万**（`ASH_SINGLE_ABS`，即 `min(账户, 50000)`）；美股 **账户全额**（默认 $5000，无额外比例闸门）。
  **旧的「账户 × 30%」百分比闸门已于 2026-09-17 移除。** 代价必须记牢：风险预算法的股数与
  每股风险成反比，失去百分比兜底后**止损越紧仓位越重** —— 0.46×ATR 的止损可吃掉约 88% 账户，
  跌停隔夜损失从 −2.9% 放大到 −8.8%。窄止损单从此**不再自带降杠杆效果**，必须自己控。
- 算不出 1 手（1 手即超硬顶）→ **明确说「不做」**，并说明是「钱不够」这个硬约束 —— 不是拍脑袋的软规则。这样才能保证**「点位到了买不到」只可能因为硬约束**。
- 旧口径「计划仓的 1/3」**已废弃**：它与风险无关，且不传 `--qty` 就算不出股数。同一笔单，止损越近买得越多、越远买得越少。
- **A 股为什么也适用**：风险预算法让**单笔最大亏损恒定 ≈ 账户 1.5%**，与股价高低、仓位百分比无关。
- **结构性门槛（下单前先筛 · 单笔硬顶 5 万）**：

  | 板块 | 最小申报 | 门槛价 = 单笔硬顶 ÷ 单位 | 超过的后果 |
  |---|---|---|---|
  | 主板 60/00、创业板 300/301 | 100 股 | **500 元**（旧 150） | 第一手 > 50,000 元 = 超单笔硬顶 → 买不了 |
  | 科创板 688/689 | 200 股（超出按 1 股递增） | **250 元**（旧 75） | 第一手 > 50,000 元 = 超单笔硬顶 → 买不了 |

  比门槛高的票，问题不在图形、不在时机、不在情绪 —— 在**「最小申报单位 × 股价」**。
  这类票要在**下单前**就排除，不要给了买区再告诉人买不了。程序算不出 1 手时会直接打
  **「结构性不可交易」**并给出该票门槛价（`ash_price_ceiling`）。
- 注意 A 股的物理约束仍可能否决交易：高价科创板股 1 手（200 股）就可能占账户 70%+，此时**不是规则不让买，是买不了** —— 两者必须分清。
- **窄止损时谁在约束（5 万账户实测，`calc_ash_position.py`；30% 闸门移除后重算）**：

  | 止损距离 | 20 元主板 | 100 元主板 | 实际风险占账户 | 谁在约束 |
  |---|---|---|---|---|
  | 1.2% | 2500 股 / **50,000 元** | 500 股 / **50,000 元** | 1.20% / 1.20% | **单笔硬顶** |
  | 3.0% | 1200 股 / 24,000 元 | 200 股 / 20,000 元 | 1.44% / 1.20% | 风险预算 |
  | 5.0% | 700 股 / 14,000 元 | 100 股 / 10,000 元 | 1.40% / 1.00% | 风险预算 |
  | 8.0% | 400 股 / 8,000 元 | 100 股 / 10,000 元 | 1.28% / **1.60%** | 风险预算 |

  结论（**与 30% 闸门时代相反**）：窄止损单不再自带降杠杆效果 —— 1.2% 止损直接顶满
  5 万单笔硬顶、吃掉 100% 账户。风险金额仍受 1.5% 预算约束，但**集中度不再有兜底**，
  跌停隔夜损失可达账户 −10%（旧闸门下最多 −3%）。

- **⚠ 最小申报单位会击穿 1.5% 风险预算**：`ash_lots` 在「风险预算算出不足 1 手」时会退回
  **给 1 手**（否则钱少的账户永远建不了仓）。旧 30% 闸门会把这类票判成「结构性不可交易」
  挡掉；硬顶抬到 5 万后它们全部放行，而 1 手的风险可能远超预算：

  | 标的 | 止损 | 股数 | 金额 | 实际风险 | 占预算 |
  |---|---|---|---|---|---|
  | 200 元科创板（200 股单位） | 5% | 200 | 40,000 | 2,000 元 | **267%** |
  | 200 元科创板（200 股单位） | 8% | 200 | 40,000 | 3,200 元 = 账户 6.4% | **427%** |

  **高价科创板（>100 元）配宽止损时必须手工核一遍单笔亏损，程序不会拦。**

### 三·B、组合层总仓位闸门（单笔之上再叠一层）

上面那条公式只管**一笔**。多笔叠加以前无人管（4 笔各 28% = 112%）。老罗 2026-09-17 定了口径：

| 项 | 值 | 说明 |
|---|---|---|
| 总资金 | **100,000** | 主力 5 万 + 备用 5 万 |
| 主力额度 | **50,000** | 常规建仓只用它 |
| 备用额度 | **50,000** | 只在主力层已满、**且信号 = 突破预案单**时才动（全样本里唯一净账户为正的那条） |
| 单笔绝对额 | **≤50,000** | 硬顶（老罗：「单笔最好不要过五万」） |

- **单笔上限 = 绝对额 5 万**（30% 闸门已移除 → 可交易价上限抬高：主板/创业板 500 元、科创板 250 元）。
- 于是 **主力层最多同时持有 3 笔**（50,000 ÷ 15,000）。全样本实测：552 条候选里只有 **13.4% 拿得到钱**，**86.6% 被额度挡掉** —— 真实约束是额度，不是信号不够。
- 剩余额度买不到 1 手 → 明确说**是「额度」不够**，不要说成「规则不让买」（`ash_portfolio_gate` 返回的 `reason` 会区分这两件事）。

盘中买入**当天不能卖**，止损次日开盘才生效 —— 这一点只影响**止损口径**，**不影响入场时机**：不再有「14:30 后不许新开仓」这条线。

### 四、硬红线（违反即不执行）

- 开盘 30 分钟内试仓（A 股通道 A/B 一律 ≥15 分钟且 ≥10:00）。
- **A 股距涨停 <1.5% 时买入**（贴板买不到，买到的多半是开板出货）。
- 涨停 / 一字板排队买（封死买不到，买到的是接盘）。已封板 → 不参与；打板是另一套体系（情绪周期 / 封单 / 竞价），本 skill 不覆盖，不许用它给打板找理由。
- 现价已出买区上沿仍市价买（唯一合法动作是挂回踩单，或走盘中三条通道）。
- 当日已有一笔止损 → 当日不再试仓。
- 同一标的当日试仓超过 1 次（「分批」会滑成加仓追高）。

> **已删除的旧规则：「A 股 14:30 之后不许新开仓」**（2026-09-16 老罗否掉）。
> 挂单/回踩只看价不看钟 —— 点位到了却因 14:30 被拒，等于白等。尾盘成交确实隔夜 T+1，程序会打一行提示，但**不否决**。

### 四·B、挂单前最后一步：量价研判（防买在派发阶段）

结构 + 位置成立 ≠ 该买。挂单前还要看**近 20 个交易日**的筹码是收集还是派发（`probe_intraday.py` 判买时自动输出，A 股第四节 / 美股第五节）：

- **涨跌量比**（阳线日均量 / 阴线日均量）：≥1.30 → 涨放跌缩，收集特征；<0.80 → 跌放涨缩，⚠ 派发特征。
- **量能趋势**（近 5 日均量 / 前 15 日均量）<1 = 回调缩量；**末根缩量刺破 MA5 收回** = 典型洗盘。
- **证伪位 = 窗口最低价**：放量（≥1.5×窗口均量）**收盘**破 = 派发确认，预案单作废。
- ⚠ 派发特征**只提示不否决**（能否决交易的只有硬约束）——但此时挂单属于逆着资金流向接货，仓位应再收一档或等缩量企稳。

### 五、主模式作废 / `wait` 时的次级观察位

大阳作废、`mode=wait` **不等于这只票没机会**。往前找最近的**结构支撑**（前低、箱体下沿、MA20）当次级观察位：

- 条件：价格回踩该位**收盘不破** + 量缩到**近期最低** → 次日该位上方 **1.0×ATR** 内为小仓试错区。
- 反例即证据（瑞达 002961，9/16）：9/15 收盘主模式作废，系统只给 `wait`；但 9/14 低 19.00、9/15 低 18.99，缩量贴支撑（量 379 万为近期最低）。9/16 开 19.12 正好落在「19.00 上方 1.0×ATR」的试错区里，收盘 20.97 —— **+9.7%**。按「等收盘确认」操作，这一天什么也吃不到。
- 该观察位**不进六种买法**，只作先手 / 试错依据；仓位同第三条，必须写明失效条件（收盘破该支撑即作废）。

### 六、美股口径（T+0 / 无涨跌停 / 北京 21:30–04:00）

美股**结构判定完全同构**（同一套六种买法、同一套买区与止损算法），差异只在**仓位、止损触发与时段**——A 股那三条结论在美股**不成立**：

| 维度 | A 股 | 美股 |
|---|---|---|
| 交收 | T+1，当天买不能卖 | **T+0**，盘中破位即可出，不必留到次日 |
| 涨跌幅 | ±10% 封顶 | **无涨跌停** → 单日缺口不受限，跌幅无法给风险封顶 |
| 仓位定量 | **同一条风险预算法**：`min(账户 × 1.5% / (入场 − 硬止损), min(账户, 50000) / 入场)`，按申报单位取整（`ash_round_qty`） | 同式，上限 = 账户全额，取整到 1 股 |
| 最小申报 | 主板/创业板 100 股整数倍、科创板 200 股起后按 1 股递增 | **1 股**（无整手约束） |
| T+1 止损口径 | 日内通道单：**日线结构位（信号日最低价）+ 收盘破才算破**，持有期仍只到次日 | 不适用（T+0 当日了结，盘中破即走） |
| 时段（北京时间） | 09:30–15:00 | **盘中 21:30–04:00；盘前 16:00–21:30** |

> **T+1 止损为什么换口径（老罗 2026-09-17 决定，选项 a）。** 旧口径把「当日日内止损」（距入场只有 1.4%–1.6%）原样搬到次日，隔夜跳空 + 次日噪音必然把它扫掉 —— 全样本四条通道的 T+1 净账户**全负**。
> 换成「日线结构位 + 收盘破」后**每条通道都变好了一点，但全部仍为负**（预案单 −0.131%→−0.108%/笔、通道A −0.038%→−0.031%、通道B −0.181%→−0.172%、通道C −0.152%→−0.151%）。改善量级只有 **0.001%–0.023%/笔**，远不足以转正。
> 顺带否掉了一个更激进的版本：既然止损是日线级的，持有期也放到日线级（**持有到结构破、最长 5 日**）—— 实测**更差**（通道B −0.274%、通道C −0.276%、突破预案单 −0.229%）。原因是结构位太宽，收盘很少破得到，绝大多数单子退化成「买入后持有 5 天到期平」，而这批信号的 5 日漂移为负。
> **结论：A 股 T+1 的问题是结构性的，出场口径修不动它。** 别在这条路上继续加参数。

- **盘前通道（美股独有）**：北京 16:00–21:30 正好是白天，这是美股唯一「人醒着」的时段。盘前价落在买区内 → 开盘前挂买区上沿即可；高于买入上限 → 回踩单作废；盘前 >3% → 直接作废（避接盘）。**盘前价 == 昨收 = 该时段无成交，按「无信号」处理，不要当利好。**
- **核心风险在教育**：A 股的风险被涨跌停「物理限制」住了，美股没有。所以**风险预算法是两轨共用的主约束**（A 股另加单笔 5 万绝对额兜底），单笔最大亏损恒定 ≈ 账户 1.5%，与股价高低、仓位百分比无关。
- **蓄势门槛按波动率归一化**：A 股用固定 2.5%（启动前振幅），美股用 `max(2.5%, 1.0×ATR/H)`——美股 ATR/H 普遍 3–5%（ILMN 4.13%、MDB 5.7%），固定 2.5% 会系统性偏严（实测 ILMN 9/15 启动前 3.41% = 0.83×ATR 被误拦）。**该系数只有 1 个样本支撑，属待验证参数。**
- **数据源**：日线/盘前/市场状态走 Nasdaq 官方（`marketStatus` 自带 `Pre-Market`/`Open`/`Closed`，夏令时不必自算）；**分钟级带量 K 线**先走东财 http，被限流时退 Yahoo（需本地代理，`WB_US_PROXY` 可指定端口）。
- 命令：`python probe_intraday.py ILMN --us-account 5000`（非 6 位代码自动按美股处理）。
- **代码格式（必读）**：美股传**裸 ticker**（`MU` / `ILMN` / `SNDK`），**不要带行情源前缀** —— `usMU` 这种写在 westock 命令里、直接喂给本引擎会打到 Nasdaq/Yahoo 上 404，报 `全部数据源失败`。A 股直接传 6 位数字（`601233`、`688758`）。

---

## 硬止损怎么定

结构止损管假跌破，硬止损管中大阴/跳空。计划卡两档都要有价。入场前必须写死：**结构锚** + **硬止损锚（三选一）** + **硬止损价**；破了禁止换锚、禁止改宽。

**结构止损**：收盘破才走。盘中刺穿、尾盘收回，不走。沿线回踩的结构锚是**回踩的那条线本身**（2026-09-18 修正，见下「铁律零」）。

**硬止损锚（三选一，入场前选定）**：

| 锚 | 怎么取 | 默认用于 |
|---|---|---|
| **MA5** | 计划当日的 MA5 价（写死数字；之后均线挪了也不改硬止损） | 沿线回踩 |
| **阳线下沿** | 入场相关那根阳线最低价（突破阳 / 回踩所依大阳） | 普通阳线突破、大阳后回踩 |
| **大阳线中点** | **(最高价+最低价)/2** | 很长的大阳（实体或振幅明显偏大，约 ≥0.8×ATR 或涨幅 ≥3%）；**平台/W底/旗形突破单若突破日是这种长阳，也用中点**，不用沿下贴毛刺 |

**硬止损定价（必须有 gap）**：

- 硬止损 = 选定锚再下 **0.10–0.15×ATR**，默认 **0.10**。**禁止把硬止损写成锚本身**（贴锚会被探底针扫掉）。
- gap 不是另找更深支撑，也不是按成本改宽。
- **锚名与数值必须绑定**：计划卡写出的锚名（MA5 / 阳线下沿 / 大阳中点 / 缺口下沿）必须真能算出那个价 —— `硬止损 = 该锚价 − 0.10×ATR`。**禁止为迁就买区把数值悄悄下移、却保留锚名**：那会印出「锚=大阳中点」配一个离中点好几个 ATR 的价，比没有锚更危险。
- **锚与买区冲突时，让路的是买区**：若 SKILL 的合法锚算出的硬止损都落在买区下沿之上（成交即已破止损），就把**买区下沿抬到硬止损之上**并标 `stop_warning`，可执行价位以 `primary_lo` 为准。不许改锚名、不许下移止损数值、不许靠「再等等」绕过。
- **该修正必须落到报告里**：`stop_plan` 靠修改传进来的 `z` 来完成上抬。若调用方只传 `dict(z)` 副本，修正会被吃掉 —— 报告仍印「买区未上抬 + 硬止损落在买区之内」，并据此打出虚高盈亏比。实测（601233 2026-09-01）：挂单 26.89、硬止损 26.84 落在买区 25.52–26.89 **之内**，报告写成 **24.20:1**，修好后是 **1.52:1**。所以：修正必须**回写真实 `z`**，且 `room_and_cap` 要在打印买区**之前**调用。
- 例：锚=阳线下沿 174.84、ATR≈8.8 → 硬止损 174.84−0.10×ATR≈174.0。长阳高 22.45 / 低 20.90 → 中点 21.675，ATR 1.42 → 硬止损 ≈21.53（比「沿 22.10−0.10×ATR」宽，抗洗）。

**铁律零：任何止损锚都必须低于现价（2026-09-18 SNDK）**

锚价 ≥ 现价 = 买入的那一刻就已经在止损之下（**买入即止损**）。这不是"宽一点/紧一点"的问题，是这个锚根本不可用 —— 必须先剔除，再谈上面那些取舍；剔除后无处可去的如实不给止损，**绝不靠抬买区去迁就**。

- 触发场景：**价格急跌、均线滞后**。SNDK 2026-09-14~09-16 从 1764 一路跌到 1519，MA5 依次还在 1676 / 1634 / 1586，全都**高过当日收盘**。旧代码在 `anchor=hl_trendline` 时把结构止损换成 MA5，于是「收盘破 MA5」在买入那一刻就已成立；`stop_plan` 接着按铁律二把买区抬到硬止损之上，输出的买区 **1580.16–1604.72 整个在收盘 1519.97 之上**，而 `recommend` 仍是 True；结构止损 1585.76 还高过次日开盘 1564.53。
- **沿线回踩的结构止损 = 回踩的那条线本身**（回踩单的定义就是"回踩到这条线买、收盘破这条线走"），不再用滞后的 MA5 顶替；MA5 只在该线已不可用（价格跌到线下）时作备选，且同样须过铁律零。
- **出口总闸门**：`attach_stops_targets` 在给出结论前再拦一道 —— `recommend=True` 必须自带一个真正位于现价之下的止损，否则撤销整单并标 `stop_above_price`、verdict 写「止损锚在现价之上·买入即止损·不接」。同类错误已出现过三次（INTC 昨收口径、赛分盈亏比压穿突破位、BE 陡线外推），共同点是**锚点逻辑正确 ≠ 锚点数值可用**，单点补锚总会漏下一个，所以必须有总闸门。
- **沿线回踩的买区**：下沿 = 线 −0.05×ATR（只留毛刺），上沿 = 线 +1.0×ATR（与贴线门槛同源）。旧版是「线 ±1.0×ATR」双侧对称，下沿落在**破线之后**的区域，且必然低于任何「锚 −0.10×ATR」的硬止损 → 每次都触发铁律二的抬买区分支，上面的荒谬买区就是这么来的。

**硬止损必须在噪声带之外（2026-09-18，只对突破类成立）**：

硬止损距**买区下沿**不足 **0.25×ATR** 时，等于把止损塞进单日噪声带 —— 名义上有止损，实际一次正常波动就被扫。此时若同族还有**更宽的合法锚**（大阳中点 → 阳线下沿），降级到更宽那条并在 `hard_note` 里留痕；同族没有更宽锚时**不许硬凑宽度**，但必须标 `hard_noise=true` 如实提示。

**口径只适用突破类**（`BREAKOUT_MODES` = 平台突破 / W底 / 旗形 / 下降趋势线）。理由是可执行价不同：突破类的买区下沿**就是突破位本身**，是真实成交价；回踩类（沿线回踩 / 大阳后缩量回踩）买在**线上**，买区下沿只是理想价，主风控是**收盘破线**的结构止损，硬止损本就是 0.10×ATR 的毛刺滤网。拿突破类阈值去量回踩类，2026-09-18 全市场样本 44 个候选里误报 15 个（全是 `line_pullback`）—— 警告一旦三成命中就退化成噪声，没人再看。回踩类只报 `hard_dist_atr`，不报 `hard_noise`。

- 触发场景：**突破后隔夜/次日入场**。买区已随突破上移，而 `bars[-1]`（突破根）中点算出的止损离沿极近。
- 实证 INTC 2026-09-17 盘中：买区 107.57–113.53、ATR 5.96。大阳中点 107.92 − gap = **107.32**，距买区下沿仅 **0.04×ATR**（当日振幅 1.05×ATR，比噪声还窄）。换锚到阳线下沿 → 硬止损 **104.10**（距沿 0.58×ATR、距现价 1.17×ATR）。
- 换锚不违反铁律一/二：数值仍恒 = 该锚价 − gap、仍落在买区下沿之下，**只是不选那条塞在噪声带里的锚**。破了之后禁止再换锚改宽（这条不变）。

**硬止损触发（写进计划卡，盘中不发明）**：

- 开盘价已在硬止损下：开盘走，不等。
- 盘中成交价跌破后，**3 分钟仍在下面，或分钟线继续新低**：市价走，不挂死限价等反抽。
- 跌破后下一两分钟立刻收回硬止损之上，且不是放量加速阴：当毛刺，不卖。
- 等 3 分钟只防单根毛刺，不能替代 gap，也不能理解成「等等看会不会探底回升」。

**禁止**：按成本改宽；硬止损已被刺穿后再换到另一个更宽的锚（例如从沿改 MA5、再改中点）；用「这只股爱探底回升」取消硬止损。要吃探底回升就只守结构止损（收盘破），两套不要混用。

**两档止损的执行口径（能不能睡着觉就靠这条）**：

两档的可执行性完全不同，输出里必须各带口径（字段 `struct_exec` / `hard_exec`）：

| 腿 | 口径 | 需要什么 |
|---|---|---|
| 结构止损（`struct`） | **收盘口径** | **不用盯盘**：次日开盘 / 晨起按收盘价判定，收盘破即走 |
| 硬止损（`hard`） | **盘中口径** | 需要**盯盘**，或券商支持条件单/止损单；**两者都没有时此腿不可执行** |

- 已知约束：**不能盯整场 + 券商不支持挂止损单** → 睡眠时段的「硬止损腿」形同虚设，保护只剩收盘轨。
- 所以不能盯盘时**不要指望硬止损**，用两个替代：
  1. **用买价替代止损**：限价压到突破位附近（≤ 突破位 + 0.5×ATR）。买在沿上，跌回沿下就是失败，风险天然只有 ~0.5–1.0×ATR，不需要任何盘中动作。
  2. **用仓位替代止损**：无盘中保护的隔夜仓，风险按「没有止损」估（大票隔夜跳空尾部约 1.5–2 倍于正常止损幅度）→ **仓位减半**。
- 反向禁令：**不许把「等次日开盘再处理」当日常止损纪律** —— 那是无保护持仓，只能作例外（临时无法盯盘），且必须同时下调仓位。风控上没有免费的睡眠。
- 注：`room_and_cap` 的买入上限 `P_max` 与仓位股数都建立在硬止损上，换锚变宽会自动压低 `P_max`、减少股数 —— 这是正确联动，不是输出变保守。

---

## 目标价（止盈档：目标1 / 目标2）

必须能回答「什么价卖、卖多少、止损移到哪」。

- **目标1**：较近阻力（次级前高、测量涨幅、通道上沿、估值中枢）。
- **目标2**：更高阻力（ATH / 平台测量 / 乐观催化）。
- 达目标1 减 1/3～1/2，止损上移到成本或沿用的那条线；达目标2 再减一半，剩余移动止损。
- 下降趋势线突破（T2）未过前高时，目标1 先看平台沿/R1。
- 催化证伪或放量破位：不等目标，直接走。

---

## 作战计划卡（首屏并列：入场 / 止损 / 目标）

0. **模式卡**：`mode` / `priority` / `verdict`。按当日唯一模式写。`wait` 只写预案。若存在「标准突破 vs 前移买点」两条路径，用一句话写清**优先哪条及赔率理由**。
1. **入场**：价来自 `buy_zone`（`plan_entry` 计算），禁止硬编码均线底座（石头科技 106–116 那种深 MA 区）。图上标 `anchor`+`level`、R1、止损、目标1/目标2。
2. **分仓**：试错 10–20% → 确认 +20–30% → 加仓 +20–30%。T2 默认停在试错仓，直到升级为 T1。
3. **止损**：结构止损 + 硬止损两档都写价，见「硬止损怎么定」。必须能回答：**在哪里知错、错了亏多少**。
4. **目标1 / 目标2 + 减仓节奏**。必须能回答：**对了能赚多少**（相对硬止损风险的约略 R）。
5. **失效条件**。
6. **高β / 大盘板块资金**是否允许执行。
7. **扫雷结论**：解禁 / 减持 / 立案调查等（有则一句话+日期；无则写「近窗无重大解禁减持立案」）。有雷且窗口临近时，即使 `mode` 可买也降级为 wait 或试错仓上限。
8. **双轨打分**：投资价值 x/10 + 短线博弈 y/10，各附一句理由（见「强制双轨打分」）。供用户综合评判仓位与是否值得拿；Agent 不得省略。
9. **龙头对照**：主线概念 · 龙头 1–3 只及理由 · 主标的 vs 龙头优先做谁（见「同板块 / 概念龙头对照」）。

---

## 数据与脚本

执行时用 skill 安装目录的**绝对路径**：
- WorkBuddy：`~/.workbuddy/skills/stock-multidim-battleplan/`
- Cursor：`C:\Users\luoyu\.cursor\skills\stock-multidim-battleplan\`

```bash
python fetch_market.py 601233 --out data/601233.json
python fetch_ashare.py 688222 --out data/688222.json --n 300   # 东财被阻断时的 A 股备用源（新浪，不复权）
python fetch_market.py CF --out data/cf.json
python rule123.py CF --data data/cf.json
python rule123.py 300207 --data data/300207.json --eod --out out.json
python probe_intraday.py 002961 --qty 1500                    # 收盘后：次日预案单 + 次级观察位
python probe_intraday.py 002961 --asof 2026-09-16 --replay --until 10:25   # 盘中/回放：量能突变三档
python probe_intraday.py ILMN --us-account 5000               # 美股：盘前/盘中（含关键位突破）
python probe_intraday.py ILMN --us-account 5000 --date 2026-09-15   # 美股回放：验证当日信号
python watch_cn.py --save                      # A股股池每日复盘（逐票 + 情绪温度计）并存档 reports/
python watch_cn.py 688758 300759               # 只复盘指定代码
```

`--eod`：丢弃今日未收盘末根。`--out`：写出 JSON（默认不写 CWD）。

**A 股股池每日复盘（`watch_cn.py` + `watch_cn.json`，2026-09-19 建）**：全市场扫描解决「有没有票」，解决不了「**我池子里这几只今天怎么了**」。池子是人工选的（自己看得懂、流动性好、强势），单独维护：
- `pool` = **交易候选**（2026-09-19 建池 9 只：赛分科技 688758 / 康龙化成 300759 / 东富龙 300171 / 正帆科技 688596 / 圣邦股份 300661 / 英杰电气 300820 / 德福科技 301511 / 海星股份 603115 / 德邦科技 688035），每只带 `theme`、可选 `pos`（qty/cost/stop）与 `flag`（已知雷/事件）。
- `sentiment` = **只读情绪温度计**：**中际旭创 300308 = 科技情绪**（它强 → 科技/算力风险偏好高、池内半导体·AI 材料线顺风；它破位 → 池内高 beta 先减）、**中国银行 601988 = 防守情绪**（它强 = 资金避险、成长线要警惕；它弱 = 风险偏好在、成长顺风）。**这两只只用来读情绪，不产生买卖信号、不入池（用户 2026-09-19 明确）。**
- 输出四节：①市场温度（3 大指数 + 2 温度计）②**持仓 —— 今天破止损了吗**（收盘 vs 两档止损、浮盈亏、引擎结构止损 vs 用户自己止损的口径差）③**池内候选 —— 今天进买区了吗**（regime / mode / 现价落在买区哪一段）④可执行明细（买区 + 两档止损与执行口径 + 目标 + 突破埋伏单）。
- **判读铁律：「在买区内」≠「现价是好买点」。** 用 `zone_pos`（0=下沿/低吸位，1=上沿/追高位）定位 —— 回踩类必须**靠近下沿**才算低吸，近上沿就是追高。另有一条自动告警：**现价距结构止损 <0.25×ATR（噪声带）时提示「现在买＝贴着止损买，应等回到买区下沿 X」**（2026-09-18 实测触发：东富龙 0.09×ATR、德福科技 0.05×ATR、海星股份 0.20×ATR）。
- 每个交易日收盘后跑一次；`--save` 存档到 `reports/cn_YYYY-MM-DD.md` + `.json`。数据源新浪（与同花顺价格一致，涨幅基准偶尔有差）。

**美股股池每日复盘（`pool_us.py` + `pool_us.json`，2026-09-19 建）**：与 A 股版分工相同（逐票收盘复盘），但**规则前提有三处不同**，必须记住：
- 池子构成：`pool` = 交易候选 9 只（SNDK 闪迪 / MU 美光 / SKHY SK海力士ADR / MRVL 迈威尔 / INTC 英特尔 / NBIS NEBIUS / HPE 慧与 / BE Bloom Energy / PURR Hyperliquid）；`watch` = **观察位** 4 只（TEM / ILMN / SDGR / TWST —— AI+医疗，**已突破过远，只跟踪不追**）；`sentiment` = 只读温度计 **^VIX 风险温度**；`indices` = QQQ 纳指100 / SOXX 费半；`manual` = 韩股 000660 / 005930（**数据源拿不到，富途自看**，美股侧用 SKHY ADR 代 SK 海力士）。2026-09-18 起建池时美股**空仓**。
- **致富证券既无 buy-stop 也无条件单**（2026-09-19 用户确认，纠正了本文档早先的说法）—— 别把「美股市场支持 stop 单」当成「你的券商能挂」。实际能力 = **只有限价单 + 市价单**：
  → 买点在现价**下方**（回踩/低吸买）→ 限价单可**隔夜预挂** ✓，睡觉也能成交；
  → 买点在现价**上方**（突破买）→ 限价挂出会立即撮合（等于市价 + 价格上限），想「等上行触发」**只能盯盘** ✗ —— 而美股盘中在你的深夜，**实际做不了**。
  ⇒ **美股只有「回踩买」是可执行的**；`pre_breakout` 突破单在美股同样**不可预挂**，只能标为「需盯盘」。这与 A 股是**同一个约束**（不是相反）。
  ⇒ **选标的时这是一个硬维度**：同一逻辑里优先做「买点在下方」的那只。
- **且券商不能挂止损单**（2026-08-26 确认）—— 不能盯盘时**硬止损档失效，真实敞口 = 隔夜跳空**。
  → 因此 **仓位上限按「最坏跳空」反推，不按止损宽度**。脚本对每只票自动给两栏：常规 `−2×ATR`（预算 `risk_pct`=1.5%）与跨财报 `−12%` 跳空（预算 3%），**取更严的一栏**。
  → **连 1 股都超预算时输出「装不下」**：这只票在现有账户上做不了仓位管理（2026-09-18 实测：SNDK 1 股 = 账户 38%、2×ATR = 4.97% → 装不下；MU 1 股可行）。
- **信号冲突自动告警**：`mode=wait`（不追）但突破埋伏单 `dist_atr < 0.25`（几乎贴现价）→ 提示「下一个交易日开盘就可能触发，两个结论互斥，须二选一」。这是「**大阳当日不追**」与「**突破买**」的正面冲突 —— ILMN 9/15 踏空正是出在这个分支。
- 数据源 `fetch_market.fetch_us`（Nasdaq → Yahoo → stooq）；**Yahoo 源可能只有 ~127 根**，Nasdaq 275 根。上市短的新股/ADR 可单独放宽门槛（`min_bars`，SKHY=45）。
- 账户 `account`=**4694.80**（2026-09-18 结算后）、`risk_pct`=1.5。存档 `reports/us_YYYY-MM-DD.md|json`。

**股池跨设备同步（`sync_library.py`，2026-09-19 建）**：资料库（WorkBuddy 云端 space）是**跨设备真源**（换电脑/换会话都能看），本地 json 是**引擎真源**（离线可跑、无鉴权依赖）。这个脚本是两者之间唯一的桥：
- A 股：`watch_cn.json` ⇄ 资料库「A股股池 / 股池配置」（`eLKj7AvdWJyFmHYP6bCmku`）
- 美股：`pool_us.json` ⇄ 资料库「美股股池 / 美股股池配置」（`5ezNsvqYiC8iFY89HaFzHc`）
- `--preview-pull`（只打印不写）/ `--pull`（资料库 → 本地）/ `--push`（本地 → 资料库；**保留快照日/快照收盘**，那是数据不是配置）
- **token 会话级 1800s**，由 agent 换票后经 `--token` 传入 —— 不能写进自动化长期持有，所以同步是**按需手动触发**，不是自动任务。
- 两张表结构统一 11 列：标的（代码+名称合并）/ 类别 / 持仓数量 / 成本 / 止损价 / 止损执行 / 状态 / 已知雷·备注 / 快照日 / 快照收盘 / 板块主题。

`probe_intraday.py` 实现「盘中先手（Probe）」：`--account` 默认 50000 —— **股数由风险预算法自动算，`--qty` 已不需要**（传了也忽略）；`--asof/--replay/--until` 用于 A 股复盘回放，**美股复盘用 `--date YYYY-MM-DD`**（只用该日之前的日线定结构，杜绝用到未来数据）。预案单额外输出 **买入上限 `P_max`**（盈亏比闸门）与 **T+1 高开处置** 两行 —— 执行时以这两行为准。

**量价研判（2026-09-17 兆龙互连案例固化）**：判买（买区有效）时，A 股输出第四节、美股第五节，用近 **20 个已完成交易日**的量价确认筹码处于收集还是派发，**防买在派发阶段**。口径：**涨跌量比 = 阳线日均量 / 阴线日均量**（`VP_COLLECT_RATIO=1.30` 以上 → 涨放跌缩，收集特征；`VP_DISTRIB_RATIO=0.80` 以下 → 跌放涨缩，⚠ 派发特征），辅以**量能趋势**（近 5 日均量 / 前 15 日均量，<1 = 回调缩量）、**末根行为**（缩量刺破 MA5 收回 = 洗盘特征）与**区间涨幅**。**证伪位 = 窗口最低价：放量（≥`VP_VOL_SPIKE`=1.5×窗口均量）收盘破 = 派发确认，预案单作废**。verdict 只提示不否决 —— 能否决交易的只有硬约束。兆龙互连 300913（9/16 收盘）实测：涨跌量比 1.51×、近 20 日 +26.1% → 「收集·洗盘，可按预案执行」。

第四节「关键位突破」的锚点 K 由 `rule123.key_break_level(bars, atr_v, last_c)` 给出：近期整理上沿（`bars[-4:-1]` 最高价）与摆动前高（`pivots`）取低者，且须落在现价上方 **0.2–1.5×ATR**；候选一天然 ≥2 根之前形成，排除「昨天刚创的高点」。

`backtest_yang_pullback.py`：可复跑的证据脚本（全新浪链路，避开东财限流）。扫全市场大阳样本，统计「挂在不同高度」的 T+1 / 5 日内成交率、永久踏空率、按高开分层，以及「踏空后追高 vs 等回踩」对照。改 `BANDS` / 最低涨幅即可重验。

`backtest_intraday.py`：**盘中通道的样本外体检**（全新浪 5 分钟链路 + 本地 `cache/`）。对「标的 × 交易日」逐根回放，用 `probe_intraday` 的**同一批常量**判定通道 A/B/C 与突破预案单，输出 R 口径与**账户口径（含双边成本）**两套结果；`--sweep` 做参数敏感性扫描，`--portfolio` 做**组合层模拟**（主力 5 万 + 备用 5 万的额度分配）。结论可 `python backtest_intraday.py` 复跑，最要紧的几条：
- **判策略只看净账户%**：预案单 T+0 的 `+0.27R / PF 1.56` 在价格口径下只有 −0.019%/笔，因为 27% 的单子止损距 <0.5%（跌 1% 就是 −5R）。R 只适合比同一策略的不同参数。
- **A 股 T+1 是结构问题**：四条通道 T+1 净账户全负，换成「日线结构位 + 收盘破」也只改善了 0.001%–0.023%/笔，仍未转正；持有期放到日线级（最长 5 日）反而更差。
- **通道 A 改定义后活了，但仍无边**：旧定义 903 个标的日只触发 3 次；改成「前 5 根均量 × 2.0」后触发 266–657 次，可 T+0 净账户恒在 −0.001% ~ −0.014%（≈扣成本后归零）。
- **额度才是真约束**：552 条候选里只有 13.4% 拿得到钱；主力 5 万 ÷ 单笔上限 1.5 万 = **最多同时 3 笔**。
- **一批参数是非约束的**：`BREAK_DMAX_ASH`（0.40~1.00 结果几乎一样）、`BURST_MA_BARS`（3/5/10 接近）、`PULLBACK_VOL_MULT`、`PRE_AMP_MAX` 实测改了结果基本不变。别再调这些数字，边际收益在「通道有效性」本身。
- **`--diagnose` 的三个判决（2026-09-17）**：① 突破预案单唯一的正数 `+0.029%/笔` 活不过 0.30% 成本，且样本外观察期 +0.070% → 验证期 −0.015% **翻负 —— 是 in-sample 幻觉**；② **买点分位必须用「截至信号那一刻」的口径**（`bpu`）—— 全日区间含未来信息，会把「买完就跌」机械地记成「买在高位」（预案单高位胜率 0% 是假的，真值 9.3%）；且梯度**按信号类型反向**：回踩类高位≈死（−0.198%/笔），量能突变高位反而最好（+0.014%/笔）；③ 通道A 对通道B/C/突破单是**负过滤器**（−0.085~−0.133%/笔）—— 它触发说明票已在拉升，此时再追更危险。组合过滤器（分位≤2/3 + 缺口<+1% + 通道A 只认早盘）能把 21 日合计从 −9 元拉到 +14 元（5 万账户）≈ 零：**纪律的收益是止损于亏损，不是创造盈利**。

`rule123` 除 `mode` / `buy_zone` 外必须带：
- `stop_plan`：`struct` + `hard` + 锚名 + `trigger`（两档止损，禁止合成一个价）+ `struct_exec` / `hard_exec`（各自执行口径）+ `hard_dist_atr` / `hard_noise`（距买区下沿的 ATR 数、是否落进噪声带，**仅突破类**）+ 可选 `hard_note`（换锚留痕）
- `pre_breakout`（预备突破单，与 `mode` **并存**）：`order=buy-stop` / `level` / `trigger` / `hard_stop` / `risk_per_share` / `dist_atr`，`anchor=down_tl` 表示斜线挂法、缺省为活平台沿（2026-09-18 起两种同时够格取更近的那条）
- `targets`：`target1` / `target2` / `rr_target1`
- 平台突破 `anchor=platform_lip`（活平台沿，不是死 R1）
- 突破类可执行三档：买区内（≤1.0×ATR）`recommend=True`；出上沿但 ≤2×ATR `recommend=True` 且 `chase_only=True`（只挂回踩单、禁市价追）；>2×ATR `recommend=False`

**新字段必须同时落进全市场报表**：`scan_all/scanner.py`（写 `results.jsonl`）+ `scan_all/report.py`（渲染 HTML）。只改 `rule123.py` 的话，CLI 与 probe 能打印、**全市场扫描报表看不到 —— 而报表才是扫描结果唯一的消费口**（`pre_breakout` / 两档执行口径 / 盘中口径曾连续三轮只活在 CLI 里，2026-09-18 补齐）。报表列：买区（回踩等待位）与预案单（buy-stop 挂价位）**分列**，别合成一格 —— 它们是两条腿、先到先做。

ATR：Wilder ATR14。

**数据源与降级链**（实测口径，2026-09-16）：

| 市场 | 主源 | 降级 | 备注 |
|---|---|---|---|
| A 股 | 东方财富 | **新浪 `fetch_ashare.py`** | HTTPS 被中间设备阻断时自动降级 HTTP（`fetch_json_fallback`）。若 https / http **双双 `RemoteDisconnected`**（2026-09-18 实测：东财 `push2` 与 `push2his` 直连全挂），**停止重试**，直接跑 `python fetch_ashare.py <code> --out data/<code>.json --n 300` —— 输出与 `rule123.py` 兼容。注意：新浪日 K 为**不复权**价（分红缺口可能含噪）；批量抓 ~180 只后返回 HTTP 456 限流 |
| 美股 | Nasdaq 官方 API | 东财 http → Yahoo → stooq | Nasdaq 独有**盘前价/盘前量/市场状态**；东财 http 独有**分钟级带量 K 线** |

- 美股 `session` 字段由 Nasdaq 直接给出（`Pre-Market` / `Open` / `Closed` / `After-Hours`），**不要自行判断夏令时/冬令时**。
- 美股日线源（Nasdaq historical）**只含已收盘交易日**，盘中取到的末根仍是昨日。因此 `rule123.py` / `probe_intraday.py` 在 `session=Open` 时**会用 Yahoo 5 分钟线合成一根「今日未收盘」bar 并并入结构**（`merge_intraday_bar`），模式与买区随盘中刷新 —— 否则突破当天引擎看不见突破，买区会一直停在昨收的回踩位（INTC 2026-09-17：已破平台 107.57，买区却还是 99.45~104.03）。输出里 `intraday=true` 即此口径，`confirm_at` 给出尾盘复核时点。
- **盘前 / 盘后不合成**（成交稀疏，不能当「收盘站上」）；`session=Closed` 时日线本身完整，也不合成。
- 合成 bar 的 `v` 是**量比口径**（盘中累计量按已走时段折算成预计全日量），`v_raw` 才是当日累计量 —— 半天量直接比整日均量会把放量突破误读成「缩量」。
- `spot_quote` 仍是 Nasdaq 原始实时价，可与合成 bar 的收盘价互相校验（差值为 5 分钟线最后一根的滞后）。
- 美股盘中时段 = 北京时间 21:30–04:00（夏令时）。

取数优先级：wb-finance-skill → `fetch_market.py` → 网页检索（并标注来源）。

---

## 输出规范

- HTML 落盘 `C:\Users\luoyunlai\stock-reports\<TICKER>-YYYYMMDD.html`（不要写入代码仓库）。首屏结论 + 模式卡 + 入场/止损/目标1-目标2 + **扫雷一行** + **双轨打分** + **龙头对照（优先做谁）**。不输出 Gamma 卡。CDN 被挡时再写 `*_standalone.html`。
- 无文件环境：markdown 同等结构。
- 对话附 200–300 字摘要（须含双轨分数 + 龙头优先结论）。关键数据标来源+日期。
- 含买卖价位时附：

> 免责声明：以上内容基于公开数据和量化分析，仅供参考，不构成投资建议。市场有风险，投资需谨慎。任何投资决策应结合个人风险承受能力、资金状况和投资目标独立判断，必要时咨询持牌专业机构。过往表现不预示未来收益。

## 避坑

- 不把「买点前移」理解成抄弱势底或第七种买法；标的仍须是强势股，入场仍须落在六种 `mode` 之一。
- 不拿事后走势证明「昨天就该预判突破」；无昨日有效买区则昨日就不该买。
- 不把突破阳的最高价当成新平台沿（睿创 8/31 高 185，近 3 根窗口里沿仍是被破的 179）。
- 不把 P0–P1 之间的旧 R1 当成活平台沿（山东黄金 32.55、闰土 13.34）。读 `platform`。
- 不把远处 ATH 当成今天的沿（紫光 45.80 不是 41.68）。
- 不把「已经涨过 R1」当成今天的平台突破。
- 不把 MA20/MA60 深度底座当成短线买区。
- **不把「均线在涨」当成「价格沿均线走」**（2026-09-18 BE）：两者视觉相似但性质相反。判据是**统计近 8 根最低/收盘价进入均线 ±1% / ±2% / ±3% 的次数** —— 真「沿均线走」应 ±1% 有多次命中；只有放宽到 ±5% 才命中，那只是「在均线上方运行」。BE 近 8 根 ±1% 命中 **0 次**，价格始终在 MA10 上方 **+3%~+22%**；真正沿 MA10 走的是 8/26–9/2（偏离 −1.5%~+3%，连续 6 天，价格 206~218），是**上一个阶段**。附图觉得「贴着均线」，往往是前期暴涨把均线甩在后面、均线在追赶（rising MA）。
- **加速段的均线：等回踩＝追涨**。均线本身每天上移（BE 的 MA10 每天 +5.76 元 = 0.31×ATR/天），今天 267.04、明天 269.8、后天 271 —— 你等 267.04，明天这个价已在线下。「越等越贵」是加速段特征，不是耐心。
- 不把下降趋势线突破或大阳后缩量回踩（都是 T2）写成和平台突破同等仓位。
- 不把沿线回踩改成「等回踩大阳」；大阳后缩量是第四种、单独的 T2。
- 不把一字板的涨停价写成止损；那种跳空用缺口下沿（前收）防守，有实体的光脚大阳仍用大阳低点、不买回补缺口。
- 不把硬止损贴在锚价本身（MA5 / 阳线下沿 / 大阳中点都必须再下 0.10×ATR gap）。
- 不把突破类买区设到突破位**之下**（那是「还没突破」的价位，挂买单自相矛盾），也不让买区落进跳空缺口（缺口上沿才是买点）。
- 不让硬止损的**锚名与数值脱钩**：写了「大阳中点」就必须等于 (最高+最低)/2 − 0.10×ATR。锚与买区冲突时抬买区，不偷偷改止损数。
- 不让硬止损落进单日噪声带（**突破类**买区下沿 <0.25×ATR）：那种「止损」一次正常波动就被扫，等于没有。同族有更宽合法锚就换过去，没有就标 `hard_noise` 如实提示。回踩类不套此阈值（买在线上，主风控是收盘破线）。
- **不把硬止损当成随时生效的保护**：它是盘中口径，需要盯盘或券商条件单。不能盯盘时保护只剩收盘轨 —— 必须同时用更低限价 + 更小仓位补上，不许假装还在被止损保护。
- 不在硬止损已被刺穿后再换锚或改宽到下一根针。
- 不把「观察会不会收回」写成无限期等待。
- 止损写完即定，禁止按成本价改宽。
- 沿线回踩结构/硬止损锚默认 MA5，不要默默改成 MA10。
- 很长突破大阳的硬止损锚用 (高+低)/2，不要默认贴平台沿下 0.10×ATR（那会过窄易洗）。
- 不做扫雷就给买入：漏查大额解禁、减持计划、立案调查等重大利空，技术面再漂亮也不能当可执行计划。
- 不省略双轨打分，不用单一「综合分」代替；不把涨停次数/弹性误当成投资价值加分。
- 不分析同板块/概念龙头就推荐买入跟风股；龙头看涨幅与主动性，不看市值最大。
