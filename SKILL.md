---
name: stock-multidim-battleplan
description: 美股/A股个股的多维度分析 + 作战计划。当用户要求多维度分析、买卖计划/作战计划、该不该买/怎么买/止损止盈时使用。入场模式：T0 均线收复+过昨高（最高优先，盈亏比最高）、预备突破单埋伏（优先T1）、平台突破 ⟷ 牛旗（旗形下降趋势线）突破（**并列**优先T1）、W底颈线突破、沿线回踩（优先T1）；大阳后缩量回踩、下降趋势线突破（次优先T2）。叠加赔率优先与买点前移（环境适配：流畅趋势仍做标准突破；高波动震荡市优先关键支撑低吸 / 紧凑盘整第一次启动）。叠加六维研究、扫雷、同板块/概念龙头对照（优先做龙头）、强制双轨打分、尾盘确认、不追高、止损 ATR+结构。叠加概率七层计数（confluence.py：整体市场/板块主题/领导者/催化剂/形态/量能/执行，只排序不否决）。强制产出入场/止损/目标1-目标2，并强制全档枚举、高亮最高盈亏比路径。美股日内做空（us_short.py：反抽空点 / RR临界入场价 / 反向ETF换算，禁追跌）。
disable-model-invocation: true
---

# 个股多维度分析 + 作战计划（stock-multidim-battleplan）

> **何时用**：用户要求对某只股票做多维度分析、作战计划、买卖点、止损止盈。
>
> **取数**：WorkBuddy 已连接通达信时优先使用 `tdx_lookup` / `tdx_quotes` / `tdx_kline`；不可用时依次降级到 `wb-finance-skill`、`fetch_market.py`。结构判定只用 `rule123.py`。通达信取数结果先用 `snapshot_from_tdx.py` 落成统一快照（**当日多只票一次取完**时用 `--batch <一段含多个返回的文本|目录>`，默认落到 `data/tdx/`），再喂 `rule123.py --data` / `probe_intraday.py --data`（禁止手工拼 `bars`）。所有日线取数（`scanner` / `watch_cn` / `pool_us`）已统一走 `bars_source.py` 三级链路 **本地快照 → 磁盘缓存 → 网络**，所以 `watch_cn.py --snap-dir data/tdx` / `pool_us.py --snap-dir data/tdx` 可**全离线**出复盘；报告首部会把「快照/缓存/网络各多少只」打出来。缓存状态与清理：`python bars_source.py --stat|--clear`。
>
> **基本面取数**：`tdx_security_deep_info` 返回 80–160KB，**必然超 token 落盘** ⇒ 拿到落盘路径后直接 `python tdx_deep_fin.py <落盘文件> --fin`（自动解析 + 按报告期排序去重 + 单位换算 + **单季/累计口径反查**），不要再手写 `python -c` 逐字段试。坑与口径细节见 [data-operations.md](references/data-operations.md) 第 11 条。

## 执行原则

1. 先确认市场、代码、分析时点和是否已收盘。
2. 主标的日线只取一次；快照落盘后 rule123、probe 与研究检索并行。同行默认轻量筛选。
3. 先过标的总否决与扫雷，再判六种买法和 T0，最后计算赔率、仓位、止损与目标。
4. 报告必须包含量价分析、双轨打分和龙头对照；未收盘不得下全天派发结论。
5. `recommend=False` 时不得给可执行下单指令；离买位大于 2×ATR 不追。
6. 止损必须分结构止损与硬止损两档，禁止合成一个价格。
7. **出报告走两步，不要手写 HTML**：`battle_analyze.py`（取数 → 结构 → 量价 → 全档赔率 → 同行 → 分时，一次算完出 `analysis.json`）→ `report_render.py`（模板 `templates/battle_report.html` + `notes.json` 渲染）。版式与表格由脚本生成，**模型只写判断与叙述**（`notes.json`，≈2K token）。细节见 [report-generation.md](references/report-generation.md)。
   - **A 股与美股同一入口**（2026-09-22 起）：`python battle_analyze.py <6位数字|ticker> --account <金额> [--cash <可用现金>] --peers ... --out out_cn|out_us/analysis_<code>.json`。市场由 `is_us_code()` 判定：6 位纯数字 = A 股（`bars_source.ash_bars` / `probe` / `ash_lots`），其余按 ticker（`bars_source.us_quote` / `probe_us` / `us_lots`，1 股起、无整手、币种 $）。
   - ★ **别再用 `rule123.py --out` 当交付物** —— 那是引擎中间产物（JSON），不是给人看的报告。它只适合全市场扫描/脚本消费；用户要「报告」就必须走上面两步出 HTML。
8. **账户与仓位额度一律从 `account_config` 读**（env > `.env` > DEFAULTS），脚本里不许出现 `account=50000`、`/ 50000`、`CAPITAL = 100000` 这类字面量。A 股**总仓位上限 100,000 = 主力 50,000 + 后备 50,000**（`ASH_PRIMARY` / `ASH_RESERVE` / `ASH_TOTAL`），单笔绝对额硬顶 50,000；`ASH_ACCOUNT` 只是**风险预算的分母**、不等于可动用资金。报告仓位要同时给「占总上限 %」与「占主力层 %」。见 [data-operations.md](references/data-operations.md)。
9. **出计划前先过「大盘情绪闸门」**：`python market_sentiment.py` 把大盘情绪量化成 0–100 分（宽度 35% + 涨停跌停 20% + 主力净额 20% + 趋势结构 15% + 量能 10%），并映射**仓位缩放**：**≥70 强**（按计划执行）/ **55–70 偏强**（标准仓，弱票不追）/ **45–55 中性**（只做最强龙头，半仓）/ **30–45 偏弱**（小仓试错 ≤1/4 仓）/ **<30 极弱**（缩到最小仓或观望）。`--json` / `--out` 供其他脚本消费。
   - ★ **情绪分是软约束：只缩放仓位，不是「开/不开仓」的开关**。能否决一笔交易的只有硬约束（最小申报单位×股价 > 仓位上限 / 涨停买不到 / 结构已坏）。**<45 的正确动作是「降仓」而不是「不许做」**，报告里写「因环境降仓至 X 成」，**禁止写成「环境否决」**——那是把软约束抬成否决权。
   - 量能维度**方向感知**：放量上涨才加分，放量下跌视为恐慌抛售而扣分；盘中成交额按时段加权折算（上午 55% / 下午 45%），不按分钟线性折算。
   - 三个映射常数（`WIDTH_CENTER=0.45` / `WIDTH_SLOPE=200` / `MONEY_SLOPE=1000`）**不得凭手感调**——它们直接决定总分跨不跨档（45→30 那条线）。每个交易日收盘 `python market_sentiment.py --out data/sentiment_$(date +%Y%m%d).json` 落盘，攒够 ≥20 个交易日后跑 `python sentiment_calibrate.py` 用真实分位数校准中性点。回归测试：`python test_market_sentiment.py`（17 项）。
10. **消息驱动型标的：四道闸（抢跑判据 → 禁止抢单 → 前置硬止损 → 禁止消息回补）**。① **抢跑判据（唯一的下单前必算项）**：任何因消息/公告才考虑参与的标的（BD 授权、对外合作、大额订单、业绩预告、政策批文、重磅临床数据），**下单前必须先跑** `python pre_runup.py <code> --date <公告日> [--bench <板块指数>]`，算它**在公告之前已经涨了多少**——消息前已有一波涨幅 = 消息早被提前定价（内幕盘已建仓），**公告日对他们是兑现窗口，不是启动信号**。判据：公告前 **5 日 ≥ 10%** 或 **10 日 ≥ 15%** ⇒ **★★★ 强抢跑，公告日禁止追入**（含午间公告后的 13:00 开盘）；5 日 ≥ 6% 或 10 日 ≥ 10% ⇒ 只做公告后 ≥5 分钟的第二结构且仓位减半。再叠加**个股涨幅 vs 板块基期涨幅**：个股独涨 >> 基准 ⇒ 内幕盘特征更显著。**A 股科创板创新药 / Biotech 的 BD 类公告最典型**（谈判周期以月计，涉及对方 BD 团队、CRO、顾问、律所 ⇒ 信息必然提前扩散）。实证：诺诚健华 688428 在 2026-09-24 礼来公告**之前**，前 5 日 **+16.09%**、前 10 日 **+15.61%**、距 20 日低点 **+29.26%** ⇒ 判定强抢跑，13:00 抢入者的对手盘正是这批埋伏盘。② **禁止在消息驱动型标的的开盘瞬间抢单**（A 股 09:30 开盘与**午间公告后的 13:00 开盘**同等适用；美股对应开盘/盘前首分钟）。公告兑现日的开盘 1~3 分钟是价格发现阶段，**市价单输在秒级报入时点，预埋单+保护限价自相矛盾（设上限可能不成交、不设则失控），不存在"既抢到又便宜"的解**。正解 = 等开后 ≥5 分钟第一波回落给出结构再评估，或完全不做。前置信号（任一命中即不参与）：公告前已带量下跌 / 主力净流出 / 内盘 > 外盘。③ **任何因消息/公告参与的仓位，下单前必须先写下硬止损价；写不出来 = 这笔不做**——A 股 T+1 下当日买入不可卖，**止损位是唯一能在下单瞬间锁死的风险上限**，没有它，止损位就形同虚设（"亏损有限"是错的：600 元只是中途浮亏，同一笔按 20% 跌停 + 次日跳空的名义极端亏损可达 −1.7k~−2.5k = 主力池 3.3%~4.9%）。④ **同一标的当日卖出后，消息不构成回补理由**（与「后悔式回补禁止」合并执行）。⑤ **午间消息先扫再决定做不做**：公告出来后、下单前必须扫两件事——该股同类利好有没有黑历史（上次公告当日高开/冲高后放量收阴），以及这次有没有利好出尽嫌疑（公告前已带量下跌，午后买盘是兑现窗口）。任一命中，当日不做，包括 13:00。案例与尾部风险测算见 [trade-lessons.md](references/trade-lessons.md) L001（诺诚 688428，2026-09-24）。
11. **买点选择前先做「股性体检」**（`python stock_character.py <code>`）：四个维度 —— **① 突破后行为**、**② 利好兑现习惯**、**③ 突破后路径（V 型 / N 字）**、**④ 当下连拉状态**。判据只决定「用哪种买法 / 能不能在公告日动手 / 止损留多宽 / 出场用固定目标还是移动止损」，**不决定「买不买」**（与第 9 条情绪闸门同一分工：软约束只换做法，不否决）。**输出顶部先给【结论】，再列依据** —— 老罗 2026-09-24：「冲击率这个指标我看不懂啊，打出来没意义，**我要的是结论**」。冲击率已从人读输出撤掉（它既非 published 判据、也不单独改类）。
    - **④ 是状态判定，不是股性标签**：老罗 2026-09-26「还有那种连续拉升型，历史股性确实要参考」—— 实测（`research_streak_type_2026-09-26.py`，376 票 / 2385 信号）：股性**存在**（收阳天数 ICC(1)=+0.066，随机基准 ≈0），但**单票估不准**（split-half corr=+0.064 t=0.74 不显著；高/低 habit 组后半期实测连拉率 42% vs 44%，方向还是反的）⇒ 经验贝叶斯最优权重 w=n·ICC/(n·ICC+1−ICC)，300 根 K 线只够 n≈6 ⇒ **历史连拉率只值 30% 权重，其余 70% 用全市场基准 40.4%**。所以**不拿历史 habit 预判这次会不会连拉**；连拉是**进行时可观测**的 ⇒ 入场后按「连阳 ≥3 且段内 ≥4%」切换成移动止损（撤掉固定目标位）。模块里的历史连拉率已自动缩水并标注「只作参考，不作预判」。
    **③ 突破后路径（老罗 2026-09-26：「有的股短调一下就上，有的喜欢画 N 字，怎么判断？次日买入适合它吗？」）** —— 与 ① 的分工：**① 回答「追了能不能赚」（结果维度），③ 回答「会不会先挖个坑把止损扫掉」（路径维度）**。睿创微纳 688002 的亏损就是路径问题：方向没错（90% 最终突破信号日高点），但习惯先回撤 6.97%（中位、盘中口径）⇒ 窄止损必被扫。判据：大阳后前瞻 10 日，按从大阳收盘起算的最大回撤深度分类 —— **V 型**（突破且回撤 ≤3%，浅调续攻）/ **N 型**（突破且回撤 >3%，先挖坑再上）/ **FAIL**（10 日未突破）。
    - **V 型占比 ≥35% ⇒ 短调续攻型**：突破买 / 次日买可用（实证 V 型次日买 +10.28%、挂 −2% 只有 27% 成交、挂 −5% **0% 成交** ⇒ V 型只能次日买）；
    - **N 型占比 ≥50% ⇒ 深调再上型（画 N 字）**：**禁止突破追与次日追**（实证 N 型次日买 −1.27%），只在回踩接，且回踩位按**中位坑深**量级设；
    - **★ 止损宽度必须 ≥ 该股「坑深 P75」**（输出里的 `习惯回撤深度 P75`），容不下就别用这个买法 —— ⚑ **口径**：默认给的是**盘中最低**口径（偏大）；我们的止损多为**收盘破**，比较时应按**收盘口径**折算（睿创 盘中 6.97% vs 收盘 4.32%；威力 14.00% vs 13.25%）。
    - ⚑⚑ **不能靠「股性惯性」预判**（实证 `research_v_vs_n_2026-09-26.py`，400 只 / 2385 信号）：habit（该股历史 V 型占比）与本次是否为 V 型 **corr = −0.019、t = −0.56，样本外 t = −1.24 ⇒ 无惯性**。**唯一显著的预判变量是前期涨幅**：60 日涨幅 V 组 9.77% vs N 组 20.02%（**t = −10.04**）⇒ **已经涨了很多的票大概率画 N 字**。全样本基准：V 19.3% / N 63.8% / FAIL 16.9% ⇒ **画 N 字是常态，不是特例**。
    **① 突破后行为** —— **同一个「突破」信号，在不同标的上的期望值可以相反** —— 关键是这只票**突破后能不能连续涨**，还是**拉一根大阳就要调几天**。判据：取历史单日涨幅 ≥5% 的大阳，以「大阳当日收盘」为买点（= 突破追入），统计其后 5 日高低点，**赔率 = 后 5 日最高价均值 ÷ |后 5 日最低价均值|**。
    - **赔率 ≥ 1.5 且延续率 ≥ 50% ⇒ 突破加速型**：可用突破类买法（平台突破 / 过前高），回踩买同样有效；
    - **赔率 < 1.0（或延续率 < 35%）⇒ 拉高消化型（震荡上涨）**：**突破当日禁止追入**，买点只放**回踩（MA10 / MA20）**；
    - 其余 ⇒ **混合型**：优先回踩；若做突破买，止损必须收紧、仓位减半。
    实证：睿创微纳 688002 近一年 20 根大阳，**赔率 0.87（< 1.0 单独命中消化型）** ⇒ 突破当日追入是负期望（该股是 2026-09 **最大亏损源 −1,528.71**）。反例方向：博济医药 300404 / 诺诚健华 688428 判为加速型、振华股份 603067 判为消化型。

    **② 利好兑现习惯**（2026-09-24 追加）—— 自动抓该股历史公告、筛出**利好类事件**（BD 授权 / 合作 / 中标 / 业绩预增 / 获批 / 回购增持…），定位**真实反应日**（公告日已显著异动则算当日，否则算次日 —— 覆盖盘后发公告），统计**「反应日收盘买入」后 5 日的期望收益**。
    - **期望 < 0 且后 5 日为正 < 50% ⇒ 出消息即顶型**：**公告日禁止追入**（含盘前 / 午间 / 开盘瞬间），利好只作减仓、离场参考；
    - **期望 ≥ 0 且后 5 日为正 ≥ 55% ⇒ 利好有效型**：可按常规买法参与，但仍须先过第 10 条的抢跑检查；
    - 其余 ⇒ **利好混合型**：公告日只做 ≥ 5 分钟后的第二结构，仓位减半、止损前置。**样本 < 3 个时标注「仅供参考」，不得作为否决理由**。
    实证：**诺诚健华 688428 的 8 次利好事件里 7 次后 5 日为负**（期望 **−3.79%**，含 2025-10-09 Zenas 授权当日 −6.24%、后 10 日 **−17.54%**）⇒ 出消息即顶型 —— 2026-09-24 礼来公告那次不是运气，是这只票的**稳定特征**。
    ⚠️ **两个维度互相独立**：博济 300404 / 东材科技 601208 / 有研硅 688432 同为「突破加速型」，利好习惯却分别是有效 / 即顶 / 有效 ⇒ **第二层给的是第一层给不出的信息**。成本：首次约 1.5~2.4 秒（联网翻 3~6 页公告），**24 小时内重复跑走缓存 ≈ 0.5 秒**；`--no-ann` 离线只跑第一层，`--events YYYYMMDD,...` 手动指定利好日。
    ⚠️ **抓取刻意「不早停」**（2026-09-24 实测后定）：试过「命中 ≥ NEWS_MIN_EVENTS(3) 个利好即停」省时间 —— 7 只票里**天元宠物 301335 结论直接翻转**（4 事件 +0.79% 混合型 → 7 事件 −0.83% 出消息即顶型），**错的方向还是「鼓励交易」**；「近期无利好就跳过」同样不成立（振华 603067 前 2 页回溯 4~5 个月零命中，第 3~4 页才有 2026-01-19 / 2025-11-25 两个事件）。**省 1 秒不值得冒翻转结论的风险** —— 要省时间请用 `--no-ann` 显式跳过，不要加隐式早停。**接口失败与「有公告但无利好」必须分开报**（前者报「接口未返回数据」，后者才报「无利好事件」）。见 [trade-lessons.md](references/trade-lessons.md) L002。
12. **T0 先判「刚收复」还是「沿线上行」；且沿线只是背景、当日形态是事件**（`rule123.ma_ride_state()`，2026-09-24 三轮定稿）：**不看「今天距某条均线几个点」，看「哪条线已经向上走了多久、够不够得着」**。MA5、EMA10、MA20 各自算：20 根斜率 > 0 且近 20 根 ≥12 根收在线上。四态：
    - `line_ride` —— 满足上述条件**且现价距该线 ≤ 1.5×ATR**（贴着线走）⇒ 有回踩锚。多条成立时取收盘下方、离现价最近的那条，回踩就锚这条，**不写死五日线**。
    - `new_high` —— 有线满足上行条件、**但一条都不在可回踩距离内** ⇒ **新高·无回踩锚**，`prefer=breakout`，**不给回踩买点**（老罗原话：「没有锚点就代表是新高，不需要找锚」）。门槛 1.5×ATR 的落点：距线 0.5~1.0×ATR 时 5 日内回踩触及率 66%、触及后期望 +1.59%；>1.5×ATR 时触及率跌到 **24%**、期望转负 **−0.22%** —— 这条线已不是可用回踩锚（振华 603067 09-24 锚 MA20 36.32、距 **1.92×ATR** 即此类）。
    - `fresh_reclaim` —— 三条都不成立、且 MA5 斜率 ≤ 0 且站上根数 ≤11 ⇒ 均线刚收复，**T0 原定场景**；其余 `mixed`。
    - ⚠️ **优先级：当日形态（事件）> 沿线上行（背景）**。沿线**不得改道掉客观突破**——当日已有 `platform_break`/`w_bottom_break`/`flag_tl_break`/`downtrend_tl_break` 且 `recommend=True` 时，突破入口为主、沿线锚只作「突破失败后的次选」，写进 `plan["ride_priority"]`（`winner=breakout`）。依据：同日突破买（1.5×ATR 止损）均R **+0.212** / 收益 +1.16% / 胜 44%，而「挂限价等回踩」只有 **31%** 的日子等得到（每机会 +0.39%）；且远锚档突破买反而最好（+0.278R）。**只有当日别无买点（wait）时，沿线回踩才是唯一入口**（此时不牺牲任何突破机会）。老罗原话：「否则所有股大部分时间都是沿均线走的。」
    - **这是改道不是否决**：买区在现价下方时是可预挂的限价单，不违反「有 setup 就该能挂单入场」。改道时记为 `t0_held_for_ride`（带 `line/line_label/anchor`）**且 `ride_redirected=True`**，报告徽标写「T0 已改道」；未改道时写「T0 未接管：当日 X 是事件、沿线只是背景 → 突破优先」。⚠️ **判「是否真改道」只看 `ride_redirected`** —— 不能看「`mode == line_pullback` 且有 `t0_held_for_ride`」（东材 601208 09-24 当日**本就有** `line_pullback` 买区、带 `t0_held_for_ride` 却未改道，按 mode 判会把徽标误标成「已改道」）。⚠️ 字段口径：`line*` = 被选中的那条线，`ma5*` / `above20` 恒为 MA5，报告与池表**不得混用**（本地 `data/` 53 只 A 股池里 `line_ride` 20 只、其中 **16 只锚非 MA5**（EMA10 8 / MA20 8），取 ma5 会报出完全不同的价位）。
    - 结果挂在 `plan["ma_ride"]`，**与 T0 是否成立解耦**（东材 601208 09-24 创 25 根新高 ⇒ T0 不成立，但它就是 `line_ride`）；报告中作为独立徽标「模式判别 = …」始终展示。周线、月线用同一套判据，结果在 `ma_ride_week` / `ma_ride_month`，不改日线 mode 和止损。季度及以上不看。
    - 作废的旧口径：`D0 收盘距 MA5 ≤ 4%` —— 那是 4 只票 × 6 个月拟合的；53 只 × 500 根全池按**真实触发价 + MA5 硬止损**回放，方向直接反转（>4% 均R −0.032 vs ≤4% −0.196）。⚠️ 真正的问题是**止损锚太紧**（离得近 ⇒ MA5 锚只有 2.3% ⇒ 77% 被毛刺扫掉），不是「追高」本身。实测表与稳健性见 [strategy-modes.md](references/strategy-modes.md) 第 ④ 条；回归 `python test_ma_ride.py`（29 项），复现 `python research_ride_priority.py`。
13. **美股日内做空先跑作战卡**（`python us_short.py <ticker> [--entry … --stop …]`，2026-09-24 SNDK 定稿）：做空只做**反抽空点**（跌破的 MA5 / 昨低回抽），**禁止开盘追跌**。核心是 **RR 临界入场价** `min_entry(RR) = (RR×止损锚 + T1)/(1+RR)` —— 止损锚与目标固定时入场越低 RR 单调递减，**RR<1.5 = 负期望区禁开空**；止损距离 <0.25×ATR 的档位是噪声带假赔率（脚本自动打标）。**反向 ETF 方向互锁：买入 SOXS/MUZ/SNDQ/SKDD = 做空标的，券商 `sell short` = 双倍做多**；MU/SNDK/SKHY 同属存储（相关性>0.9）只做一个。止损出局后同板块反手风险减半；**2 倍 ETF 睡觉前无论盈亏都平仓**（正股可以拿过半夜，起来盘后确认止损）。指标只走引擎口径（Wilder ATR，手算简单均值已废弃——差 9.5%）。**每次输出「多空转换地图」**：上方压力位（均线/昨高/20/60日高）= 做空参考（RSI14≥70 超买加分、≤30 超卖禁追空），下方支撑位 = 平空档、**收盘站稳 → 反多候选**（需 rule123 买法确认、均线之上才做多；贴噪声带需两日确认）——多空转换一律收盘确认，盘中触碰不算数。细则见 [us-short.md](references/us-short.md)；回归 `python test_us_short.py`。
14. **多空参考位是独立模块 `levels.py`**（2026-09-24 从 us_short.py 抽出，A 股美股通吃）：压力位/支撑位分区 + Wilder RSI14 超买超卖 + 多空转换地图，任何市场同一套判据（收盘确认、贴噪声带两日确认、超卖禁追空）。**T0 / 缩量回调模式下支撑档=回踩低吸买区参考、收盘破位=结构止损锚；做空时压力档=反抽空点参考。** CLI：`python levels.py 300404`（A股）/ `python levels.py SNDK --us`（美股）；模块调用 `from levels import levels_from_bars, resistance_levels, support_levels, rsi_stance, flex_map`（us_short.py re-export 保持旧名）。回归 `python test_levels.py`。

15. **压力位突破前瞻 `breakout_edge.py`**（2026-09-24 老罗提出：「临近压力位时压力位是拿来突破的，提前算突破盈亏比和概率」；A 股美股通吃）：临近带 ≤0.5×ATR 的压力位逐档给「突破买 RR」（entry=位、stop=−0.30×ATR、T1=上方下一压力位，无参照位 +2×ATR 估），并做**单票对称回测**（无未来函数：i 日只用截至 i 日的层）——3 日内收盘突破率（体系口径：一律收盘确认）、盘中触及率、放量日（≥1.3×前5日均量）/非放量拆分、真实突破样本 5 日先到先得均R/胜率。**样本 <10 = 样本不足仅供参考**；突破率 ≠ 突破买入赚钱（反例博济 300404 突破率 54% 但均 −0.40R；正例 SNDK 69%/+0.27R）——RR 与均R 双读数缺一不可。维度声明：板块强度、个股历史主力净额无稳定数据源未接入。CLI：`python breakout_edge.py 300404` / `SNDK --us`；回归 `python test_breakout_edge.py`。与 stock_character.py 突破后行为体检互补。

16. **顶部标志 K 线（五形态 + 变种）与「次日确认制」**（`top_signals.py`，2026-09-25 分两批定）：**第一批三根**是「冲高被卖回」的同族表达，差别只在实体厚薄与影线方向 —— **墓碑线**（几乎无实体 `body_r≤0.10` + 长上影 `≥0.60` + 收低位 `pos≤0.25`）、**射击之星**（实体小 `0.10~0.35` 根幅 + 长上影 `≥0.50` + `pos≤0.35`）、**上吊线**（长下影 `≥0.50` 根幅 + 小实体居上端 `pos≥0.55`、几乎无上影）；**第二批两根** —— **顶部十字星** `doji`（实体 ≤0.05 根幅，老罗定**次级**高危）、**大阴线** `big_yin`（阴线 + 实体 ≥0.55 根幅 + 收位 ≤0.30，**上影不设门槛** —— 2026-09-25 取消，理由见下，老罗定**顶级**高危）。**优先级固定**：墓碑→射击之星→上吊线→大阴线→**十字星最后**。四道闸门：① 形态（`classify_shape`）② 位置＝**是这一波最高点**（`is_wave_high`，左侧 60 根内无更高高点；**不通过的不叫顶部** —— 同形态在低位属**看涨**反转，避雷①）③ 量能档（巨量 ≥2.0×20日均量 / 放量 ≥1.5 / 平量 / 缩量）④ **次日确认**（其后收盘 < 信号收盘 ⇒ `confirmed`；其后收盘收复信号最高价 ⇒ `invalidated`）。★ **实证推翻了四条直觉**（393 只随机全A / 169,247 根日线 / 后 5 日，`python research_top_candles.py --universe sample` 可复现，各组用**同位置对照组**）：**形态当日无预测力**（同位置对照超额 +0.68%、t=1.59）；**「必须是这一波高点」这道位置闸门不成立**（闸门内 +0.68 vs 闸门外 +0.48 ⇒ 保留为标注 `is_top`，**不作否决依据**）；**「次日走弱」才是那个信号、且与形态无关**（有形态 −6.64% vs 无形态 −6.95%，**t=0.43**；反包组 +1.37% ⇒ 推翻成立）；**第二批两形态没有改变上面任何一条**（当日同样无预测力：大阴线 +0.06/t=0.11、十字星 −0.25/t=−0.40；走弱确认后 −5.91% / −8.66%，与对照 −6.95% 同量级）⇒ **五根一视同仁**。⚠️ **大阴线不得用「当日自确认」**（初版实现过、实证推翻已删）：当日 −0.28% 无信息，而 **80.9% 的大阴线其后会被反包（+1.75%）**。「顶级/次级」只是**显示标签**，**回测不支持它排序预测力**。⇒ **veto 默认 `confirmed`**（只在「波峰 + 次日走弱」否决 `recommend`；`veto="signal"`＝当日即否决，回测不支持、默认关），命中即 `recommend=False` + `top_signal_veto=True` + `pre_breakout.suppressed_by="top_signal"`；**未确认档只降档预警、不改 recommend**（同口径：波动系数 ≠ 准入闸门）；`_size_factor` 已确认→0 / 巨量未确认→0.5 / 放量→0.75。★★ **2026-09-25 第三批修订（老罗又报 5 个亲身案例：顺丰/茅台/石头/平安/比亚迪）**：① **取消大阴线的上影门槛**（原 `upper_r ≤ 0.20`）—— **数学上多余**（`upper_r = 1−body_r−pos`，而 `body_r ≥0.55`、`pos ≥0` ⇒ **`upper_r ≤ 0.45` 恒成立**，任何 ≥0.45 的上限都触发不到），且恰好把**最典型的顶部形态「冲高回落」排除在外**（茅台 2021-02-18 历史最高 2627.88 当天，实体 0.718/下影 0.037/收位 0.037/位置=波峰**全部达标**，**只因上影 0.245 被卡掉 ⇒ 引擎当时全程零反应**）；大样本（波峰大阴线 1,575 例）取消后确认率 **18.1%→22.5%**、确认后 −7.29%→−6.54%（同量级）、样本 **1.56×** ⇒ **不稀释、覆盖更完整**；变种改为三分 `textbook`/`long_body`/`spike`（**`BY_UPPER_SPIKE` 只作标签、不作门槛**）。② **射击之星的「下影≤0.15 / 收位≤0.35」暂不放宽**（比亚迪 2022-06-13 那个 358.86 的高点因此仍未命中）：放宽后确认率 13.4%→13.8% **不塌**、确认后 −6.51%→−7.13% **略更负**，但波峰候选 **1,512→3,249（×2.15，即全组合 `alert`/缩仓翻倍）**，且差异**统计上不显著**（SE≈1.5~1.7）⇒ **不因单一案例改阈值**（同 SNDK 跳空案的纪律），记为**已知未覆盖样本**。③ ★★ **结构性发现：四个「断头铡刀」案例里，唯一四要件齐备的那一根（石头科技 2021-06-23）恰恰不是波峰，而三个真波峰（顺丰 02-18 / 茅台 02-18 / 平安 11-30）四要件一个都不齐** ⇒ 「铡刀」（均线关系定义）与「顶部标志 K 线」（位置+次日行为定义）**两套定义互不对齐**，「一口气破几条线」不构成顶部判据（与断头铡刀零信息的结论互相印证）。④ **平安 2020-11-30 是确认制漏掉的一次历史大顶**：波峰 + 教科书射击之星 + 巨量 2.24×（成交额 ≈140.7 亿，老罗记的 143.16 亿对应均价 93.0、落在当日区间内 ⇒ 对得上），但次日反抽 +3.72% ⇒ 全程只 `alert`、**未升级 `block`** ⇒ 如实记录「确认制有必然代价」。**13 条避雷**（同名不同义：上吊线/锤子线、射击之星/倒锤子线只能靠位置分；形态当日不是判据；未确认≠顶部；**反包即作废**；无量不算数；**量能放大的是波动不是方向**；要看**收盘位**不看涨跌幅；近一字/异常振幅降级；样本不足不判定；**十字星方向中性**；**「巨量大阴线」≠「更准」**；**跳空放大波动不放大方向**）。★ **2026-09-25 追加：跳空 / 断头铡刀两组提名全部被否**（源自老罗给的 **SNDK 2026-06 教科书顶部序列** 逐根核对 —— 引擎抓 6 根、漏 4 根核心 K 线，共因是「跳空不参与形态判定」+「位置闸门只认绝对最高点、认不出右肩」，两点均为**刻意保留**）。1079 只 / 465,317 根日线实测：**跳空低开（−1.48% / −1.28%）与跳空高开（−1.66%）一样差、只有无跳空明显更好（+0.30%，t +2.46）**⇒ 跳空是**波动**维度（−5% 概率 16.6% ⇒ **42.9%~47.9%**，与「巨量」同构），**不接 veto**；**断头铡刀**（两连阴破全部短均线）**3674 例 +0.33%（t −0.10）**、对照「两连阴（全部）」+0.39%、标尺 +0.34% ⇒ **中性偏正，明确否定**；位置闸门**不放宽**（闸门内 −7.65% vs 闸外 −5.32%）。⇒ **形态族仍是五根，不加第六个**；「跳空低开大阴」在 A 股 1079 只仅 25 例（跌 ≤−8% 只 15 例），**美股个案不可照搬**。该序列已固化为回归 fixture `python test_sndk_top_sequence.py`（12 项，内置 84 根真实日线），**改形态阈值或位置口径会触发它失败**。接入＝**出口包装**（`plan_entry = apply_top_signal_gate(_plan_entry_core)`，闸门落在 T0 之后），全市场扫描/报告已落**第 16 列三态**（已确认·否决 / 待确认·预警 / 已推翻）。**筛选层与顶分型统一入口 `screen_exclude` / `top_fractal(s)` —— 本仓库 7 处重复实现已收敛。** ★ **单票硬指标 `top_verdict()`（老罗：「可以单独提取作为硬指标，我在跑个股的时候，如果出现这种要避雷」）**：独立 CLI `python top_signal_check.py <code> [--us] [--strict] [--json] [-v]`，**退出码 = level（`ok=0` / `alert=1` / `block=2`，多票取最大，取数失败 3）可直接用于流程拦截**；`battle_analyze` 终端摘要、HTML 报告（首屏「最高盈亏比路径」下方独立卡片 + 徽标 + 一页汇总）、`probe_intraday`（A 股 `probe` / 美股 `probe_us`）三处**全部读同一出口**，不得各自解释 `plan["top_signal"]`；**已推翻（invalidated）回落成 `ok` 不报预警**（报成预警就是把方向说反）。细则见 [top-signals.md](references/top-signals.md)；回归 `python test_top_signals.py`（**47**）+ `python test_top_signal_gate.py`（9）+ `python test_top_signal_check.py`（15）+ `python test_sndk_top_sequence.py`（12）+ `python test_lj_top_sequence.py`（12）+ `python test_casebook_top_sequence.py`（**34**）。

17. **「叠加概率」七层计数 `confluence.py`（2026-09-25 老罗分享「Stacking Probabilities」后新增）**：把「一笔交易对上了几层有利因素」从经验判断变成**可复现的计数**，直接回答「这单是单因素还是多因素」。七层顺序固定（= 推文漏斗顺序）：**整体市场 → 板块主题 → 领导者 → 催化剂 → 形态 → 量能 → 执行**。判据**全部锚在既有规则上**（不引入本仓库没验证过的新标准）：市场 ≥55（= `market_sentiment.LEVELS` 的「偏强」线）；板块 = 池层共振 ≥2 只（= `watch_cn` / `pool_us` 口径）；领导者 = 本票 20 日涨幅 ≥ 同板块最强者（= 龙头定义「近期涨得最好」）；催化剂 = 定性（模型写入，缺省不计）；形态 = `recommend=True` 且 `mode` 不是 `wait/none`；量能 = RVOL20 ≥1.0 且涨/跌均量比 ≥1.0（<1/1.15 = 派发）；执行 = `R→t1` ≥1.5 且风险 ≥0.25×ATR（= 买入上限闸门与突破类噪声带两条既有阈值）。
    ⚑⚑ **性质：只用于排序与解释，不作准入闸门、不缩放仓位**（与 `market_sentiment` 同一分工 —— 软约束只换做法）。**「全对齐」不等于可买，「少对齐」也不等于不能买** —— 能否决交易的只有硬约束（钱不够 / 涨停买不到 / 结构已坏 / 顶部已确认 / 标的总否决）。顶部标志 K 线**单独放 `veto` 字段，不与七层相加** —— 一个是否决权、一个是解释力，混起来就分不清了。
    ⚑ **`unknown` 层不计入分母，但必须在输出里可见**（终端 / HTML / 池层段落三处都打印），且「部分缺数据」**不得**当作「不达标」（量能、执行各缺一项时只按有的那项判，并在依据里注明缺了什么）—— 分母静默缩水等于把「没数据」伪装成「对上了」，这是本仓库最贵的那类错误。
    三个出口：① `battle_analyze` → `summarize()` 终端 + `analysis.json["confluence"]`；② `report_render` 首屏区块（**板块与催化剂单票报告算不出**，可由 `notes.json` 的 `sector_count` / `sector_bucket` / `sector_peers` / `catalyst_has` / `catalyst_note` 注入，不写就记「无数据」而非留白充数）；③ 池层 `watch_cn` / `pool_us` 报告末段【叠加计数】按对齐层数排序（**池层是唯一拿得到「板块共振」的地方**，所以板块层只有在这里才算得出来）。CLI：`python confluence.py <analysis.json> [--market-score N --sector-count N --sector-bucket X --catalyst yes|no]`。市场层自动去 `data/sentiment_<基准日>.json` 找当日情绪分（**日期必须严格一致，隔日不许冒充当日**；池层全缺时会主动提示去跑 `market_sentiment.py --out-dir data/`）。回归 `python test_confluence.py`（**100 项**）。

18. **飙股体检 `gates/surge_pick.py`（2026-09-26 老罗给出「飙股」定义后新增）**：老罗定义 —— **飙股 = ① 业绩超预期增长（**非必要条件、大加分项**；业绩不好也可能大涨）② 股性好（容易大涨或涨停，≈ 高 beta）③ 板块领头羊**。本工具把这三条**自动化**，回答「给一只票，它是不是飙股、它所在的板块里谁是领头羊」。CLI：`python gates/surge_pick.py <code> [--boards N --top N --pool N --json --no-net]`（`--no-net` 只算股性）。
    - **① 业绩**：走东财 `datacenter-web` 的 `RPT_PUBLIC_OP_NEWPREDICT`（业绩预告）。⚠ **预告口径只是代理、不是「超预期」** —— 真正的「超预期」要卖方一致预期，本仓库没有该数据源，输出里必须把这句话打出来；预告向好只记**大加分项**、**不构成否决**（同仓纪律：加分项 ≠ 闸门）。同一报告期常有多行（扣非续亏 / 营收预增 / 归母扭亏 同时存在），**必须取最新报告期并优先 `PREDICT_FINANCE_CODE == "004"`（归母净利润）**，否则会揉出一个假的「混合 −16.8%~19.4%」（688361 实测暴露）。
    - **② 股性（弹性）**：近 win 根（默认 250）自算 —— 涨停次数（涨跌幅上限按板块：ST 5% / 双创 20% / 北交所 30% / 主板 10%）、≥10% 大阳次数、≥5% 大阳次数、ATR%、`beta`（对板块指数 60 日收益的最小二乘斜率）。三档：**活**（易大涨/涨停，弹性好）/ 中 / 钝。
    - **③ 板块与领头羊**：东财 `slist/get?spt=3` 一次拿个股**全部**所属板块 + BK 代码（`clist/get?fs=b:BKxxxx` 取成分、`kline/get?secid=90.BKxxxx` 取板块指数日线；**东财必须 http 优先，https 会 RemoteDisconnected**）。先剔除**指数/风格/资金/地域/事件标签**（那不是产业概念），再按成分家数**从少到多**挑 —— **只有成分 8~400 家的才用来定领头羊**（成分 522 只的「电子」定不出龙头，25 只的「半导体设备」才行），**全部所属板块也一律显示出来**。领头羊排名口径**必须沿用** `research/screen_wind_dual_2026-09-25.py`，不得新造：**动量分 =（个股 20 日涨幅 − 板块指数同期 20 日涨幅）+ 距 60 日高 + max(0, 距 MA20)**；「距 60 日高」天然是负数，**直接相加**（老罗口径：负得越多扣越多，**不许取负变加分**）。
    - ⚑⚑ **三条判据都是「加分 / 排序」，不是准入闸门**（同仓既有分工：波动系数 ≠ 准入闸门）。**只要板块本身在退潮（板块 20 日为负），输出必须注明「此时排名靠前也不算真领头羊」** —— 只按板块内分位排名会把「矮子里拔将军」说成龙头（实测 688361 在半导体设备内第 3/13、分位 85%，但板块 20 日 −2.4% ⇒ 只够「边缘」）。判定：**分位 ≥0.8 且动量分为正 ⇒ 是**；≥0.6 ⇒ 边缘；其余 ⇒ 不是。
    - ⚠ 两个必须随输出一起说的限制：**领头羊排名是抽样的**（先按东财 60 日涨幅取前列候选再算 20 日口径，**不是全板块排名**）；**业绩是预告代理口径**。仅 A 股（港股/美股直接返回错误，不硬凑）。
    - 报告顺序与【结论】顺序固定为 **① 业绩超预期 → ② 股性 → ③ 板块领头羊**（= 老罗的定义顺序）。回归 `python tests/screen/test_surge_pick.py`（**24 项**，全离线纯函数）。

19. **牛旗（上升旗形）· 单票硬指标 `gates/bull_flag_check.py`**（2026-09-26 老罗给 MRNA 案例后新增）：老罗原话「增加识别突破后的旗形整理案例-牛旗的图形 MRNA，**过牛旗即是买点**」+「牛旗形态的突破，应该和平台突破**并列是 T1 买点**，和大阳后缩量回踩，是同一种逻辑……**大阳之后缩量不代表次日一定能涨，但是如果涨了过了牛旗就是买点**」。
    - **CLI**：`python gates/bull_flag_check.py <code> [--us] [--json] [--data x.json] [-v] [--holding|--flat]`。**退出码可直接用于流程拦截**：`0 = 无牛旗`、`1 = 旗面成型·未过线（给埋伏单）/ **贴线待突破** / 过线已久（不追）`、`2 = **已过牛旗（买点成立）**`、`3 = 取数失败`、`4 = **旗形无效**（违反了下面两条硬条件 ⇒ 不作牛旗买点）；多票取最大。输出：旗杆（起止/根数/涨幅）、旗面（根数/回撤 % 上限 66%）、**失效线（旗杆大阳最低点）+ 旗面低点 + 余量 ×ATR**、**整理上限 20 根**、旗面线与锚点、现价距线 ×ATR、过线第几根、**变盘临界（地量小实体）**、买区、结构/硬止损、牛旗埋伏单 / 贴线待突破条件。
    - **★★ 两条硬条件（老罗 2026-09-26：「旗形整理最好不要跌破大阳线的最低点，否则无效，然后整理天数不要太长（不超过 20 日）」）**：① **旗面收盘不得跌破旗杆「最后一根大阳」的最低点**（`bull_flag_anchor` 定位大阳 + 复用 `yang_floor`：普通大阳=当日最低价、真一字/跳空=缺口下沿）。**收盘破才算破** —— 盘中插针破、收盘收回**不判无效**（实测「盘中破收盘守」那组过线后 5 日表现与全程守住组无差别，砍它是白砍；与仓库既有「结构止损=收盘破」口径一致），只标 `undercut` 供报告提示。② **整理天数（旗面自身根数）≤ 20**（`FLAG_LEN_MAX`）。**被任一条否掉 ⇒ 形态无效**（`detect_bull_flag` 返回 None；`diagnostic=True` 时回 `{valid:False, invalid_reason}` 供 CLI 说清被哪条否掉）。MRNA 失效线 = 08-19 低点 **114.46**，旗面低 128.61（**+1.1×ATR**）⇒ 通过；回归 **36 项**。
    - **★ T1 内序改为「平台突破 ⟷ 牛旗突破（并列）→ W底 → 沿线回踩」**（2026-09-26 老罗定）。理由：① 平台与牛旗只是整理段上沿「水平 vs 下倾」的两种几何，同一件事；② **形态尺度** —— MRNA 2026-09-17 的 W底（9 根）**嵌在旗面内部**（19 根），大结构压小结构；旧序取了 W底颈线 156.42，而「过牛旗」的真实触发位是旗面线 145.28（更低）。改后当日 mode = `flag_tl_break`。
    - **★★ 修掉两个真缺陷**：① **`flag_len` 改为「旗面自身长度」**（旗杆末端 → **突破前最后一根**），**不含突破后连续站线的根数**。旧口径 `n-1-pe` 随时间无限增长，撞上「旗面 ≤20 根」闸门 ⇒ **在突破后第二天把形态整段判死**（MRNA 旗面 19 根：09-17 过线当天 flag_len=20 通过 → 09-18 变 21 → `detect_bull_flag` 返回 None）；且与下游 `flag_tl_break` 要求的 `days_above_tl ∈ [1,3]` **自相矛盾**（要在线上连续 3 根，flag_len 至少 21 ⇒ 该窗口对 ≥18 根的旗面不可达）。修后旗面长度稳定（MRNA 恒为 19），新鲜度只由 `days_above_tl` 管。② **旗面线锚点必须落在旗面内**（突破后的新高也会进 `Hs`，被选成 `pa` 会把线拖到突破价上 —— TEM 家族「形态在突破当天自我消失」）。
    - **★ 与「大阳后缩量回踩」合并口径**：两档是**同一件事的两个阶段** —— **阶段一（未过整理上沿）= `impulse_pause`**，**缩量到位只说明整理成立，属预测性买点、试错仓**（buy_zone 带 `confirm_edge` / `confirm_kind` / `confirm_tier` 指明该盯哪条确认线）；**阶段二（过沿）= T1 接管**（水平沿→`platform_break`、下倾→`flag_tl_break`）。⇒ **「缩量」不是买点，「过沿」才是**；两档不会同时给出矛盾买区（T1 判定排在前）。
    - **★ 未过线给 buy-stop 埋伏单**（`pre_breakout_flag_order`，`anchor=flag_tl`）—— 「过牛旗即是买点」的**可预挂**形式（老罗：「很多时候我们并不能第一时间买到突破」）。触发价 = **下一根**旗面线值 +0.05×ATR（线在下移，用当日线值会把单挂高），硬止损 = 线下 1.0×ATR。与平台沿版、泛化斜线版（`down_tl`）并列挂出、先到先做；三者重合时去重（MRNA 09-15 旗面线 = down_tl = 148.98；旗面线通常更低：09-16 是 146.39 vs 148.16）。
    - **★★ 「贴线待突破」+「地量小实体」（2026-09-26 老罗给天能重工 300569 案例后新增；回归 `tests/entry/test_300569_bull_flag.py` 27 项）**：老罗原话「**2026-09-23 收地量十字星，次日就有极大可能变盘，果然次日就画了 N 字，那么次日过牛旗的点位就是买点**」。**① 贴线待突破**：旗面线**每日下移**，于是「收盘贴线」必然踩中旧址守 `last_c >= line_next`（300569 09-23：当日线 4.99 > 收 4.94 = 真的未站上；**次日线 4.940 = 收 4.940** ⇒ 被误判「已站上」，埋伏单在最接近突破的那天整档消失）。但**实测不能简单放宽**：该档 n=213「线自己贴上来」的过线（次日过线率 71.8%）过线后 5 日中位 **−1.81%**、胜率 43.8%（正常组 +1.30% / 56.1%）＝**廉价过线**（不需要价格涨）。⇒ 改由 `flag_knife_edge` 出预警：**次日开盘不低开（≥线值）且量 ≥1.5×近5日均量**才买（放量组 **+2.76%**/52.6%；缩量组 **−1.83%**/42.5%）；buy-stop 表达不了量能条件，故此档**只给盯盘条件、不给挂单价**。300569 09-24 全中（开 4.94 不低开、量 2.74×、收 5.28 +6.88%）。**② 地量小实体（十字星）= 变盘临界**（`dry_small_body`，实体 ≤**0.15×ATR**）：1511 个「大阳后缩量」实例里，次日 P(涨≥2%) **31.3%** vs 26.5%、过线后 5 日中位 +1.00% vs −0.18%（滞后 5 日 t=+2.4）；各档单调、**与振幅无关**（再叠「振幅≤0.5×ATR」反而变差）⇒ **只作标注、不作闸门**（不改 recommend）。
    - **回归** `python tests/entry/test_mrna_bull_flag.py`（**26 项**；fixture `tests/entry/fixtures/MRNA_bars.json` = 2025-11-03→2026-09-25 共 225 根真实日线，nasdaq 源）。案例事实：旗杆 **08-19 单日 +176.97%**（收 174.38，量 199.3M）；旗面 **19 根**（08-20~09-16，量从 99.5M 缩到 7.9~15.4M，最低 128.61，回撤 **39.1%**）；**09-17 收 158.07（+8.55%）站上旗面线 145.28 = 过牛旗**。

## 按需读取

只读取当前任务所需文件，不要预加载全部 references：

- **准备追消息 / 抢开盘 / 回补卖出前**：必读 [trade-lessons.md](references/trade-lessons.md)。
- **选股 / 建股池 / 有人在问"这票值不值得看"**：读取 [stock-selection.md](references/stock-selection.md)（三因子**参考清单**：总市值 ≤1000 亿 → 股东户数 → 距 52 周高点；**均为参考，非门禁，趋势为准**）。
- **判顶部 / 出现长上影·长下影·十字星·大阴线 K 线 / 持仓要不要走 / 跑个股要避雷**：读取 [top-signals.md](references/top-signals.md)（五形态 = 墓碑线 / 上吊线 / 射击之星 / 顶部十字星 / 大阴线 + 次日确认制；**形态当日只预警，次日走弱才否决；反包即推翻**；单票硬指标 `python top_signal_check.py <code>`，退出码 0/1/2）。**跳空与「断头铡刀」两组提名 2026-09-25 已被大样本否定**（跳空是波动不是方向；断头铡刀中性偏正）⇒ **别把「跳空大阴」「两根铡刀破均线」当顶部依据**（§4.3）。
- **问「有没有牛旗 / 过牛旗了没有 / 突破后整理算不算旗形 / 大阳后缩量该不该买 / 线已经贴到价上了算不算过线」**：规则 19 + [strategy-modes.md](references/strategy-modes.md) §3（牛旗）与 §5（大阳后缩量回踩）。**「缩量」不是买点，「过整理上沿」才是**；**「线自己贴上来」的过线（贴线档）必须先放量才买**。单票体检 `python gates/bull_flag_check.py <code> [--us]`（退出码 0=无 / 1=成型未过线·**贴线待突破**或过线已久 / 2=**已过牛旗·买点成立** / 4=**旗形无效**：跌破旗杆大阳最低点 或 整理超 20 根）。可选读 [entry-odds.md](references/entry-odds.md) 算赔率。

- **完整单票报告**：读取 [research-report.md](references/research-report.md)、[strategy-modes.md](references/strategy-modes.md)、[entry-odds.md](references/entry-odds.md)、[risk-exit.md](references/risk-exit.md)、[data-operations.md](references/data-operations.md)、[output-pitfalls.md](references/output-pitfalls.md)、[report-generation.md](references/report-generation.md)。
- **只问买点/止损/持仓处置**：读取 [strategy-modes.md](references/strategy-modes.md)、[entry-odds.md](references/entry-odds.md)、[risk-exit.md](references/risk-exit.md)。
- **盘中信号/分钟复盘**：读取 [intraday.md](references/intraday.md)、[risk-exit.md](references/risk-exit.md)、[data-operations.md](references/data-operations.md)；生成正式报告时再读 [research-report.md](references/research-report.md)。
- **股池扫描/批量复盘**：读取 [strategy-modes.md](references/strategy-modes.md)、[risk-exit.md](references/risk-exit.md)、[data-operations.md](references/data-operations.md)。
- **只问基本面/估值/扫雷**：读取 [research-report.md](references/research-report.md)、[risk-exit.md](references/risk-exit.md)。
- **生成或更新报告**：额外读取 [output-pitfalls.md](references/output-pitfalls.md)、[report-generation.md](references/report-generation.md)。
- **读写资料库持仓（真相源）**：读取 [library-writeback.md](references/library-writeback.md)。持仓/止损/状态的**真相源是资料库「股池配置」表**，本地池只是缓存 —— 跑票前先 `sync_pos_from_library.py`（读），要改库用 `library_pos.py`（写，支持 list/find/set/note/add，`--dry-run` 预览）。

## 不可省略的核心规则

- 只做六种 `mode`，禁止自拟第七种；T0 均线收复+过昨高优先级最高。
- 模式优先级 T1/T2 不等于止盈档目标1/目标2，报告必须写全称。
- 赔率优先于形态；必须回答知错位、错了亏多少、对了赚多少。距入场 ≤0.5×ATR 的压力、下降趋势线的前高、旗形和 W 底头顶的平台高点，都是要穿过的门。突破之后空间打开，目标用门后的下一档；没有更远的墙就用入场+6×ATR。创新高且上方无墙时空间已打开，目标用入场+6×ATR 这个口径，盈亏比可以很高。推荐档分开：A 股仍取可预挂里 R 最大的回踩；美股追涨档自己 R≥1.5 就可以做。
- 报告量价结论分别输出“当日判定”和“近20日判定”，并给综合结论与证伪位。
- 主标的（以及用户明确要求完整分析的同行）必须扫大额解禁、减持、立案调查、**标的自身的「利好公告反应史」**（上次同类公告当日是否高开低走 / 放量收阴）及重大利空。
- 双轨打分固定为投资价值与短线博弈各 0–10 分，禁止合成总分。
- **选股参考清单（三因子：以趋势为准，仅作排序与风险提示，**均非门禁**）**：① **市值** —— 粗筛用**总市值 ≤ 1000 亿**（老罗口径「大票」= 超 1000 亿，超了只是**不优先**，非禁买）；`ExtInfo.FreeLtgb × 现价` = 自由流通市值仅作**弹性细看**的补充视角（锐捷 301165 总市值 1,249 亿属大票，但浮筹仅 149.9 亿 = 12.0%）。② **股东户数**（`tdx_security_deep_info`）—— **必须叠趋势读，单看增减无意义**：上升趋势中户数增加是常态（锐捷 2025 主升段股价 +54% 而户数 +83%）；只有**横盘/下跌 + 户数增**才偏派发，且只作**警惕项**不作否决。③ **距 52 周高点 ≤ 15%** ⇒ 上方套牢盘偏薄（**同样仅参考**：套牢盘是**动态量**，会被下跌换手消解 —— 静态看会让「下跌过一段的票永远涨不起来」，与突破类买法自相矛盾；只要放量站上关键位，套牢区被吃掉反而是买点）。**三条均为参考，任一不通过都不构成否决**，能否决交易的只有硬约束（钱不够 / 涨停买不到 / 结构已坏）。细则与实测样本见 [stock-selection.md](references/stock-selection.md)。
- **顶部标志 K 线（五根：墓碑线 / 上吊线 / 射击之星 / 顶部十字星 / 大阴线）已确认（波峰 + 次日走弱）⇒ 否决 `recommend`；形态当日只降档预警、不改 recommend**（形态当日 t≈1.6 无预测力，别拿当日形态当既成顶部）；**五根一视同仁** —— 大阴线**也不许**「当日自确认」（实证推翻：当日 −0.28% 无信息，80.9% 其后被反包 +1.75%）；**反包（收盘收复信号 K 线最高价）即推翻**；同形态在低位属**看涨**反转、**不算顶部**（十字星、大阴线尤其如此）；无量/缩量长影只预警不否决。**跑个股时先过一遍硬指标**：`python top_signal_check.py <code>`（退出码 `0/1/2`），命中 `block` 即避雷（持仓走减仓腿、空仓禁买）。细则见 [top-signals.md](references/top-signals.md)，实现 `top_signals.py`。
- 用户点名个股时必须找 1–3 只同板块龙头做轻量对照；除非用户显式要求，不得对同行运行完整单票流程。
- **「叠加概率」七层计数（`confluence.py`）只用于排序与解释，不作准入闸门、不缩放仓位**：它回答「这单对上了几层（整体市场 / 板块主题 / 领导者 / 催化剂 / 形态 / 量能 / 执行）」，**不回答「能不能买」**——「全对齐」不等于可买，「少对齐」也不等于不能买。顶部标志 K 线是**否决权**，单独列 `veto` 字段，**不与七层相加**；`unknown` 层不进分母但必须显示出来。
- **给「最佳价格」时必须同时给「开盘作战方案」（`open_playbook.py`，2026-09-25 老罗定）**：**挂单价只是计划的一半**，另一半是「次日开盘价落在哪里时我该做什么」—— 不预先写死，临场就靠情绪决策，那正是「手痒模式」的入口。按开盘价 O 相对挂单价 E、止损位 S 出**六档**：① 涨停/一字 ⇒ 买不到（硬约束）、撤单；② 跳空高开 >+2% ⇒ **不追**、挂单原样保留（**不上移**挂单价）、全天不回踩就当日放弃；③ 小高开/平开在挂单价上方 ⇒ 不成交、等回踩；④ **★刚好符合**（挂单价附近）⇒ 按计划成交；⑤ 深低开但仍在止损上方 ⇒ 成交但必须辨性质（缩量回落可接受 / 放量低开需 15 分钟内站回挂单价，否则次日减半），**不因便宜加仓**；⑥ **低开破止损 ⇒ 不接飞刀、开盘前撤单**（A 股 T+1：当日买入当日不能卖 ⇒ 当日止损腿不存在 ⇒ 硬约束禁买）。**必须回答集合竞价买不买**：A 股默认**不买**，理由三条 —— 竞价无分时结构、**T+1 当日买入当日止损腿不存在**、开盘价常是当日极值的一端；深交所 9:15–9:20 可撤单、**9:20–9:25 不可撤**（深证会〔2016〕138 号）。另附时点清单（09:25 定档 / 09:30–09:45 观察窗不动作 / 10:00 首检 / 14:30 尾盘收盘位 / 收盘破止损 ⇒ 次日 09:25 挂限价卖）。美股不套这六档，和做空一样五档：R≥3 强烈推荐、2≤R<3 推荐、1.5≤R<2 合格、R<1.5 不推荐、价格≤止损放弃。前三档买，后两档不买。盘前、盘后、夜盘到价算数。日线结构只认正规时段收盘。半夜不盯盘：正股拿着睡觉，起来盘后确认止损；2 倍 ETF 睡前无论盈亏都平仓。报告插槽 `OPEN_PLAYBOOK_BLOCK`。⚠ 档位默认取 `odds_recommend`，**人工改过锚的档位必须在 notes 的 `open_playbook` 里覆盖**（`{"last","entry","stop","target","shares"}`）—— 不覆盖会拿引擎默认锚生成一份错的方案（新强联引擎默认止损 24.44 即已判为过宽不可用）。CLI：`python open_playbook.py <analysis.json> --notes <notes.json> [--md]`。
- **宏观（利率 / 收益率 / 加息概率）只作背景与波动系数，不作入场闸门**：**加息不代表不涨** —— 能否参与**一律以个股与板块自身为准**（趋势 / 结构 / 量能 / 赔率）；**禁止**用宏观对整条产业线做「默认暂停新建仓」式的全局否决（旧规则「高久期承压 ⇒ 硬件线默认暂停」已于 2026-09-25 按老罗指示删除）。宏观偏紧时用「**等回踩 + 仓位缩小**」承担波动，不是禁止买 —— 与「68% 分位」同一分工：**波动系数 ≠ 准入闸门**。能当闸门的只有**标的级**（财报窗口 / 顶部已确认 / 结构已坏）与**硬约束**（钱不够 / 涨停买不到）。

## 同板块 / 概念龙头对照（强制）

用户给出一只个股并要求跑本 skill 时，**不得只分析这一只**。必须完成：

1. **定主线**：该股当前归属的 1–2 个主板块或主概念（如核级锆、固态电池、消费电子、AI 算力；美股则同业 peer）。写清依据（业务/近期驱动，不是随便贴标签）。
2. **找领头羊**：在该板块/概念里找出 **1–3 只龙头**（领头羊可以不止一只）。  
   ★ **自动识别用 `python gates/surge_pick.py <code>`**（2026-09-26 新增，见第 18 条）：它一次给出该股**全部**所属板块（剔过指数/风格/资金/地域/事件标签、按成分家数从少到多排列）+ 每个候选板块里的动量分排名与主标的分位，并显式标注「板块本身在退潮时排名靠前也不算真领头羊」「排名是抽样的」。**不得**只看「板块内分位」就宣布龙头。
   **龙头定义（强制）**：不是市值最大，而是近期**涨得最好、主动性最强、趋势最好**的标的。综合看：阶段涨幅与相对强度、是否领涨/带节奏、量价主动性（先于板块动、回撤浅、突破干净）、日线趋势结构是否更干净。市值只能当备注，不能当入选标准。
3. **同行默认轻量对照**：候选同行先用批量报价或同一份日线快照筛选，只保留 1–3 只近期最强者；默认仅输出阶段涨幅/相对强度、趋势状态与“主标的 vs 龙头优先做谁”。**不得默认**为同行追加分钟线、完整基本面、逐项扫雷、双轨打分、仓位止损或独立报告，也不要求跑 `rule123`。只有用户明确说“同行也跑完整分析 / 同样跑 skill”时，才对入选同行执行与主标的相同的完整流程；此时各票独立取数仍须并行且每票只取一次。
4. **开仓优先级**：  
   - 轻量对照只决定**优先研究谁**，不得直接生成同行下单计划；主标的是跟风/杂毛且龙头明显更强时，先提示改看龙头。
   - 用户要求同行完整分析后，若龙头有可执行 mode → **优先做龙头**，主标的降级为观察或更小试错。
   - 主标的本身就是龙头之一 → 正常执行，仍列出其他龙头作对照。
   - 主线已经启动（如国产替代下的半导体设备）→ 领头羊按突破跟随猛干：站上均线当日或次日、以及过前高，都是主升前的买点，不改成等深回踩。吃到任意一段即可。  
   - 龙头趋势弱，或完整分析为 `wait`，而主标的只是「看起来便宜」→ **不因为便宜改做杂毛**。
5. **输出必须有**：`主线概念` · `龙头名单（1–3）+ 为何是龙头（各一句）` · `主标的 vs 龙头：优先做谁`；**判「龙头是否名副其实」用 `surge_pick.py` 的动量分与板块 20 日方向说话，不靠市值/名气**。

**禁止**：用市值/名气冒充龙头；默认把多个同行扩成多个完整单票任务；发现主标的不是龙头仍按同等仓位推荐买入。

---
