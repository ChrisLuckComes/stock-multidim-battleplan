---
name: stock-multidim-battleplan
description: 美股/A股个股的多维度分析 + 作战计划。当用户要求多维度分析、买卖计划/作战计划、该不该买/怎么买/止损止盈时使用。卖方级六维（基本面/估值/技术面/资金面/催化剂/风险）叠加 Swing 纪律（pullback/breakout、低吸/尾盘/不追高、止损 ATR+结构双校准、强股回踩 VWAP/0.382、高β不双持、Conviction vs Swing 二分）。强制产出目标价与退出条件（T1/T2）。结构门控：延续走回踩/突破；123 三项全过则无论方向都可推荐；下跌且 123 未完成才硬否决。
disable-model-invocation: true
---

# 个股多维度分析 + 作战计划（stock-multidim-battleplan）

> **何时用**：用户要求对某只股票做"多维度分析""作战计划""该不该买/怎么买/止损止盈"。本 skill 把"研究"和"下单计划"合成一套可复用流程，输出一份 HTML 研报 + 对话摘要（无文件写入环境时改输出 markdown 研报）。
>
> **前置依赖（环境自适应——两端都可用）**：
> - **场景 A · WorkBuddy 等已装 `wb-finance-skill` 的环境**：金融数据优先走 `agentic_search`（wb-finance-skill 硬约束），禁止凭记忆裸答。分析前先加载 `wb-finance-skill` 读红线 + 按场景加载其 references（stock-deep-research / valuation-pricing / trade-plan / stop-discipline）。本仓库的 `fetch_market.py` 在该环境下**可选**（你已有更好的数据源）。
> - **场景 B · Cursor / 通用 AI Agent（无 wb-finance-skill）**：用本仓库自带的 `fetch_market.py` 取数（零密钥、A 股走东方财富、美股走 Yahoo），再调用 `rule123.py` 做方向感知结构判定。
> - **两端共用**：结构判定只用 `rule123.py`。不使用 Gamma / GEX / 期权墙。

## 核心目标
研究端回答"市场为什么买它、未来还买不买"；计划端给出"具体条件 + 具体动作"的可执行单，而非"逢低布局、注意风险"。

## 分析六维（必须逐项覆盖，关键数据带来源+时点）
1. **基本面**：最新季报（营收/净利/EPS 同比环比、是否创纪录）、业务结构（哪块决定估值、是否真壁垒）、用户/资产增长、一次性损益需剔除还原经营性 EPS、资产负债与 ROE。
2. **估值**：PE/PB/PS/EV-EBITDA 选合适框架（高成长用 PE/PEG/PS）；**拆解"股价已计入什么预期"**；历史纵向分位 + 同业横向；重估/杀估值触发因素。
3. **技术面**：均线排列、MACD、KDJ（J 是否 >90 超买）、RSI、布林带（是否破上轨）、近期涨跌幅、量比（突破日量能是否 >1 倍确认）、β。
4. **资金面**：成交额/换手/量比、空头持仓比例、回购/增发/可转债、机构与内部人动向。
5. **催化剂**：业绩、产品、监管、国际化、下一次财报日期（本地+北京双标，盘前/盘后）。
6. **风险**：短期 vs 中期分开（估值/周期/监管/竞争/利率），不泛泛提示。
不使用期权 Gamma / GEX。支撑阻力只用价格结构、均线、VWAP、斐波那契。

## 交易位置判定（先定位置，策略完全不同）
| 位置 | 特征 | 策略 |
|---|---|---|
| 低位启动 | 长期跌后横盘/放量突破 | 30日低+放量 |
| 趋势中继 | 上升中回调 | 5日线附近+缩量 |
| 高位加速 | 连续大涨后加速 | 远离5日线+放量 → **不追高** |
| 高位震荡 | 高位横盘 | 5日走平+高换手 |
| 下跌反抽 | 下跌后反弹 | 5日下+无量 |

## 布林上轨解读（关键修正·避免误判尖顶）
布林上轨触及**本身不是卖出/尖顶信号**（John Bollinger 原话：tags of the bands are not signals）。同一根上轨线对应两种相反 regime，必须区分：
- **主升浪 / band-walking（贴轨走）**：价格连续 3+ 日贴着上轨上行、带宽（bandwidth）同向扩张、ADX>25 且升、量能持续 = 趋势延续；此时**上轨变支撑**，属用户框架里的 Breakout 突破确认买（回踩上轨/VWAP 浅买，不追尖刺）。
- **尖顶 / blowoff（派发）**：单日爆量刺穿上轨、次日速回布林内侧、ADX 弱或量能峰值后枯竭 = 派发，等更深回踩。
- **纪律 reconciled（两层，勿混）**：用户"不追高"= 不追尖刺（wick），≠"趋势不能买"。
  - **① Breakout 收盘确认买（动量买点本身）**：突破日**收盘站上突破位/布林上轨即为 trigger**，尾盘或盘后入场即合规动量买点（契合"尾盘确认"+"不追盘中尖刺"），止损设于突破位（缺口下沿）下方结构位，**无需再等 1–3 日**——收盘确认本身就是确认动作。误将"突破日收盘确认买"写成"必须等 MA20 深回踩"= 与 Breakout 原则直接矛盾，强股里会踏空整段主升（对照"强股回踩锚 VWAP/浅回撤"原则）。
  - **② Pullback 浅买（突破后的加仓/优化成本）**：若已突破、想加仓或优化成本，才用 VWAP/0.382 浅回撤（回踩上轨/突破位不破）= band-walking 浅买；判不清 walk/blowoff 时等 1–3 日确认。
  - **ETF 比单点高β 个股更易走出可持续 band-walk**（成分分散、波动平滑），单日 +X% 后是否 walk 需看后续贴轨持续性。

## 方向感知结构门控（强制调用 `rule123.py`）
先判趋势方向，再决定怎么写路径。**不用 MA60**（与 EMA10/SMA20/SMA50 栈重复）。**>2×ATR 不追**压在两条路径之上。**HH 两两比较不作门控**（只当备注）。

cond2 定义：自最近摆动低点 P1 以来未再创新低（不是 P1>P0 的两两 HL）。结构完好 = 收盘 > P1 且 cond2。

趋势判定（脚本输出 `regime`）：
1. 收盘在 **SMA20 之上**，且 **(EMA10 > SMA20 > SMA50 或 SMA20 上行)**，且结构完好 → `continuation`（上升延续）。
2. 收盘在 SMA20 下，或 cond2 坏了（自 P1 后再创新低）→ `reversal`（下跌反转）。
3. 其余 → `mixed`（观察）。

**123 三项全过优先于 regime**：cond1（趋势线突破）+ cond2 + cond3（收盘 > R1）全过 → `recommend=True`，与方向标签无关。延续标 `breakout`（RVOL≤1.5 则 `breakout_lightvol`，文案打折）；反转标 `reversal_123`；mixed 标 `base_breakout`。

**上升延续（continuation）且 123 未完成**：不用底部 123 否决整笔；**cond1 不作门控**。研报拆成两张路径卡，**不要把上升路径叫「123 门控」**。
- **买点 A（回踩 / Pullback）**：cond2 + 不破 P1；再叠加 **VWAP / 0.382 / EMA10 + 缩量**。脚本 `setup=pullback` 时 `recommend=False`，由研报再判现价是否落在买区 A。
- **买点 B（突破 / Breakout）**：cond3（收盘 > R1）为触发，须 **RVOL > 1.5** 才算确认。`setup=breakout` 才 `recommend=True`。

**下跌反转（reversal）且 123 未完成**：走 Vic 底部 123 硬门控，三项不全过 → 不推荐。研报输出「123 趋势法则判定」卡。

**mixed 且 123 未完成**：只观察，不写推荐买入。

## Swing 双分类（用户框架，必须贴标签）
- **Pullback 回踩确认买**：买在尖顶/趋势回踩缩量企稳位（非尖顶追高）。
- **Breakout 突破确认买**：买在跳空/平台突破收盘确认位（缺口下沿或突破位当结构止损）。
- 先判清本笔属哪类，再按对应纪律评估买点。

## 入场哲学二分：Conviction 买 vs Swing 买（用户框架·关键修正）
同一套"买点三模式/等回踩"**不能无差别套所有票**。下判断前先分本票是**租趋势**还是**买生意**：
- **Swing / Momentum 技术买（租趋势）**：HOOD、DE、LLY、TEM 类——靠技术结构/动量吃饭，破位即失效。纪律 = **等回踩确认（VWAP/0.382/MA5 企稳）或突破收盘确认**，不盘中追尖顶。"不追高"防的是这种。
- **Conviction / Franchise 基本面买（买生意）**：RVMD 类——突破性 franchise、durable 多年度 thesis、用户已重分类为"核心名"。纪律 = **信早信、宁可付溢价提前买，不等回踩**。理由：①最好的生意很少给你要的完美回踩；②等回踩常等于错过整段；③回踩常以"盘中暴力洗盘后收回"形式已发生（如 RVMD 8/26 从 $218 砸 $199 再收 $215，那次下杀就是 test，不是"还没回踩"）。
- **关键边界（Conviction 买≠无纪律）**：仍须两件事——①**小仓试错**（biotech/单催化票 binary risk，禁重仓）；②**Invalidation 位**（thesis 破位点，如 RVMD 收盘破 $179 结构 = 叙事受损），破了即走，而非"等回本"。
- **夜盘/盘前流动性警告**：Conviction 买若在下杀后夜盘执行，用**限价单**、接受点差，避免市价追薄流动性插针。
- **判定口诀**：用户说"这是好生意/核心名/要信早信"→ 走 Conviction 通道；用户说"技术突破/回踩/不追高"→ 走 Swing 通道。两者都禁"一把梭 + 无止损"。

## 买点三模式纪律（用户框架）
**低吸 / 尾盘 / 不追高**，且必须叠加量能过滤：
- 低吸 = 回踩结构位（MA5/支撑/箱底）缩量企稳买（pullback 本质）。
- 尾盘 = 收盘确认买入（契合 A 股 T+1 尾盘原则，过滤假突破）。
- 不追高 = 绝不在盘中拉升/尖顶延伸段追入。
- **量价配合是确认有效性的必要非充分条件**：收盘站回但量能太小 = 假确认/缩量上涨接力陷阱（参考 MDB 8-21 缩量收红后次日破位教训）。

## 目标价与退出条件推导（必填一级模块·用户框架修正）
作战计划**必须显式给出退出条件**，否则六维研究再完整也无法下单执行。目标价不靠拍脑袋，从三个角度交叉验证，取最保守可辩护者为基准；并必须写明"每档减多少、止损移到哪"。

- **技术目标**：①前高/历史阻力位（ATH 或近期平台高点）= **T2**；②次一级阻力（突破位+测量涨幅、或 0.618 延伸、或通道上轨）= **T1**；③均线/通道上轨作为跟踪止盈参考。
- **估值目标**：用同业 PE/PEG/PS 或 EV-EBITDA 给"合理中枢价"，高于中枢即透支、低于即安全边际；成长股用 forward PE × FY 一致预期 EPS 反推目标中枢。
- **催化目标**：若单催化定价（FDA/财报/法案），按市场一致预期幅度给情景价（乐观/中性/悲观），中性价=基准，乐观价=T2 上限。
- **T1/T2 命名约定**：T1 = 第一目标（较近阻力/估值中枢），T2 = 第二目标（前高/ATH/乐观情景）。写作时必须写清"价 + 来源依据"，不许只写"前高"。
- **减仓节奏（必写，与 T1/T2 绑定）**：达 T1 减仓 1/3~1/2 并**上移止损至成本线或 MA5 跟踪**；达 T2 再减 1/2，剩余用移动止损/时间止盈了结。必须写明每档减多少、止损移到哪。
- **失效即退（非目标价相关，见失效条件）**：催化证伪/板块转弱/龙头负反馈/放量破位 → 不等目标价直接走。

> 验证清单：交付前自问——读者不看正文，只看作战计划卡，能否一句话答出"我在什么价卖、卖多少、止损在哪"？答不出则模块不完整。

## 作战计划结构（输出必备）
0. **结构门控（前置，强制调用 `rule123.py`）**：先输出方向卡（`regime` / `setup` / `verdict`）。
   - **123 三项全过**：无论 regime，按突破买点写（量能不足则打折）。
   - `continuation` 且未完成 123：两张路径卡——**买点 A 回踩**（VWAP/0.382/EMA10 + 自 P1 未创新低 + 缩量）、**买点 B 突破**（收盘过 R1 + RVOL>1.5）。现价不在 A、也没触发 B → 不推荐，只写预案。
   - `reversal` 且 123 未完成：输出「123 趋势法则判定」卡；入场/止损/目标仅作预案。
   - `mixed` 且 123 未完成：观察。
   - 现价距买区 **>2×ATR** → 不追，等回踩或等突破确认。
1. **入场**：分买区 A（首选回踩）/ B（突破）/ C（预案）。**强股首选买区锚定 VWAP / 0.382 / EMA10**，深调 MA20 仅作极端情景；写明与哪条均线/结构/VWAP 重合；确认方式（尾盘+量比>1）。支撑阻力只用价格结构、均线、VWAP、斐波那契，不用期权墙。
2. **分仓路径**：试错仓 10–20% → 确认仓 +20–30% → 加仓仓 +20–30% → 极强势 >70%。不一把梭。
3. **止损（三维，用户框架硬约束）**：
   - 价格：结构位（前低/MA5/MA20/突破位）或幅度 -5%~-8%（按波动率，高β放宽）。
   - **ATR + 结构双校准，禁止用整数关/心理位当止损**（如科创板 171 错例：ATR≈9.28 元 5.4%，整数关必被扫）。
   - 触发写清：收盘破位 / 盘中破位 / 连续破位（插针不追）。
   - 逻辑止损：核心催化失效/板块转弱/龙头负反馈/放量破位。
   - 时间止损：短线 5–10 交易日、中线 1–2 月未验证则降仓。
4. **目标价与退出条件（必填，见上节）**：T1/T2 明确写出价 + 来源依据（技术阻力/估值中枢/催化情景三选其依据）；**减仓节奏**（达 T1 减 X%、上移止损至成本线/MA5；达 T2 再减 X%、剩余移动止损了结）；情绪止盈（换手>15%+放量滞涨）/ 时间止盈作为补充。与「入场」「止损」在作战计划卡中**并列首屏可见**，缺任一则计划不可执行。
5. **失效条件**：催化证伪/板块转弱/龙头负反馈/放量破位/次日反馈差。
6. **高β集中度检查（用户框架关键）**：汇总本票 β 与账户现有持仓 β，**禁止高β票双持叠加**；建议小仓战术或与现有高β票做轮动；环境不支持（大盘/板块/纳指转弱）则直说"不执行/降仓观望"。
7. **执行前环境适配**：大盘支持/中性/不支持、板块主线/跟风/退潮、资金进攻/防守/观望。

## 数据获取与脚本调用（环境自适应·强制）
**通用环境（无 wb-finance-skill）必须按此取数**，WorkBuddy 环境可选（已有更好数据源时优先用 wb-finance-skill，但仍可用本仓库脚本做结构判定）。

**路径写法**：脚本示例里的 `python fetch_market.py`、`python rule123.py` 是相对仓库根目录的写法。**实际运行时请以 skill 安装目录的绝对路径调用**，避免 Agent 当前工作目录不一致导致找不到脚本：
- WorkBuddy：`~/.workbuddy/skills/stock-multidim-battleplan/fetch_market.py`、`~/.workbuddy/skills/stock-multidim-battleplan/rule123.py`
- Cursor / 通用 Agent：本机已安装路径为 `C:\Users\luoyu\.cursor\skills\stock-multidim-battleplan\`（`fetch_market.py` / `rule123.py`）。美股若 `fetch_market.py`（Yahoo/stooq）失败，改跑 `scripts\fetch_market.js`（东财公开 JSON）。

下文示例仍用相对路径以保持简洁，但**执行时必须替换为绝对路径**。

### 1. 行情取数 → `fetch_market.py`（仓库根目录）
```bash
# A 股自动识别（6位数字：60/68 开头=沪，其余=深；也支持 .SS/.SZ/.BJ 后缀）
python fetch_market.py 601233 --out data/601233.json
python fetch_market.py 688002
# 美股走 Yahoo
python fetch_market.py CF --out data/cf.json
```
输出统一 JSON：ticker / market / name / spot / prev_close / open / high / low / volume / turnover / change_pct / pe_ttm / pb / market_cap / float_cap / bars[{d,o,h,l,c,v}] / source。

### 2. 结构判定 → `rule123.py`（方向感知）
```bash
python rule123.py 601233                 # A股
python rule123.py CF LLY MU              # 美股批量
python rule123.py CF --data data/cf.json # 直接消费 fetch_market.py 产出（推荐）
```
输出 `regime`（continuation / reversal / mixed）、`setup`（pullback / breakout / breakout_lightvol / wait / reversal_123 / base_breakout）、cond2（自 P1 未创新低）、均线、RVOL、verdict、recommend，并写入 `rule123_out.json`。HH 仅备注，不作门控。

> **环境探测技巧**：数据获取的优先级为 **① wb-finance-skill（WorkBuddy 内可用时）→ ② 本仓库 `fetch_market.py` → ③ WebSearch/WebFetch 手工检索兜底**。`fetch_market.py` 测的是本机能否直接取数，不是测 `wb-finance-skill` 在不在；如果身处 WorkBuddy 且 `wb-finance-skill` 可用，优先用它拿实时行情，再拿 `fetch_market.py` / `rule123.py` 做结构判定。若 `fetch_market.py` 也失败（无网络/无 Python/外网被墙），则退回 WebSearch + WebFetch 手工检索，并在研报标注"数据来源：公开检索"。

## 输出规范（环境自适应）
- **优先 HTML（环境支持文件写入+预览时）**：落盘 `C:\Users\luoyu\stock-reports\<TICKER>-<YYYYMMDD>.html`（不要写入代码仓库）。浅底深字研报风，首屏结论先行，含价格图（关键位 markLine：买区 A/突破位 R1/EMA10/SMA20/止损/目标/52w 高低）。**HTML 必须含独立的「目标价 / 退出条件」卡片**（T1/T2 价+依据+减仓节奏+移动止损），与「入场」「止损」卡并列、首屏可见。**不输出 Gamma / 期权流卡片。** 123 三项全过：写突破确认，附三条件勾选。`continuation` 且 123 未完成：两张路径卡（买点 A 回踩 / 买点 B 突破），不要叫「123 门控」。`reversal` 且 123 未完成：出「123 趋势法则判定」卡（逐项勾选，标 P0/P1/R1）。**预览环境屏蔽外链 CDN 导致整页空白时，生成 `*_standalone.html` 去外链版保底**（内联 JS，不引用 echarts CDN）。
- **无文件写入环境（通用 Agent）**：直接输出 **markdown 研报**到对话，结构同上（结论先行 → 方向/路径卡 → 六维 → 作战计划卡含入场/止损/目标价退出条件）。图表以文字/关键价位列表替代。
- 对话正文附 200–300 字摘要 + 文件路径（或 markdown 结论）。
- 含具体操作价位/买卖判断的，末尾必须附固定免责声明：
  > 免责声明：以上内容基于公开数据和量化分析，仅供参考，不构成投资建议。市场有风险，投资需谨慎。任何投资决策应结合个人风险承受能力、资金状况和投资目标独立判断，必要时咨询持牌专业机构。过往表现不预示未来收益。
- 所有关键数据附近标注来源 + 时点（YYYY-MM-DD）；非一手来源标"需核实原文"。

## 避坑
- 不做"逢低布局、注意风险"式模糊话；必须具体条件 + 具体动作。
- 止损位写完即定，触发后禁止重新评估/事后宽松（锚定偏差：成本价/前高不是市场卖出位）。
- 高估值≠必卖，看增速质量是否匹配；跨公司比 PE 先核财年口径。
- 区分噪音（盘中插针快速收回）与真失效（连续趋势性破位）。
